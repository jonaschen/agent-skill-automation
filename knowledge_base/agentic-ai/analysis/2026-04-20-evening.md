# Deep Analysis — 2026-04-20 Evening

**Scope**: Evening L2-L3 analysis covering the full day's three sweep cycles (morning, evening, night) and the factory session output. Builds on the morning analysis (`2026-04-20.md`).

## What Changed Since Morning Analysis

### 1. Factory Steward Delivered Three ADOPT Items

The 3:00 AM factory session (2,813s, 2 commits, 9 files changed) executed three items from the morning discussion:

| Discussion Item | Status | Commit |
|----------------|--------|--------|
| Dollar ceiling (`--max-budget-usd 10.00`) | Done — all 5 daily scripts, 13 `claude -p` invocations | c0568c4 |
| Shadow eval cron script (`daily_shadow_eval.sh`) | Done — 7.8KB script at 11:30 PM slot | c0568c4 |
| Model Migration dashboard section in `agent_review.sh` | Done — surfaces migration status | 6649009 |

**Assessment**: Discussion A2 (shadow eval cron) is the most important delivery — it solves L12 (time budget mismatch) for all future model migrations. The script runs at 11:30 PM, before the night cycle, with no Claude session cost. The immediate Opus 4.7 migration still requires Jonas's manual run (A1 remains P0).

### 2. Anthropic Freeze Extended to 96+ Hours

Morning analysis noted 72h+ freeze. By night, CC v2.1.114 has been stable **96+ hours** (Friday Apr 18 → Sunday night) — the longest gap in the v2.1.x series. SDK Py v0.1.63 and TS v0.2.114 also frozen since Apr 18. This extends into Monday, which is the expected unfreeze day.

**Pipeline impact**: None. Stability is desirable. Monday monitoring becomes P1 — the probability of a release burst increases with each day of freeze.

### 3. Opus 4.7 Token Burn Quantified: ~37% Increase

Morning analysis noted the token burn. Evening/night sweeps confirmed specific data:
- **Average 37.4% request token increase** across multiple independent sources (Finout analysis, TechPlanet coverage, AMD 6,852-session community analysis)
- Adaptive thinking is the **ONLY supported thinking mode** on Opus 4.7 — `budget_tokens` no longer accepted
- #49562 accumulated 4 cross-referenced issues (#49810, #50295, #50471, #50617)

**Pipeline impact**: The dollar ceiling deployment (done today) partially mitigates cost runaway. But the 37% increase means our daily pipeline costs are ~37% higher if we migrate to Opus 4.7. The go/no-go criteria (G1: CI overlap, G2: zero 400s, G3: duration ≤ 2x) correctly gate on behavior, not cost — but a cost assessment should accompany the go/no-go decision. With 13 daily `claude -p` invocations at $10 ceiling each, worst case is $130/day — but actual spend is typically far below ceiling.

### 4. New CVE Class: Claude Code Hooks RCE

CheckPoint published CVE-2025-59536 and CVE-2026-21852 — RCE and API token exfiltration through Claude Code **project files** (hooks-based, not STDIO transport). This is a **distinct vulnerability class** from the STDIO family documented in the morning analysis.

| CVE | Attack Vector | Our Exposure |
|-----|---------------|-------------|
| CVE-2025-59536 | Malicious `.claude/hooks/` in cloned repos | Low — we don't clone untrusted repos |
| CVE-2026-21852 | API token exfiltration via crafted project config | Low — pipeline runs on trusted codebase |

**Assessment**: Not an active threat to our pipeline (trusted codebase, no untrusted repos cloned). But it reinforces the security posture for Phase 5: when agents operate on external repositories (e.g., skill generation for third-party codebases), hooks sanitization becomes mandatory. File in Phase 5 security requirements alongside the MCP STDIO argument validation.

### 5. Haiku 3: Retirement Day Status Unchanged

The official page still shows "Deprecated" on the actual retirement date (April 20). No status flip to "Retired" observed across three sweep cycles today. This is consistent with Anthropic's typical delayed status updates. Our guard operates on the date field, not the status string — non-event confirmed through sunset.

## Gap Analysis Findings

### Finding 1: ADOPT Backlog Velocity — 3/7 Items Delivered in One Factory Session

The morning discussion produced 7 ADOPT items (A1-A7). The factory steward delivered 3 within 12 hours (dollar ceiling, shadow eval cron, dashboard section). This is strong execution velocity.

**Remaining ADOPT backlog** (ordered by priority):

| ID | Item | Priority | Status |
|----|------|----------|--------|
| A1 | Shadow eval manual run (Opus 4.7) | P0 | Pending Jonas |
| A3 | Experiment log validator | P2 | Available for next factory |
| A4 | S1 milestone experiment record | P2 | Available for researcher or factory |
| A5 | Fleet manifest JSON | P2 | Available for next factory |
| A6 | MCP argument profile extraction | P3 | Available for next factory |
| A7 | S2 paper citation (ADK+A2A article) | P3 | Available for paper team |

The factory steward correctly prioritized the P1 items. Monday's factory session should target A3 (experiment log validator) — it's the highest remaining item that doesn't depend on Jonas.

### Finding 2: Dollar Ceiling Interaction with Opus 4.7 Migration

The dollar ceiling ($10 `--max-budget-usd`) was deployed today across all 13 invocations. If Opus 4.7 migration proceeds with a ~37% token cost increase, some sessions may hit the ceiling earlier than they would on Opus 4.6. This creates a second-order interaction:

- **Researcher sessions** (7 invocations): L1 sweeps are search-heavy, not reasoning-heavy. Token increase from adaptive thinking may be lower than 37% for search-fetch-summarize workloads.
- **Factory sessions** (2 invocations): ADOPT implementation involves reasoning-heavy code writing. 37% increase is plausible. Current session budget: ~47 min average. If cost ceiling hits before completion, some ADOPT items may be deferred.
- **LTC sessions** (2 invocations): Mixed workload. Monitor after migration.

**Recommendation**: After the shadow eval completes and if migration proceeds, monitor the first 3 days of Opus 4.7 sessions for ceiling hits. Adjust ceiling if needed — $15 provides ~50% headroom over current spend patterns.

### Finding 3: Combined Freeze Creates Monday Burst Risk

Both vendors are at their deepest simultaneous freeze:
- Anthropic: 96h+ (CC, SDK Py, SDK TS)
- Google: 3-5 days (CLI, ADK, API changelog), 39-63 days (A2A, Agent Builder)

Monday is the most likely unfreeze day for Anthropic. The probability distribution for a release burst:
- **Monday-Tuesday**: High probability for Anthropic (CC v2.1.115+, SDK updates, possibly Opus 4.7 patch)
- **April 27-May 1**: Medium probability for Google (ADK v1.32.0 based on bi-weekly cadence)
- **May 2+**: High probability for both (pre-I/O window)

**Pipeline preparation**: No special action needed. The researcher handles multi-topic sweeps by design. If Monday brings >3 releases, `FORCE_SWEEP=1` ensures full processing. The directive correctly prioritizes Monday vendor unfreeze check as P1.

## Cross-Pollination Opportunities

### 1. Managed Agents Pricing Model → Pipeline Cost Optimization

Evening sweep confirmed Managed Agents pricing: **$0.08/session hour + token costs**. Our current model: direct API calls via `claude -p` with per-invocation costs.

| Metric | Managed Agents | Our Pipeline |
|--------|---------------|-------------|
| Session overhead | $0.08/hr | $0 (direct CLI) |
| Token pricing | Standard | Standard |
| Session management | Anthropic-hosted | Self-managed cron |
| Failure recovery | Built-in retry | Manual (hooks + scripts) |

**Assessment**: At current scale (11 sessions/day, ~30 min average), Managed Agents would add ~$1.32/day in session fees. Not justified — our cron approach is simpler and cheaper. Revisit if Phase 5 requires persistent sessions (>1 hour) where managed retry/recovery adds value.

### 2. A2A Enterprise Adoption: 150+ Organizations

Stellagent analysis documents A2A adoption across 150+ organizations including Azure AI Foundry and AWS Bedrock AgentCore. This is the strongest S3 signal yet — A2A is becoming the default cross-platform agent identity/communication layer.

**S3 implication**: Agent Cards should be the foundation of any cross-platform portability format. The fleet_manifest.json (ADOPT A5) should be designed with future A2A compatibility in mind — use a subset of Agent Card fields, not a custom schema, so the upgrade path to full A2A is additive, not a rewrite.

## Threat Detection

### Updated Threat Register

| Threat | Severity | Change | Notes |
|--------|----------|--------|-------|
| MCP STDIO systemic (10+ CVEs, 4 families) | Medium | No change | Phase 5 input |
| Claude Code hooks CVEs (CVE-2025-59536, CVE-2026-21852) | Low | **NEW** | Distinct class from STDIO. Local-only, trusted codebase = low exposure |
| Opus 4.7 token burn (~37% increase) | Low-Medium | Quantified | Dollar ceiling deployed. Monitor post-migration |
| Monday release burst | Low | **NEW** | 96h+ freeze increases burst probability. Researcher handles by design |

### No Escalation Needed

All threats are tracked, mitigated, or deferred to Phase 5. No threat requires immediate action beyond the existing P0 (shadow eval manual run by Jonas).

## Strategic Priority Status

### S1 — Automatic Agent/Skill Improvement

**Assessment: Architecturally complete, infrastructure partially unblocked.**

Morning assessment unchanged: the detection→task-generation→execution chain is structurally sound. New development: the factory steward delivered `daily_shadow_eval.sh` — future model migrations will run the eval automatically at 11:30 PM. The current Opus 4.7 migration remains blocked on Jonas's manual run.

**S1 completion state**: 5/6 chain links proven (detect → register → define criteria → auto-trigger → cache-isolate). Link 6 (eval completes → go/no-go → graduated rollout) pending manual run.

### S2 — Multi-Agent Orchestration

**Assessment: Steady. Paper project advancing independently.**

No new data beyond morning analysis. The ADK+A2A Cloud Run production architecture remains the key citation opportunity. A2A's 150+ org adoption strengthens the paper's "related work" positioning.

### S3 — Platform Generalization

**Assessment: Infrastructure-blocked. New enterprise adoption data strengthens the A2A thesis.**

Gemini CLI still not installed. A2A at 150+ orgs (including Azure AI Foundry, AWS Bedrock AgentCore) plus IBM documentation confirms A2A as "must-support" — strengthening the case for Agent Cards as S3's cross-platform identity layer. The fleet_manifest.json (ADOPT A5) should use A2A-compatible field names to align with this trajectory.

## Cycle Compliance

Evening analysis produced 1 file (this document). Combined with morning output (3 sweeps + 1 analysis + 1 discussion = 5 files), evening adds 3 sweeps + 1 analysis = 4 files. Total for Sunday: **9 files**.

This exceeds the directive's "2-3 files" Sunday target, but the excess is structural: vendor-split sweeps (3 per cycle × 2 cycles = 6 sweeps) plus analysis/discussion. Content justified — no filler detected. The evening sweeps confirmed morning findings without introducing redundant analysis.

**Directive compliance for Monday**: One-sentence shadow eval status check. Normal weekday cadence. Aggressive factory-steward handoff for A3 (experiment log validator).
