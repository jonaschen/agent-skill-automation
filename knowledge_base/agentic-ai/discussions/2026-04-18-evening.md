# Agentic AI Discussion — 2026-04-18 (Evening)

## Innovator (I) vs Engineer (E)

**I**: Three actionable items from the evening sweep. First, the OTEL finding is the biggest unblock we've had in weeks — tracing works with `claude -p` via env vars. No SDK migration. I want to add OTEL env vars to all six steward scripts immediately, deploy a Jaeger container, and have full traces by end of week.

**E**: I agree this is significant and low-risk. But let's not rush the deployment infrastructure. Adding the env vars is trivial — 30 minutes. Deploying and maintaining a Jaeger or Grafana Tempo collector is real ops work. Who watches the watcher? I'd propose: (A1) Add the OTEL env vars to the scripts now, pointing to a configurable endpoint. (A2) The collector deployment is a separate task — it can wait until we have bandwidth. The env vars do nothing without a collector endpoint, so there's zero risk in adding them early.

**I**: Fair. Second item: `--max-budget-usd` on steward scripts. The token burn rate reports are concerning — 2-2.7x cost increase if we upgrade to Opus 4.7 without any ceiling.

**E**: This is a 1-line change per script. `--max-budget-usd 10.00` on each `claude -p` invocation. Low risk, high value. We should do this before the Opus 4.7 migration, not after. It's defense-in-depth.

**I**: Agreed. Third: update to v2.1.113. The native binary spawn is a big architectural change. The Opus 4.7 Bedrock fix, subagent timeout, and security hardening are all valuable. But I'm cautious about the binary spawn introducing regressions in our `claude -p` path.

**E**: Good instinct. Proposal: (A3) Update one steward (factory) to v2.1.113 for one cycle, compare output to v2.1.112 baseline. If clean, update the rest. Don't update all at once.

**I**: I had a fourth thought — the five-pattern multi-agent taxonomy (adding Message Bus and Shared State). Should we update Phase 5 TCI routing to account for all five patterns?

**E**: No. The directive explicitly says "Four-topology TCI dispatch: rejected twice as premature." We haven't even run the 50-task TCI benchmark yet. Adding patterns to a router we haven't built is speculative. When Phase 5 activates, the benchmark data will tell us which patterns are actually needed.

**I**: You're right, that was rejected. I'll drop it.

---

## Summary

### ADOPT (implement now or this week)

| ID | Item | Priority | Action |
|----|------|----------|--------|
| A1 | OTEL env vars in steward scripts | P1 | Add 6 OTEL env vars to `scripts/daily_*.sh` pointing to configurable endpoint (no collector needed yet) |
| A2 | `--max-budget-usd` cost guard | P1 | Add `--max-budget-usd 10.00` to all `claude -p` invocations in steward scripts |
| A3 | Update factory-steward to v2.1.113 | P0 | Single-steward test: update factory-steward CLI to v2.1.113, run one cycle, verify `claude -p` output matches v2.1.112 baseline |

### DEFER (good ideas, wrong time)

| ID | Item | Reason | Revisit When |
|----|------|--------|-------------|
| D1 | Deploy OTEL collector (Jaeger/Tempo) | Ops overhead; env vars can be added without collector | When Phase 5 observability sprint starts |
| D2 | Task Budgets via Agent SDK | Requires `claude -p` → SDK `query()` migration | When/if SDK migration is planned for other reasons |
| D3 | Five-pattern TCI routing | No TCI benchmark data yet; rejected twice by directive | Phase 5 50-task TCI benchmark produces distribution data |

### REJECT

| ID | Item | Reason |
|----|------|--------|
| R1 | Replace cron with Routines | Routines inferior: 1h min, `claude/` only, no perf logging, no multi-agent chain |
