# Discussion — 2026-04-10

**Input**: Deep Analysis 2026-04-10 + ROADMAP.md + CLAUDE.md
**Format**: Innovator/Engineer structured debate (3 rounds)
**Goal**: Identify actionable improvements to the agent-skill-automation pipeline

---

## Round 1

### INNOVATOR

**Proposal 1.1: A2A-Native Message Bus for Phase 5 (not a wrapper)**

The analysis confirms A2A crossed the irreversibility threshold — 150+ orgs, triple hyperscaler integration. Our Phase 5 scrum-team-orchestrator currently specifies a custom 6-message-type bus. Instead of building a custom bus and then wrapping it with A2A later, we should design Phase 5.3 directly on the A2A Python SDK. Concretely:

- Each agent publishes an A2A AgentCard at registration (capabilities, model, tools)
- The 6 message types (TASK_ASSIGNMENT, PARTIAL_OUTPUT, etc.) become A2A Task metadata fields
- The scrum-team-orchestrator discovers agents via AgentCard queries, not a static routing table
- File to update: `.claude/agents/scrum-team-orchestrator.md` (doesn't exist yet) — design it A2A-native from day one

This eliminates a future migration from custom→A2A and makes our Phase 7 AaaS immediately interoperable with enterprise A2A infrastructure.

**Proposal 1.2: Factory-Steward Structured Inbox**

The researcher writes ADOPT items into discussion transcripts. The project-reviewer writes steering notes. The factory-steward reads both ad-hoc at session start. This is lossy — ADOPT items get buried in prose, and there's no state tracking (NEW/IN_PROGRESS/DONE).

Proposal: Create `knowledge_base/agentic-ai/inbox.md` as a structured work queue:
```
| ID | Source | Priority | Status | Item | Created |
```
- Researcher appends ADOPT items with `Status: NEW` after each discussion
- Project-reviewer appends steering notes similarly
- Factory-steward reads inbox at session start, picks highest priority NEW item, marks IN_PROGRESS, marks DONE on completion
- Simple markdown table — no new tooling needed

Files to update: `agentic-ai-researcher.md` (append to inbox after discussions), `factory-steward.md` (read inbox at start), `project-reviewer.md` (append steering items).

**Proposal 1.3: Claude Code Fleet Version Pinning**

The analysis flags MCP memory leak (50 MB/hr), 429 retry burn, and permission bypass fixes in v2.1.97. Our daily scripts don't pin or verify the Claude Code version. A broken update could silently degrade all 8 daily runs.

Proposal: Add a version check to `scripts/lib/` — a shared function sourced by all daily scripts that runs `claude --version`, compares against a pinned version in `scripts/lib/fleet_version.txt`, and warns (not blocks) on mismatch. This gives us:
- Visibility into which version the fleet is actually running
- A single file to update when we intentionally upgrade
- Warning logs when an OS-level update changes the binary unexpectedly

---

### ENGINEER

**Response to 1.1 (A2A-Native Bus):**

Strong idea, wrong timing. Phase 5 is blocked by Phase 4 completion (deadline May 9). A2A v1.1 is expected at Google I/O (May 19-20). The ROADMAP already defers A2A evaluation to post-I/O (task 5.3.0). Designing the bus now on A2A v1.0 risks rework if v1.1 changes the SDK surface.

However, the *design principle* is sound: don't build custom-then-migrate, build A2A-native. The right action is a **design note** in the ROADMAP Phase 5.3 section that says "implement on A2A SDK, not as a wrapper" — zero implementation cost now, correct direction later.

The AgentCard discovery idea is genuinely valuable but adds complexity. Our 6-agent fleet is small enough that static routing works. AgentCard discovery pays off at 20+ agents or when external agents join. Defer the dynamic discovery to Phase 5 implementation and evaluate then.

**Verdict: DEFER** — Record A2A-native design principle in ROADMAP 5.3. No implementation until post-I/O evaluation completes.

**Response to 1.2 (Structured Inbox):**

This is a real problem. I've seen the discussion transcripts — ADOPT items are embedded in prose paragraphs. The factory-steward has to parse natural language to find them. Some get missed.

But: the proposed markdown table adds coordination overhead to three agents. Every agent now needs table-editing logic, and markdown tables are fragile to concurrent edits (two agents appending simultaneously = merge conflict).

Simpler alternative: Keep the discussion format as-is, but have the researcher append a machine-readable `## ADOPT Items` section at the bottom of each discussion file with a simple bullet list. The factory-steward greps for `## ADOPT Items` in the most recent discussion file. Status tracking via commit messages ("implemented ADOPT item: X").

This gets 80% of the value (structured items, easy discovery) without the coordination cost (no shared mutable state, no merge conflicts, no table parsing).

**Verdict: ADOPT (simplified)** — P2. Researcher appends structured `## ADOPT Items` section to discussion files. Factory-steward greps for it. No shared inbox file.

**Response to 1.3 (Fleet Version Pinning):**

Clean, low-risk, immediately useful. The only concern: `claude --version` adds ~1s latency to every daily script startup. For 8 runs/day that's 8 seconds total — negligible.

One refinement: make the pinned version a minimum, not an exact match. We want `>=2.1.97`, not `==2.1.97`. Exact pinning would require manual updates on every Claude Code release, even for patch versions that don't affect us.

**Verdict: ADOPT** — P1. Create `scripts/lib/check_fleet_version.sh` and `scripts/lib/fleet_min_version.txt`. Source from all daily scripts. Warn on mismatch, never block.

---

## Round 2

### INNOVATOR

**Proposal 2.1: Watchdog Memory Monitoring for Phase 5**

The analysis identifies MCP memory leak as a systemic concern for longer Phase 5 multi-agent sessions. Our Phase 5.4 watchdog-circuit-breaker currently monitors token velocity only. Proposal: add process RSS tracking to the watchdog spec.

Concretely, update `.claude/agents/watchdog-circuit-breaker.md` (when written) to include:
- Monitor `/proc/<pid>/status` VmRSS field every 60s during multi-agent sessions
- Alert threshold: RSS growth >200MB/hr sustained for 10 min
- Halt threshold: RSS exceeds 4GB absolute (configurable)
- Log RSS time series to `logs/watchdog/memory_<session>.jsonl` for post-mortem

This is cheap to implement (shell one-liner reading /proc), catches memory leaks from any source (not just MCP), and provides forensic data for debugging.

**Proposal 2.2: Eval Suite Expansion — Promote Real-World Cases**

We have `scripts/promote_cases.py` and `scripts/skill_logger_hook.sh` installed in two projects. But the promote_cases script hasn't been run at scale — our eval suite is still the original 54 prompts. The analysis mentions our 44-prompt test set (now 54 with G18 additions).

Proposal: Run a one-time audit of accumulated skill usage logs, identify the top 10 most informative real-world triggers that aren't already represented in the eval set, and promote them. This directly improves measurement quality — the currency of Phase 3.

Files: `scripts/promote_cases.py` (already exists), skill logger logs from long-term-care-expert and The-King-s-Hand projects.

**Proposal 2.3: Pre-I/O Monitoring Automation**

The analysis recommends starting I/O monitoring in late April (pre-leak window ~May 1-5). Currently the researcher runs a generic sweep. Proposal: add I/O-specific tracking queries to the researcher's sweep for the April 25 - May 20 window:

- Track: ADK v2.0 release indicators, A2A v1.1 draft, Gemini 4/3.2 announcements, Android XR SDK
- Auto-tag findings with `[I/O-WATCH]` in sweep reports
- Factory-steward treats `[I/O-WATCH]` items as potential Phase 5 inputs, logs them but doesn't act until post-I/O

This is already partially done (researcher has I/O tracking queries from task 4.4). The delta is: formalize the tagging and factory-steward awareness.

---

### ENGINEER

**Response to 2.1 (Watchdog Memory Monitoring):**

Good addition but premature to implement. Phase 5.4 (watchdog-circuit-breaker) doesn't exist yet — the agent definition hasn't been written. Adding memory monitoring to a spec that doesn't exist is writing design notes, not code.

Also, `/proc/<pid>/status` monitoring requires knowing the PID of the agent process. In our cron-based fleet, the Claude Code process is spawned by the daily script and terminates when done. For Phase 5 multi-agent sessions, process topology gets complex (parent orchestrator + child agents). The monitoring approach needs to handle that.

The principle is right: memory should be a watchdog signal alongside tokens. Record it as a Phase 5.4 design requirement. Don't spec thresholds yet — they should be derived from actual Phase 5 session profiles.

**Verdict: DEFER** — P2 design note for Phase 5.4. Record "RSS monitoring alongside token velocity" as a requirement. Thresholds TBD from real data.

**Response to 2.2 (Eval Suite Expansion):**

This is the highest-value proposal so far. Our measurement infrastructure is only as good as our test prompts. Real-world usage data is the best source of new test cases — it captures patterns we didn't anticipate.

One concern: promote_cases.py needs actual log data. Do we have enough logged invocations? The memory file says the skill logger is installed in long-term-care-expert and The-King-s-Hand. If there are <20 logged invocations, the audit won't yield much.

Prerequisite check: count the logged invocations first. If >50, run the audit. If <20, defer until more data accumulates.

**Verdict: ADOPT (conditional)** — P1. Check log volume first. If sufficient (>50 invocations), run `promote_cases.py` audit and add top 10 cases to eval suite. Update `splits.json` accordingly.

**Response to 2.3 (Pre-I/O Monitoring Automation):**

Already mostly done. The researcher agent was updated in task 4.4 with I/O-specific sweep queries. The `[I/O-WATCH]` tagging is a nice-to-have but adds parsing complexity for marginal benefit — the researcher already writes I/O findings in a recognizable format.

The factory-steward awareness piece is the actually useful part: factory-steward should know that I/O-tagged items are "monitor only, don't implement" until post-May 20. But this is a one-line instruction addition to `factory-steward.md`, not a new system.

**Verdict: ADOPT (minimal)** — P2. Add one instruction to `factory-steward.md`: "Between April 25 and May 20, treat I/O-related research findings as monitoring inputs only — log but don't implement until post-I/O evaluation completes."

---

## Round 3

### INNOVATOR

**Proposal 3.1: Settings.json Prototype Collision Audit**

The analysis flags P1: audit `.claude/` config files for JS prototype property names that could silently bypass permission rules in pre-v2.1.97. This is a 30-second grep that either confirms we're clean (most likely) or reveals a real security gap. Zero cost, high assurance.

Run: `grep -rn 'toString\|valueOf\|hasOwnProperty\|constructor\|__proto__' .claude/settings.json .claude/agents/*.md`

**Proposal 3.2: Phase 5.3.0 Evaluation Scope Expansion**

The analysis recommends expanding the A2A evaluation (task 5.3.0) from "can A2A express our 6 message types?" to "what SDK integration pattern gives us A2A-native transport?" This is a text edit to ROADMAP.md — change the task description to reflect the expanded scope. The evaluation itself is still deferred to post-I/O.

This costs nothing now but ensures that when we do the evaluation, we answer the right question. The original scope was written when A2A was speculative. Now that all three hyperscalers are on board, the question isn't "should we use A2A?" but "how do we integrate the A2A SDK?"

**Proposal 3.3: Effort Monitoring Resolution (Day 2 of 3)**

Today is day 2 of the 3-day effort monitoring window (Apr 9-12). The analysis mentions continuing to monitor. Rather than waiting passively, we should run `scripts/effort_impact_analysis.sh` now to see the day 1-2 trend. If costs are stable, we can close the monitoring window early and mark the effort-level task as resolved. If costs spiked, we have a day to react before the window closes.

This is operational hygiene, not a new feature — but it prevents the monitoring window from silently expiring without a decision.

---

### ENGINEER

**Response to 3.1 (Prototype Collision Audit):**

Just do it. This is a security verification with zero cost and immediate value. The grep either returns nothing (clean — log the result and move on) or returns something (fix immediately). No reason to defer a 30-second check.

**Verdict: ADOPT** — P0. Run the grep, log the result. If clean, record in today's work. If not clean, escalate immediately.

**Response to 3.2 (Phase 5.3.0 Scope Expansion):**

Correct and trivial. The ROADMAP task description should reflect the current understanding. Update the text in Phase 5.3.0 to say: "Evaluate A2A v1.1 SDK integration patterns for scrum-team-orchestrator transport" instead of just message format mapping. Add a note that the Python A2A SDK is the expected implementation target.

**Verdict: ADOPT** — P1. One-line ROADMAP edit. No implementation change.

**Response to 3.3 (Effort Monitoring Check):**

Practical and timely. The monitoring window was set up precisely to catch cost regressions. Running the analysis script on day 2 gives us an interim signal. If the trend is flat, we don't need to wait until day 3 — we can close with confidence.

One nuance: we should still check day 3 data even if day 2 looks clean. Cost patterns can vary by day-of-week (weekend vs weekday agent loads differ). But an early interim check is still valuable for catching obvious problems.

**Verdict: ADOPT** — P1. Run `effort_impact_analysis.sh` today. If stable, plan to close monitoring window on Apr 12 with final confirmation. If elevated, investigate before day 3.

---

## Summary

### ADOPT (implement now or this week)

| ID | Item | Priority | Action |
|----|------|----------|--------|
| A1 | Prototype collision audit | P0 | Run grep on `.claude/` configs for JS prototype names. 30-second verification |
| A2 | Fleet version check | P1 | Create `scripts/lib/check_fleet_version.sh` + `fleet_min_version.txt`, source from all daily scripts |
| A3 | Phase 5.3.0 evaluation scope expansion | P1 | Update ROADMAP task 5.3.0: "SDK integration patterns" not just "message format mapping" |
| A4 | Effort monitoring interim check | P1 | Run `effort_impact_analysis.sh` on day 2; plan final resolution on Apr 12 |
| A5 | Eval suite expansion from real-world logs | P1 | Check log volume (>50 threshold), then run `promote_cases.py` to add top 10 cases |
| A6 | Structured ADOPT items in discussions | P2 | Researcher appends `## ADOPT Items` section to discussion files; factory-steward greps for it |
| A7 | Factory-steward I/O freeze instruction | P2 | Add instruction: I/O-related findings are monitor-only between Apr 25 and May 20 |

### DEFER (good ideas, wrong time or missing prerequisites)

| ID | Item | Reason | Revisit When |
|----|------|--------|-------------|
| D1 | A2A-native bus design (Phase 5.3) | A2A v1.1 expected at I/O; designing on v1.0 risks rework | Post-I/O (after May 20) |
| D2 | AgentCard-based discovery | Fleet too small (6 agents) for dynamic discovery to pay off | Phase 5 implementation (20+ agents) |
| D3 | Watchdog RSS memory monitoring | Phase 5.4 agent doesn't exist yet; thresholds need real session data | Phase 5.4 design time |

### REJECT

| ID | Item | Reason |
|----|------|--------|
| — | None this round | All proposals were either adoptable or deferrable with clear rationale |

---

*Discussion generated 2026-04-10 by claude-opus-4-6 (Innovator/Engineer structured debate)*
*Input: Deep Analysis 2026-04-10 + ROADMAP.md Phase 4 state + CLAUDE.md architecture*
