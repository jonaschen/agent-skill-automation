# Research Directive — 2026-04-21

**Issued by**: agentic-ai-research-lead
**Effective**: 2026-04-21 afternoon through 2026-04-23 (next directive)
**Prior directive**: 2026-04-20-night.md

## Strategic Context

The Opus 4.7 shadow eval finally ran and returned **NO-GO** (posterior mean 0.683, CI [0.535, 0.814], NG1: mean < 0.75). This is the most consequential finding since the shadow eval infrastructure was built. The S1 meta-optimization loop worked exactly as designed: detect new model → evaluate → reject unsuitable upgrade → hold on current model. The system correctly prevented a quality regression. This is an S1 success, not a failure.

Both vendors remain frozen into Monday afternoon (~130h+ Anthropic, day 4-6 Google). The Monday unfreeze did NOT materialize — unprecedented for a weekday. This may signal coordinated larger releases staging on both sides.

Researcher directive compliance today was **excellent**. One discussion, 4 ADOPT items (under 5-cap), one-sentence stable topics. Volume controls working. Content quality high — the discussion's ADK v2.0 comparison framework (A2) and experiment log validator tests (A4) are well-reasoned strategic prep work.

## Strategic Alignment

### S1 — Automatic Agent/Skill Improvement
- **Major milestone achieved**: The full S1 meta-optimization loop executed end-to-end for the first time. The pipeline autonomously detected Opus 4.7, built shadow eval infrastructure, ran the evaluation, and correctly rejected the migration. This validates the architecture described in `strategic-priorities.md` — "Can we build a capability diff that compares a new release's features against our agents' assumptions and generates upgrade tasks?"
- **Shadow eval verdict**: NO-GO. Opus 4.7 scores 0.683 on T=39 (27/39 pass), below the 0.75 minimum threshold. 12 test failures — likely a mix of routing regression from adaptive reasoning and tokenizer changes. The result falls within the baseline CI [0.702, 0.927] overlap range (G1 PASS), meaning it's not a catastrophic failure, but the mean is too low for migration.
- **Next S1 action**: Wait for Opus 4.7 adaptive reasoning tuning patch (#49562). When it ships, re-run shadow eval. The infrastructure is in place — `daily_shadow_eval.sh` handles this automatically if `PENDING_MIGRATION_MODEL` remains set.
- **Shadow eval topic: CLOSED for researcher.** The diagnosis is complete, the infrastructure works, the result is logged. No further analysis, discussion, or proposals about shadow eval until a new model or patch arrives. One-sentence status checks only.

### S2 — Multi-Agent Orchestration
- **ADK v2.0 comparison framework (A2)** is excellent Phase 5 prep. When built, it gives the Design Freeze week a structured input for the highest-priority S2 cross-pollination opportunity.
- **Paper project**: Advancing independently. Production A2A+ADK+Cloud Run article (from Sunday analysis) is a strong citation for the S2 paper's real-world validation section.
- **No new S2 developments** from either vendor during the freeze.

### S3 — Platform Generalization
- **Fleet manifest with A2A field mapping (A1)** is the right incremental S3 step. Machine-readable fleet state that maps to Agent Card schema is low-cost, high-value groundwork.
- **Gemini CLI still not installed** — S3 transpiler prototype blocked.
- **No change** in S3 research surface.

## Priority Topics (April 21-23)

### P0 — Must Research

- **Vendor freeze break monitoring**: Both vendors are staging releases behind the deepest combined freeze of the era. When the freeze breaks, multiple releases may land simultaneously.
  - **What to look for**: CC v2.1.115+ (especially Opus 4.7 adaptive reasoning tuning), ADK v1.32.0, SDK updates, Haiku 3 official "Retired" status.
  - **Why**: Accumulated unreleased work behind a 130h+ freeze = elevated burst probability. An Opus 4.7 patch specifically changes our S1 posture (triggers shadow eval re-run).
  - **Output**: Standard sweep report(s). If Opus 4.7 patch ships, flag prominently — this unblocks the next S1 evaluation cycle.

### P1 — Should Research

- **Opus 4.7 NO-GO failure analysis** (one-time, targeted): The shadow eval showed 12 failures out of 39 tests. Which tests failed? Were they positive tests (routing regression) or negative tests (false triggers)? Understanding the failure pattern tells us whether the issue is fixable (description tweak for 4.7's routing behavior) or fundamental (4.7's adaptive reasoning is incompatible with our trigger patterns).
  - **What to do**: If `experiment_log.json` or eval output contains per-test results, analyze the failure pattern. If not, document what data is missing for the factory to collect on next re-run.
  - **Why**: The difference between "4.7 routes differently but we can adapt" (S1 recoverable) and "4.7 fundamentally changes trigger behavior" (S1 blocked until Anthropic patches) determines our entire S1 posture for the next month.
  - **One-time analysis, not ongoing tracking.** Write findings in the sweep report or analysis. Do not create a separate document.

- **1M context beta sunset (9 days, April 30)**: Check if Anthropic publishes migration guidance. Otherwise one sentence: "Non-issue for fleet (GA 4.6 models)."

### P2 — Watch Only

- **Google I/O pre-leak window**: Opens ~May 2 (~11 days). Standard monitoring for ADK v2.0, Gemini 4, A2A v1.1 leaks.
- **Opus 4/Sonnet 4 retirement (June 15, 55 days)**: Already guarded in `deprecated_models.json`. No action unless migration guidance changes.
- **MCP STDIO**: One sentence max. No new CVEs expected during freeze.

## Deprioritized

- **Shadow eval re-analysis**: CLOSED. Result logged (NO-GO). Infrastructure validated. No further discussion until a new model or patch.
- **Four/five-topology TCI dispatch**: Rejected. Do not propose.
- **Managed Agents adapter**: Phase 5.
- **Conway/persistent agents**: No updates.
- **Agent payment protocols**: Phase 7.
- **Computer Use**: Mention only if GA announced.
- **Routines vs. cron**: Closed.
- **Task Budgets CLI integration**: Closed (API-only).
- **Session Storage Python SDK**: Closed until Python SDK ships it.
- **S3 transpiler prototype**: Blocked on Gemini CLI install.
- **EAGLE3**: Available. Stop tracking.
- **Burst mode / factory handoff / pre-migration cost extrapolation**: Rejected per prior directives.
- **Generalized lifecycle event system**: Deferred to Phase 5.
- **Teach & Repeat → S1**: Deferred. Add to `strategic-priorities.md` research questions when document next updated.

## New Research Areas

- **Opus 4.7 failure pattern characterization** (P1, one-time): Understanding which test categories fail on 4.7 is new information that shapes S1 strategy. This is not ongoing tracking — it's a one-time diagnostic to be completed in the next sweep cycle.

## Research Quality Feedback

### Directive Compliance Assessment (2026-04-20-night directive → Monday output)

**Overall: EXCELLENT. Best compliance day since volume controls were introduced.**

1. **One discussion per quiet day: COMPLIANT.** One discussion file produced (2026-04-21.md). Three rounds, 4 ADOPTs, 2 DEFERs, 2 REJECTs. Clean and focused.

2. **5-ADOPT cap: COMPLIANT.** 4 items adopted (A1 fleet manifest, A2 ADK comparison, A3 design index, A4 validator tests). All four connect to strategic priorities (S3, S2, operational, S1 respectively).

3. **One-sentence stable topics: COMPLIANT.** Shadow eval reported as "pending" in one sentence. Haiku 3 reported as "guard validated." Opus 4.7 as "no patch." MCP STDIO not mentioned (correctly — no new CVEs). Good discipline.

4. **Proposal file discipline: COMPLIANT.** One `team-2026-04-21.md` file consolidating all 4 proposals. No per-cycle split. This is exactly what was requested.

5. **Content quality: HIGH.** The ADK v2.0 comparison framework (A2) is the standout item — it correctly identifies the I/O-to-Design-Freeze timeline crunch and pre-builds the analytical structure to address it. The null hypothesis requirement from the Engineer is a valuable addition. The deferred items (lifecycle event generalization, Teach & Repeat) were correctly triaged — the Engineer's reasoning was sound in both cases.

6. **Discussion self-assessment: COMPLIANT.** The discussion included its own directive compliance self-check at the end. This metacognitive step is valuable and should continue.

### What to Do Differently Next Cycle

1. **Continue current volume discipline.** Today's output was well-calibrated for a frozen Monday. Maintain this level even when the freeze breaks — the temptation to produce 3 cycles on a release day must be resisted unless >3 simultaneous releases justify extended analysis.

2. **Opus 4.7 NO-GO failure analysis (P1, new).** The shadow eval result opens a genuine analytical question: what specific failure pattern does 4.7 exhibit? This is new territory — prior cycles didn't have eval data to analyze. The researcher should investigate this as a P1 one-time task and integrate findings into the next sweep report. This is NOT an open-ended research direction — it's a specific diagnostic with a bounded answer.

3. **Factory queue is reasonable.** The discussion produced 4 ADOPT items (A1-A4), all P2-P3. The factory steward delivered shadow eval hardening + experiment log validator + ROADMAP entries today (commit 3c4ebcf). Factory throughput is healthy — ~3 items per session, queue growing at ~4 items per day on active days. No backlog concern.

## Team Recommendations

### Factory-Steward Priorities (April 22-23)

Priority-ordered queue for next factory sessions. Shadow eval NO-GO means no migration work — factory can focus on Phase 5 prep:

1. **A4: Experiment log validator inline tests** (P2, 10 min): 3 test cases in `eval/validate_experiment_log.py`. S1 critical path integrity.
2. **A1: Fleet manifest** (P2, 30 min): Build `fleet_manifest.json` with A2A Agent Card field mapping. Schema validation script as follow-up.
3. **A2: ADK v2.0 ↔ TCI comparison framework** (P2, 30 min): Pre-fill our design dimensions from existing 8+ design docs, leave ADK v2.0 GA columns blank, include null hypothesis.
4. **A3: Phase 5 design index** (P3, 20 min): Annotated index of Phase 5 design documents with decision status (Decided / Blocked on I/O / Pending).
5. **Carried forward**: Programmatic Tool Calling deny rule (P1, human action — settings.json write), G20 MCP false-positive tests (P2).

### Human Action Items for Jonas

1. ~~Run shadow eval manually~~ **RESOLVED** — shadow eval ran (commit befdca9), result: NO-GO. No manual action needed.
2. **Install Gemini CLI** — gates all S3 research beyond format comparison.
3. **Upgrade Claude Code** to v2.1.114+ (fleet version check warns on mismatch).
4. **Review Opus 4.7 NO-GO decision**: The pipeline correctly rejected migration. When Anthropic patches adaptive reasoning (#49562), the cron will automatically re-evaluate. No human action needed unless Jonas wants to override the NO-GO threshold.

### No Structural Team Changes

The two-agent structure (researcher + factory-steward) continues to perform well. Volume controls are working. Factory throughput matches research output rate. The Opus 4.7 NO-GO result validates the S1 infrastructure investment — the system works.

---

*This directive supersedes 2026-04-20-night.md. The researcher should read this at the start of its next sweep. Key change: shadow eval is RESOLVED (NO-GO). New P1: analyze 4.7 failure pattern (one-time). All volume controls remain in effect.*
