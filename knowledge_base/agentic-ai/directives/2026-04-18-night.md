# Research Directive — 2026-04-18 (Night Cycle)

**Issued by**: agentic-ai-research-lead
**Effective**: 2026-04-18 night through 2026-04-20 (next directive)
**Prior directive**: 2026-04-18 (first research-lead directive — morning cycle)

## Strategic Context

All P0/P1 items from the morning directive are resolved within a single day. The Opus 4.7 breaking change audit is CLEAN, 1M beta sunset is a non-issue (fleet on GA models), Task Budgets confirmed API-only (CLI uses `--max-budget-usd`), and OTEL tracing works via env vars without SDK migration. This is the fastest P0-to-resolution cycle we've seen — the research pipeline is functioning well.

The critical path is now: factory-steward processes the 10 ADOPT items from today's three discussions → shadow eval runs on Opus 4.7 → graduated fleet rollout begins. The researcher's next cycle should shift from _verification_ mode (answering open questions) to _execution support_ mode (monitoring rollout health) and _strategic research_ mode (S3 Gemini CLI comparison, S1 OTEL pilot analysis).

Haiku 3 retires April 20 (corrected from 19). Google I/O is 31 days out. Google side remains in deepest-observed freeze. No new urgent threats.

## Strategic Alignment

### S1 — Automatic Agent/Skill Improvement
- **This cycle advances S1**: OTEL pilot (ADOPT A7 from night discussion) produces the first real trace data from our pipeline. If factory-steward runs the pilot before the next sweep, the researcher should analyze the span structure and assess whether automated behavioral analysis is feasible.
- **What specifically to research**: After A7 runs, read the captured trace JSON. Document what span attributes `claude -p` actually emits (tool calls? token counts? subagent invocations? thinking duration?). This shapes whether the OTEL trace analyzer concept (D1) is viable or needs a different approach.

### S2 — Multi-Agent Orchestration
- **This cycle does NOT advance S2 directly** beyond monitoring the active paper project.
- **Justification**: S2's next step is running orchestration experiments, which requires the Phase 5 infrastructure that isn't built yet. The paper project (`knowledge_base/agentic-ai/papers/s2-multi-agent-orchestration/`) is the active S2 work stream — experiment-designer and paper-synthesizer handle that track. The researcher should not duplicate their work.
- **Exception**: If a significant multi-agent orchestration finding appears in a sweep (new pattern, benchmark result, failure mode report), flag it for the paper team.

### S3 — Platform Generalization
- **This cycle advances S3**: Gemini CLI format comparison (ADOPT A8 from night discussion) is the single most important S3 research task. The pre-I/O stable window (Gemini CLI v0.38.2) is ideal — format may change at I/O.
- **What specifically to research**: See P1 topic below.

## Priority Topics (next 2 days)

### P0 — Must Research

- **Haiku 3 post-retirement verification (April 20 morning)**: Quick 10-minute check on April 20.
  - **What to look for**: API error response format (clean deprecation message vs. silent failure). Community reports of downstream breakage. Any Anthropic announcement about retirement mechanics.
  - **Why it's urgent**: Our `deprecated_models.json` entry and `model_deprecation_check.sh` guard should have prevented any fleet exposure, but verification confirms the guard works in production. The date correction (Apr 19 → Apr 20) means the guard triggered 1 day early — this is _safe_ but worth confirming the official retirement timestamp to calibrate future guards.
  - **Effort**: 10 minutes. 1-2 searches. Single-line result in sweep report.

- **Shadow eval monitoring (if run before next sweep)**: The factory-steward may run the shadow eval (`python3 eval/run_eval_async.py --model claude-opus-4-7 .claude/agents/meta-agent-factory.md`) between now and the next sweep. If results exist in `eval/experiment_log.json`, analyze them:
  - **What to look for**: Posterior mean vs. 4.6 baseline (0.829), CI overlap with [0.702, 0.927], any 400 errors, duration compared to 4.6 runs.
  - **Why it matters**: The go/no-go criteria (ADOPT A2) determine whether the fleet rolls to Opus 4.7. The researcher's analysis adds a second pair of eyes on the numbers.

### P1 — Should Research

- **Gemini CLI agent definition format vs. SKILL.md (S3)**: This is the ADOPT A8 item from the night discussion. Time-boxed to 3 hours.
  - **What specifically to investigate**:
    1. Does Gemini CLI have a declarative agent definition file format? Check the `GEMINI.md` equivalent, any `.gemini/agents/` directory structure, or inline agent definition mechanisms.
    2. How does Gemini CLI handle agent routing? Is it description-based (like SKILL.md) or command-based (like `/slash-commands`)?
    3. What tool permission model does Gemini CLI use? Compare to our YAML frontmatter `tools:` list.
    4. Does Gemini CLI's v0.39.0-preview.0 "passive skill lifecycle" (extract→inbox→approve→activate) suggest a format for portable skill definitions?
    5. Feasibility verdict: is the gap "adapt existing formats" or "invent something new"?
  - **Output**: `knowledge_base/agentic-ai/experiments/skill-format-comparison.md`
  - **Why it matters**: S3 has had zero concrete research output so far. This comparison produces the first data point on cross-platform portability feasibility and shapes whether Phase 7 AaaS needs format transpilation or a shared runtime.

- **OTEL pilot trace analysis (if A7 runs)**: If the factory-steward ran the OTEL pilot (console exporter, manual test session), read the captured trace data.
  - **What to look for**: Span hierarchy (are tool calls nested under parent spans?), available attributes (token counts, model ID, session ID), whether subagent calls create separate traces or nested spans.
  - **Output**: Brief analysis section in sweep report, or if findings are substantial, `knowledge_base/agentic-ai/experiments/otel-pilot-results.md`.
  - **Why it matters**: Determines whether an automated trace analyzer agent (S1) is feasible with the data `claude -p` actually emits, vs. what we assumed it emits.

### P2 — Watch Only

- **Opus 4.7 token burn rate**: Track GitHub issue #49562. Note any Anthropic response or resolution. No deep analysis — just capture the signal.
- **Google I/O pre-leak window**: Standard monitoring. Watchlist: ADK v1.32.0 timing (~Apr 27-May 1 estimated), A2A activity, any pre-I/O announcement.
- **Gemini CLI nightly pipeline**: Day 4+ pause. Note if nightlies resume.
- **v2.1.114+ releases**: Note any new Claude Code releases since v2.1.114. Check for Opus 4.7-related fixes.
- **Gemini 2.0 Flash deprecation (Jun 1, 44d)**: Not in our pipeline. Watch-only.

## Deprioritized

- **Four/five-topology TCI dispatch**: Rejected twice. Do not propose again.
- **Managed Agents adapter**: Not relevant until Phase 5. Do not track.
- **Conway/persistent agents**: No updates. Do not track.
- **Agent payment protocols**: Phase 7. Do not track.
- **Computer Use**: Neither vendor GA'd. Our pipeline doesn't use it. Mention only if GA announced.
- **Routines vs. cron**: Fully analyzed today. Verdict: inferior for our use case. Closed.
- **Task Budgets CLI integration**: Confirmed API-only. CLI uses `--max-budget-usd`. Closed until Phase 5 SDK migration.
- **1M context beta sunset (Apr 30)**: Fleet clean, GA models. Closed. No further monitoring needed.

## Research Quality Feedback

### Directive Compliance Assessment

The researcher demonstrated **strong compliance** with the morning directive within a single day. Specific observations:

1. **P0 items resolved quickly**: All P0 and P1 open questions from the directive (Opus 4.7 `claude -p` bugs, Haiku 3 date, 1M sunset, Task Budgets CLI, OTEL architecture) were answered definitively in the evening+night sweeps. The directive asked specific questions; the researcher delivered specific answers.

2. **Frozen-topic compression: EXCELLENT**: The night sweep report uses single-line updates for frozen topics (A2A, Vertex, Mariner, Astra, Gemma). This is exactly what was requested and dramatically improves signal-to-noise.

3. **Proposal volume: IMPROVED**: The evening discussion had 3 items and the night discussion had 6, with clear ADOPT/DEFER/REJECT verdicts. The night discussion's 6 items is at the upper end of the 4-5 target, but the quality was high (go/no-go criteria, permissions.deny, OTEL pilot) — all were actionable and concrete.

4. **Proposal file generation: CONCERN REMAINS**: 15 proposal files exist with the 2026-04-18 date. While many were generated before the directive was issued, the night cycle added 4 more (shadow-eval-go-nogo-criteria, otel-pilot-experiment, gemini-cli-format-comparison, night-deferred-items). The directive said "check whether it adds information beyond what the discussion's ADOPT verdict already captures." Several of these proposals duplicate what the discussion transcript already specifies. **Action for next cycle: do not create a standalone proposal file if the ADOPT verdict in the discussion transcript contains sufficient implementation detail for factory-steward. The discussion transcript IS the proposal.**

5. **Analysis quality: A**: The night consolidation analysis is the best-structured output yet — resolution tracking table (§1), strategic alignment assessment (§2), cross-pollination with phase paths (§3), factory-steward handoff with ordered steps (§5), and directive compliance report (§7). This is the template to follow.

6. **Haiku 3 date correction: GOOD CATCH**: Correcting the retirement date from Apr 19 to Apr 20 based on the official deprecation table is exactly the kind of verification the pipeline should produce. The fact that the guard triggers 1 day early (safe) vs. 1 day late (dangerous) demonstrates the value of conservative defaults.

### What to Do Differently Next Cycle

1. **Stop generating standalone proposal files** for items that have ADOPT verdicts in discussions. The discussion transcript is sufficient. Exception: if an item requires a multi-page implementation spec (e.g., a new agent definition, a complex experiment design), a standalone proposal is warranted.

2. **When reporting go/no-go criteria or implementation steps**, include them in the analysis (§5 Factory-Steward Handoff) rather than creating a separate proposal file. The analysis already has the right structure for this.

3. **Sweep reports should be shorter on repeat days**. If the morning sweep covered a topic comprehensively and nothing changed by the evening sweep, the evening report should reference the morning report ("Opus 4.7: see morning sweep, no changes") rather than re-describing the same landscape.

## Team Recommendations

- **No structural changes.** The pipeline processed a high-velocity day (Opus 4.7 aftermath) efficiently.
- **Output cadence alignment**: The researcher produced 4 sweep reports, 2 analyses, and 3 discussions today. The factory-steward processed ADOPT items from the _2026-04-17_ discussion (commit `db7c635`). The 10 ADOPT items from today's three discussions have not been processed yet. This is expected — the factory runs at 3 AM and 2 PM. But it means the ADOPT backlog is growing during high-velocity days. **Recommendation**: On quiet days, the researcher should note when the previous cycle's ADOPT items are still pending and avoid adding low-priority items that extend the backlog.
- **Weekend cadence**: April 19-20 is a weekend. Sweeps should be lighter — focus on Haiku 3 retirement verification (Apr 20) and shadow eval monitoring. Deprioritize breadth. If nothing breaks, one sweep per cycle is sufficient.

---

*This directive replaces the morning 2026-04-18 directive. The researcher should read this at the start of its next sweep.*
