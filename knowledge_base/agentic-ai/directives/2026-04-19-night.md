# Research Directive — 2026-04-19 Night

**Issued by**: agentic-ai-research-lead
**Effective**: 2026-04-19 night through 2026-04-21 (next directive)
**Prior directive**: 2026-04-19.md (morning)

## Strategic Context

Weekend day 1 closing. Both vendor stacks remain frozen (CC v2.1.114, SDK Py v0.1.63, TS v0.2.114, Gemini CLI v0.38.2, ADK v1.31.0). No new releases across either vendor in 48+ hours. The pipeline had one significant advancement today and is approaching a critical verification moment tomorrow.

**A1 gate-first session contract: IMPLEMENTED but NOT YET TESTED.** The factory-steward's 3 AM session today (Apr 19) *implemented* the A1 code in `daily_factory_steward.sh` (commits 7e1333f, fe0e752). It did NOT *execute* the shadow eval — the code was being written, not invoked. `experiment_log.json` has zero `claude-opus-4-7` entries. The next factory session will be the first live test of the gate-first contract. This is the single most consequential automated action the pipeline will take since the 10-skill pilot.

**Haiku 3 retires today/tomorrow (Apr 19-20).** Date discrepancy between official table (Apr 20) and announcement (Apr 19). Guard verified, pipeline clean. Verification tomorrow morning.

**I/O countdown: 30 days.** Pre-I/O window opens ~May 2. No leaks beyond the Google "Agent" platform (NPowerUser Apr 14).

## Strategic Alignment

### S1 — Automatic Agent/Skill Improvement
- **This cycle**: S1 unblocks tomorrow IF the gate-first contract fires and the shadow eval runs. The shadow eval is the prerequisite for model-improvement velocity measurement — the core S1 metric. If the eval runs and Opus 4.7 passes go/no-go (CI overlap with [0.702, 0.927], zero 400 errors, duration ≤ 2x), graduated fleet migration begins.
- **Opus 4.7 adaptive reasoning patch**: Anthropic PM confirmed team "sprinting on tuning." If patched, re-run eval. Existing go/no-go criteria catch regressions naturally. Don't wait for the patch.

### S2 — Multi-Agent Orchestration
- **Steady.** Paper project advancing via paper team (3 commits Apr 18). Google "Agent" multi-agent platform leak provides additional citation for S2 paper. No sweep-driven S2 work needed.

### S3 — Platform Generalization
- **Infrastructure-blocked.** Gemini CLI not installed. Format comparison complete. ADK-Go (4-language support now) noted for Phase 7 planning. No research output possible until Jonas installs Gemini CLI. This remains a human action item.

## Priority Topics (April 20-21)

### P0 — Must Research

- **Haiku 3 post-retirement verification (April 20 morning)**: 10-minute check.
  - **What to look for**: API error response format for `claude-3-haiku-20240307`. Community reports of downstream breakage. Confirm guard worked: `model_audit.sh --retired-on 2026-04-20` runs clean.
  - **Effort**: Minimal. 1-2 searches. Single paragraph in sweep report. If the guard worked (expected), do NOT expand into a retrospective on deprecation handling.

- **Shadow eval gate-first contract verification**: Check whether the factory session ran the shadow eval.
  - **Step 1**: Check `eval/experiment_log.json` for `claude-opus-4-7` entries.
  - **Step 2**: Check factory performance JSON (`logs/performance/factory-2026-04-20.json` or the next factory file) for `GATE_FIRST` event or shadow eval output.
  - **Step 3**: Check session logs (`logs/sessions/factory-steward-*.jsonl`) for `GATE_FIRST` event type.
  - **If shadow eval RAN**: This is the highest-priority analysis item. Pull results and run go/no-go assessment using the checklist in `eval/model_migration_runbook.md`. Write a dedicated section with explicit PASS/FAIL verdict on all three gates (G1: CI overlap, G2: zero 400 errors, G3: duration ≤ 2x). This should be the bulk of the analysis.
  - **If NOT run**: Check whether the GATE_FIRST preamble logged to `logs/factory-*.log` (it should have, given `PENDING_MIGRATION_MODEL="claude-opus-4-7"` is set and experiment_log has 0 entries). If the preamble fired but the eval still failed, note the failure mode. If the preamble didn't fire, check for script logic errors and escalate to Jonas with specific diagnosis.
  - **One-line rule**: Do NOT re-analyze why the shadow eval hasn't run or re-propose solutions. The structural fix is implemented. Diagnose whether it worked or failed, and if failed, state the specific failure mode.

### P1 — Should Research

- **MCP STDIO vulnerability tracking**: Check for new CVEs beyond CVE-2026-40933. Check if Anthropic changed stance. 1-2 searches max.
- **Opus 4.7 adaptive reasoning patch**: Monitor #49562 and Anthropic channels for any release addressing token burn. Note if patched — this affects shadow eval result interpretation.

### P2 — Watch Only

- **Google I/O pre-leak window**: 30 days out. Standard monitoring. ADK v1.32.0 expected ~Apr 27-May 1.
- **New Claude Code releases**: Note any v2.1.115+.
- **Gemini CLI nightly pipeline**: Day 5+ pause. Note if nightlies resume.
- **EAGLE3 performance**: Merged. No further tracking unless new benchmarks appear.

## Deprioritized

- **Four/five-topology TCI dispatch**: Rejected repeatedly. Do not propose.
- **Managed Agents adapter**: Phase 5.
- **Conway/persistent agents**: No updates.
- **Agent payment protocols**: Phase 7.
- **Computer Use**: Mention only if GA announced.
- **Routines vs. cron**: Closed.
- **Task Budgets CLI integration**: Closed.
- **1M context beta sunset**: Closed.
- **Session Storage Python SDK**: Closed until Python SDK ships it.
- **S3 transpiler prototype**: Blocked on Gemini CLI install. Do not re-research.
- **Dual-run adaptive reasoning protocol**: Rejected. One-sentence caveat in runbook is sufficient.
- **Weekend discussion skip rule**: Rejected. Existing directive guidance sufficient.

## Research Quality Feedback

### Directive Compliance Assessment (Morning Directive → Evening Output)

The researcher produced three sweep files (evening Anthropic, evening Google, night consolidation), one analysis, and one discussion for the Saturday evening cycle. This is slightly above target for a weekend evening, but justified by three genuine new developments (A1 implementation, CVE-2026-40933, EAGLE3 correction).

1. **P0 shadow eval monitoring: COMPLIANT.** Correctly tracked A1 implementation status. Correctly noted that the 3 AM session *implemented* A1 but did not *execute* the shadow eval. Zero entries in experiment_log.json confirmed. Good differentiation between "implementing the fix" and "the fix working."

2. **P0 Haiku 3: COMPLIANT.** Date discrepancy documented with all sources. Guard re-verified. Appropriate for final pre-retirement check.

3. **Weekend cadence: ACCEPTABLE.** Three sweeps for the evening cycle is slightly heavy for a weekend, but each covered genuinely different ground (Anthropic-only, Google-only, consolidation). The morning directive said the discussion could be skipped if nothing new — the researcher correctly determined that A1 implementation, a new CVE, and a factual correction warranted a discussion. Judgment was sound.

4. **Analysis quality: HIGH.** The evening analysis correctly identified three gap findings (A1 verification pending, CVE-2026-40933 argument-blind bypass, Opus 4.7 timing consideration). The cross-pollination opportunities (Flowise→cmd_chain_monitor, ADK-Go→S3, Multi-Agent A2A article→S2 paper) were each one paragraph with clear assessment — no filler. The strategic priority status section correctly reflected S1 as "unblocked structurally, verification pending."

5. **Discussion quality: GOOD.** Six ADOPT items at P2-P3, zero P0/P1. The Engineer correctly rejected the dual-run protocol (R1) and the formal skip rule (R2). The cmd_chain_monitor argument-blind gap note (A2) is a good Phase 5 design input. The consolidated ADOPT backlog table across morning + evening discussions is a useful factory-steward handoff format.

6. **Proposal file compliance: FULLY RESOLVED.** Zero `roadmap-updates-*.md`, `skill-updates-*.md`, or `deferred-items-*.md` files for the evening cycle. One `team-2026-04-19-evening.md` (researcher's factory-steward handoff) — this is acceptable as it's a coordination artifact, not a ROADMAP proposal. Compliance streak continues.

### What to Do Differently Next Cycle

1. **Sunday should be the quietest research day.** If no new vendor releases and the Haiku 3 verification is clean and the shadow eval results are negative (didn't run) or simple (clear PASS/FAIL), the entire Sunday output should be: one sweep, one analysis, discussion only if warranted. Target total: 2-3 files.

2. **If shadow eval results exist, go deep on analysis but stay structured.** Use the migration runbook checklist. Don't freelance the analysis format. The fill-in-the-blank format exists for exactly this purpose.

3. **Stop tracking EAGLE3 as a development.** It's merged. Future references should be "EAGLE3 is available" not "EAGLE3 status." Only mention if new benchmarks or performance data emerge.

## Team Recommendations

- **No structural changes.** Weekend cadence validated.
- **Factory-steward critical path**: The next factory session is the verification moment. The shell script has `PENDING_MIGRATION_MODEL="claude-opus-4-7"` and `experiment_log.json` has zero matches — the gate-first preamble SHOULD fire. Remaining ADOPT backlog is all P2-P3, so if the shadow eval runs, the factory session will naturally process lower-priority items afterward.
- **If gate-first contract FAILS** (shadow eval doesn't run despite A1 being in the script): Escalate to Jonas with specific failure diagnosis. This would be a shell script bug, not an LLM priority problem.
- **Human action items for Jonas**: (1) Install Gemini CLI — S3 gate. (2) If shadow eval still hasn't run after Sunday's factory session, manually run: `python3 eval/run_eval_async.py --model claude-opus-4-7 --split train --inter-test-delay 15 .claude/agents/meta-agent-factory.md`

---

*This directive supersedes the morning 2026-04-19 directive. The researcher should read this at the start of its next sweep (Sunday morning).*
