# Research Directive — 2026-04-21 (Night)

**Issued by**: agentic-ai-research-lead
**Effective**: 2026-04-21 night through 2026-04-23 (next directive)
**Prior directive**: 2026-04-21.md (morning/afternoon)

## Strategic Context

Both vendor freezes broke today — the first releases in 130h+ (Anthropic) and 4-6 days (Google selective). Anthropic shipped CC v2.1.115 + v2.1.116 and SDK v0.1.64. Google shipped only ADK v1.31.1 (critical RCE fix), while CLI, A2A, and Vertex remain frozen. The evening consolidation sweep correctly caught a hallucinated ADK v1.32.0 from the afternoon sweep — only v1.31.1 exists.

The day's research output was substantial: two sweep cycles (morning freeze + afternoon/evening freeze break), two analyses (morning strategic + afternoon findings), two discussions (morning quiet-day + afternoon freeze-break event), and the evening correction sweep. This volume was justified by the material change in vendor state — but now that the freeze break is digested, the next cycle should return to standard single-cycle volume unless further releases land.

The factory-steward implemented A4 (validator inline tests) and A1 (fleet manifest with A2A field mapping) from the morning discussion today (commit bf6c2ca). Factory throughput is healthy — the afternoon discussion generated 7 more ADOPT items (A1-A7), creating a reasonable multi-session backlog. The highest-priority item — shadow eval per-test logging — is the factory's next focus.

## Strategic Alignment

### S1 — Automatic Agent/Skill Improvement
- **Shadow eval per-test logging** is the single most important factory item. The NO-GO verdict (0.683, 27/39) is logged but unexploitable: without per-test data, we cannot determine whether Opus 4.7 failures are recoverable (description tuning) or fundamental (Anthropic must patch). Afternoon proposal A1 defines the exact format (`per_test_results` array in `experiment_log.json`). This must be in place before Opus 4.7 #49562 is resolved — otherwise the next shadow eval re-run produces another aggregate-only result.
- **Failure analysis template** (afternoon A4) converts the next shadow eval output into a mechanistic S1 decision (positive-concentrated → tune descriptions, negative-concentrated → wait, mixed → both). Pre-writing this eliminates analysis delay.
- **Opus 4.7 #49562**: Still OPEN, zero staff responses, last updated Apr 19. The "sprinting on tuning" PM comment from Apr 19 is the only Anthropic signal. No patch imminent.
- **Net S1 posture**: Architecturally validated (the loop works), instrumentally blocked (per-test logging gap), externally waiting (Anthropic patch). The directive priority correctly shifts from "run shadow eval" (resolved) to "instrument shadow eval" (active).

### S2 — Multi-Agent Orchestration
- **SessionStore v0.1.64** de-risks Phase 5.3.2 session persistence. Afternoon A3 proposes documenting it as a leading candidate (not decided) in `workflow-state-convergence.md`, with Design Freeze (May 22-26) as the decision point. This is the right level of commitment — record the option, defer the architecture choice.
- **ADK v1.31.1 RCE** provides a concrete security comparison case study for the A2 comparison framework and the S2 paper. Afternoon A5 adds this as a dimension. Fair framing: note both the vulnerability and the rapid response.
- **Paper project**: Steady. No new S2 findings from either vendor during the freeze. The production A2A+ADK+Cloud Run article (from Sunday analysis) remains the strongest recent S2 citation.

### S3 — Platform Generalization
- **Fleet manifest created** (commit bf6c2ca) with A2A Agent Card field mapping — 16 agents, 6 steward configs. This is the cheapest S3 progress available while Gemini CLI remains uninstalled.
- **Gemini CLI**: Still not installed. S3 transpiler prototype remains blocked. This is the longest-standing infrastructure blocker (since 2026-04-19).
- **No change** in S3 research surface.

## Priority Topics (April 22-23)

### P0 — Must Research

- **Post-freeze release cadence monitoring**: Both vendors broke their freezes today. Anthropic tends to ship daily after a freeze break. The next 48h may see CC v2.1.117+, SDK v0.1.65+, and potentially the Opus 4.7 adaptive reasoning patch (#49562). Google CLI may follow ADK's lead and unfreeze.
  - **What to look for**: CC releases (especially anything mentioning Opus 4.7 or adaptive reasoning), SDK updates, Gemini CLI unfreeze, Haiku 3 official "Retired" page status.
  - **Why**: The accumulated unreleased work behind a 130h+ freeze creates burst probability. An Opus 4.7 patch specifically triggers automatic shadow eval re-run.
  - **Output**: Standard sweep report. If Opus 4.7 patch ships, flag it as the headline — this is the S1 critical event.

### P1 — Should Research

- **1M context beta sunset (9 days, April 30)**: Final check for Anthropic migration guidance. If nothing published, one sentence: "Non-issue for fleet (GA 4.6 models), sunset passes silently."
  - **Why**: Last checkpoint before the 1-week-out window. After this, stop tracking unless Anthropic surprises.
  - **One-time item, then close.**

### P2 — Watch Only

- **Google I/O pre-leak window**: Opens ~May 2 (~11 days). Standard monitoring for ADK v2.0 GA signals, Gemini 4 leaks, A2A v1.1 pre-announcements.
- **Opus 4/Sonnet 4 retirement (June 15, 55 days)**: Guarded. No action unless migration guidance changes.
- **MCP STDIO**: One sentence. No new CVEs expected.
- **ADK v1.32.0 (actual)**: Expected ~Apr 27-May 1 per bi-weekly cadence. Monitor for security-relevant changes.

## Deprioritized

- **Shadow eval re-analysis**: The NO-GO verdict is final until a new model or patch arrives. Do not discuss, propose, or analyze further. The per-test logging (factory action) and failure analysis template (factory action) are both in the factory queue — the researcher does not need to revisit.
- **Four/five-topology TCI dispatch**: Rejected. Do not propose.
- **Managed Agents adapter**: Phase 5.
- **Conway/persistent agents**: No updates.
- **Agent payment protocols**: Phase 7.
- **Computer Use**: Mention only if GA announced.
- **Routines vs. cron**: Closed.
- **Task Budgets CLI integration**: Closed (API-only).
- **Session Storage Python SDK**: Available. Design decision deferred to Phase 5 Design Freeze.
- **S3 transpiler prototype**: Blocked on Gemini CLI install.
- **EAGLE3**: Available. Stop tracking.
- **Burst mode / factory handoff / pre-migration cost extrapolation**: Rejected.
- **Generalized lifecycle event system**: Deferred to Phase 5.
- **Teach & Repeat to S1**: Deferred.
- **ADK v1.32.0 hallucination follow-up**: The correction is logged. The evening sweep architecture works. No process change needed (afternoon R1 correctly rejected LLM verification instructions).
- **Sweep verification formalization**: Rejected (R1). Two-sweep architecture is the defense.
- **SessionStore conformance tests as Phase 5 gate**: Rejected for now (afternoon D1/R2). Decision deferred to Design Freeze.

## New Research Areas

None. The current research surface is well-calibrated. The freeze break was digested in a single afternoon cycle, and the factory queue has 9 items to process over the next 2-3 sessions. Adding new research areas now would create unfunded mandates.

## Research Quality Feedback

### Directive Compliance Assessment (2026-04-21 morning directive → afternoon/evening output)

**Overall: STRONG. Volume escalation was justified by material event. Quality remained high.**

1. **Volume control**: The morning directive was written for a quiet freeze day (one discussion, 5-ADOPT cap). The freeze broke mid-day, which is a material event justifying a second discussion. The afternoon discussion explicitly documented this justification in its self-assessment. Good metacognitive awareness.

2. **ADOPT volume**: 4 morning + 7 afternoon = 11 total for the day. This is above the 5-cap, but the cap applies per quiet day. The freeze break created a non-quiet day. The afternoon discussion's items are all well-reasoned and strategically aligned. However, 11 items in a single day strains factory throughput (3 items/session = ~4 sessions to clear). Going forward: even on event days, aim to combine items when possible (e.g., A5/A6/A7 could have been a single "framework enrichment + process notes" item rather than three separate proposals).

3. **Data integrity**: The evening sweep's ADK v1.32.0 correction is the standout quality moment. The researcher caught its own prior error, verified via three independent sources (`gh api`, PyPI, GitHub Releases), and issued a clear correction with root cause analysis. This is the two-sweep architecture working exactly as designed. The correction note in INDEX.md is prominent and unambiguous.

4. **Analysis quality**: Afternoon analysis is strong. The shadow eval per-test data gap (Finding 1) is the most actionable finding in weeks — it identifies a specific instrumentation gap, explains why it matters for S1, and proposes a concrete fix. Finding 5 (selective freeze break pattern) demonstrates release cadence intelligence that improves I/O forecasting.

5. **Discussion quality**: The Engineer's pushback was substantive on three items: (a) R1 correctly rejected LLM verification (the diagnosis of "LLM instructions don't prevent LLM hallucination" is exactly right), (b) SessionStore was downgraded from "decided" to "leading candidate" (correct — Design Freeze is the decision point), (c) conformance test gate was rejected as premature (correct — acceptance criteria follow architecture decisions). The Engineer is providing real value, not rubber-stamping.

### What to Do Differently Next Cycle

1. **Return to single-cycle volume** unless another material event occurs (>2 simultaneous releases, Opus 4.7 patch, security CVE). Tomorrow's sweep should produce one sweep report, one analysis (if warranted), and one discussion. The freeze break has been fully digested — the factory queue is well-stocked.

2. **Combine low-priority items** in discussions. Three P3 items (A5, A6, A7) each costing 5 minutes should be proposed as a single "housekeeping bundle" rather than three separate proposals that each need discussion and verdict. This reduces cognitive overhead in both the discussion and the factory queue without losing any content.

3. **Continue evening consolidation pattern**. Today's ADK correction validates the two-sweep architecture. No changes needed.

4. **Shadow eval is now purely a factory concern**. The researcher identified the data gap, proposed the fix, and the factory has it queued. Do not re-analyze, re-propose, or discuss shadow eval instrumentation. One-sentence status checks only: "Per-test logging: [implemented/pending]. #49562: [OPEN/patched]."

## Team Recommendations

### Factory-Steward Priority Queue (from afternoon discussion, reordered)

1. **A1-afternoon: Shadow eval per-test logging** (P1, 30 min): `per_test_results` array in `experiment_log.json`. S1 critical path — must be in place before next Opus 4.7 patch.
2. **A2-afternoon: Fleet version bump** (P1, 1 min): `fleet_min_version.txt` to >=2.1.116.
3. **A4-afternoon: Shadow eval failure analysis template** (P2, 15 min): Three-category breakdown in `model_migration_runbook.md`.
4. **A2-morning: ADK v2.0 TCI comparison framework** (P2, 30 min): Pre-fill our design dimensions, include security dimension (A5).
5. **A3-afternoon: SessionStore design note** (P2, 10 min): Leading candidate in `workflow-state-convergence.md`.
6. **A3-morning: Phase 5 design index** (P3, 20 min): Annotated index with decision status.
7. **A6+A7-afternoon: I/O playbook note + sweep corrections log** (P3, 10 min combined): Bundle these.
8. **Carried forward**: Programmatic Tool Calling deny rule (P1, human action), G20 MCP false-positive tests (P2).

### Human Action Items for Jonas

1. **Upgrade CC to v2.1.116** — 67% `/resume` speedup, sandbox security fix.
2. **Install Gemini CLI** — gates all S3 research beyond format comparison.
3. **No shadow eval action needed** — infrastructure autonomous, per-test logging is factory's next item.

### No Structural Team Changes

The two-agent structure (researcher + factory-steward) continues to perform well. Today was a stress test — freeze break required elevated volume, and the pipeline handled it cleanly. The researcher correctly escalated volume, maintained data integrity (evening correction), and produced actionable findings. The factory implemented two items same-day. No changes needed.

---

*This directive supersedes 2026-04-21.md. The researcher should read this at the start of its next sweep. Key changes: (1) return to single-cycle volume, (2) shadow eval is now purely a factory concern — one-sentence status checks only, (3) P0 is post-freeze release monitoring (especially Opus 4.7 patch), (4) 1M beta sunset gets one final check then closes.*
