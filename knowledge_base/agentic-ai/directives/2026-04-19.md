# Research Directive — 2026-04-19

**Issued by**: agentic-ai-research-lead
**Effective**: 2026-04-19 evening through 2026-04-21 (next directive)
**Prior directive**: 2026-04-18-afternoon.md

## Strategic Context

Weekend day 1 complete. Both vendor stacks are frozen (Claude Code v2.1.114, Gemini CLI v0.38.2, ADK v1.31.0 — all unchanged for 48+ hours). No new releases, no pre-I/O leaks, no breaking changes. The pipeline is in steady state — this should be a low-output weekend.

**Critical path: shadow eval, now in 5th deferred cycle.** The Opus 4.7 shadow eval has been P0 for five consecutive directive cycles without execution. Every prerequisite is met: `--model` flag added (Apr 17), breaking change audit CLEAN (Apr 18), go/no-go criteria formalized (Apr 18). The discussion today correctly diagnosed this as L12 urgency bias and proposed a structural fix (A1: gate-first session contract). This is the single most important pipeline improvement right now — it makes shadow eval execution structurally un-deferrable instead of relying on directive guidance that the factory-steward's LLM deprioritizes.

**Haiku 3 retires tomorrow (Apr 20).** Guard verified, pipeline clean. Non-event requiring only a brief verification check.

**S3 infrastructure blocked.** Gemini CLI confirmed NOT installed. The transpiler prototype cannot proceed beyond the completed format comparison (`experiments/skill-format-comparison.md`). This is a human action item (Jonas) — not research.

## Strategic Alignment

### S1 — Automatic Agent/Skill Improvement
- **This cycle**: No direct S1 advancement. The shadow eval is the S1 gate-blocker — we cannot measure model-improvement velocity (the core S1 metric) without a baseline on the new model.
- **Justification**: The shadow eval deferral makes S1 work impossible. Fixing the deferral mechanism (A1 gate-first contract) is the highest-leverage S1 action. Once the eval runs, the comparison against [0.702, 0.927] baseline tells us whether Opus 4.7 enables S1-relevant improvements.
- **The dual-observability pattern (OTEL + Session Storage) remains queued.** OTEL env vars are a factory-steward ADOPT item (A6 from Apr 18-night). Session Storage Alpha is TS-only — closed until Python SDK adds it.

### S2 — Multi-Agent Orchestration
- **S2 advancing via paper project**, not sweeps. Three paper commits on Apr 18 (experiment analysis, candidate draft, reviewer fixes). The paper team owns S2's experimental track.
- **No new sweep findings relevant to S2.** The five-pattern multi-agent taxonomy is documented. No further research until Phase 5 TCI benchmark produces distribution data or the paper team requests specific input.

### S3 — Platform Generalization
- **S3 is infrastructure-blocked.** Gemini CLI not installed → transpiler prototype untestable. The format comparison is complete and confirms transpiler viability (identical SKILL.md patterns, bridgeable gaps in tool permissions and model override).
- **Next S3 step is human action**: install Gemini CLI. Until then, no research output is possible. Do NOT re-research the format comparison or propose alternative approaches.

## Priority Topics (April 20-21)

### P0 — Must Research

- **Haiku 3 post-retirement verification (April 20 morning)**: 10-minute check.
  - **What to look for**: API error response format for `claude-3-haiku-20240307`. Community reports of downstream breakage. Confirmation that our guard worked (no Haiku 3 references in operational code, `model_audit.sh --retired-on 2026-04-20` runs clean).
  - **Effort**: Minimal. 1-2 searches. Single paragraph in sweep report.

- **Shadow eval status check**: At the start of each sweep, check `eval/experiment_log.json` for Opus 4.7 entries.
  - **If the factory-steward ran the eval**: This is the highest-priority analysis item. Pull results and compare against go/no-go criteria (CI overlap with baseline [0.702, 0.927], zero 400 errors, duration ≤ 2x). Write a dedicated analysis section with explicit PASS/FAIL verdict. Include the fill-in-the-blank format from `model_migration_runbook.md`.
  - **If NOT run**: Note it in one line. Do NOT re-propose or re-analyze why it hasn't run. The structural fix (A1) is in the factory-steward's queue. If A1 hasn't been implemented either, note "A1 gate-first contract also not implemented — escalate to Jonas" as a single line.

### P1 — Should Research

- **MCP STDIO vulnerability tracking**: The vulnerability (150M downloads, 10 CVEs, Anthropic "expected behavior") is documented. Check if any new CVEs have been assigned or if Anthropic has changed its stance. 1-2 searches max.

### P2 — Watch Only

- **Google I/O pre-leak window**: Standard monitoring. 30 days out. ADK v1.32.0 expected ~Apr 27-May 1.
- **Opus 4.7 token burn rate**: Track #49562. Note any Anthropic response.
- **New Claude Code releases**: Note any v2.1.115+.
- **Gemini CLI nightly pipeline**: Day 5+ pause. Note if nightlies resume.

## Deprioritized

- **Four/five-topology TCI dispatch**: Rejected repeatedly. Do not propose.
- **Managed Agents adapter**: Phase 5. Do not track.
- **Conway/persistent agents**: No updates. Do not track.
- **Agent payment protocols**: Phase 7. Do not track.
- **Computer Use**: Mention only if GA announced.
- **Routines vs. cron**: Closed.
- **Task Budgets CLI integration**: Closed (API-only, CLI uses `--max-budget-usd`).
- **1M context beta sunset**: Closed (fleet clean).
- **Session Storage Python SDK**: Closed until Python SDK ships it (currently TS-only).
- **Canonical agent definition format**: Premature. Revisit after transpiler validated.
- **S3 transpiler prototype**: Blocked on Gemini CLI install. Do not re-research.

## Research Quality Feedback

### Directive Compliance Assessment (Afternoon Directive → Saturday Output)

The researcher produced one sweep report (with afternoon update), one analysis, and one discussion on Saturday. This is the correct weekend cadence.

1. **P0 shadow eval monitoring: COMPLIANT.** Status tracked in every output. Correctly flagged as "5th directive cycle" deferred. One-line note, no over-analysis.

2. **P0 Haiku 3 retirement: COMPLIANT.** Re-verified the retirement date (Apr 20) against official deprecation page. Confirmed `deprecated_models.json` date is correct. Appropriate level of detail.

3. **P1 Gemini CLI access check: COMPLETE.** Confirmed NOT installed. This resolves the S3 gating question — blocked on infrastructure, not research. Good — concise finding.

4. **Weekend sweep length: EXCELLENT.** The morning sweep was appropriately condensed. The afternoon update was short (30 lines of new content). Per-topic updates used single-line "no change" format for frozen topics. Signal-to-noise ratio is the best it's been in 5 days.

5. **Proposal file proliferation: FULLY RESOLVED.** Zero `roadmap-updates-*.md`, `skill-updates-*.md`, or `deferred-items-*.md` files generated. The mandatory instruction from the afternoon directive was followed. The discussion generated 6 ADOPT items (slightly above the 0-3 target for quiet days, but all 6 were well-justified and the discussion quality was high — the A1 gate-first contract proposal alone justifies the discussion).

6. **Discussion quality: HIGH.** The A1 gate-first session contract is the best structural proposal the team has produced since L12 was identified. It correctly identifies that directives are advisory and the fix must be at the shell script layer. The Engineer's simplification (use experiment_log.json as state, not a new flag file) is sound. A2 (results checklist in existing runbook) and A5 (dashboard model migration section) round out a coherent "make the shadow eval impossible to ignore" strategy.

7. **Analysis quality: GOOD.** Clean structure, no over-analysis on quiet topics. The MCP STDIO vulnerability analysis correctly identifies Phase 5 design implications without proposing premature Phase 4 changes. The S1/S2/S3 strategic status section is well-calibrated.

### What to Do Differently Next Cycle

1. **Maintain weekend cadence.** Today's output volume and quality are the target. One sweep (with optional update), one analysis, one discussion if warranted. If nothing changed and no P0 event occurred, the discussion can be skipped entirely — write "No discussion items — quiet weekend day" as a single line.

2. **Haiku 3 verification should be SHORT.** 10-minute check. One paragraph. If the guard worked (expected), don't expand into a retrospective on deprecation handling. If it didn't work, escalate immediately.

3. **If shadow eval ran, prioritize analysis.** This is the most important data point for the pipeline. The analysis should be the bulk of the next sweep's analysis output. Use the go/no-go criteria from `model_migration_runbook.md` as a structured checklist.

## Team Recommendations

- **No structural changes.** Weekend cadence confirmed working well.
- **Factory-steward ADOPT priority**: (1) A1 gate-first session contract from today's discussion — this is the structural fix for the 5-cycle shadow eval deferral. (2) Shadow eval execution (if A1 not yet implemented, run manually). (3) Remaining backlog (A3 v2.1.113 update, A5 permissions.deny, etc.).
- **Human action item for Jonas**: Install Gemini CLI to unblock S3 transpiler work. This is the S3 gate — no amount of research can advance S3 without it.

---

*This directive supersedes the afternoon 2026-04-18 directive. The researcher should read this at the start of its next sweep (Sunday morning).*
