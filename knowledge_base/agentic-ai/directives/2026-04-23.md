# Research Directive — 2026-04-23

## Strategic Context
The research pipeline has recovered from the "Observability Blackout" (April 18-21). However, the **Opus 4.7 Shadow Eval** has revealed a critical regression in our core `meta-agent-factory` (0.63 vs 0.829 baseline). Opus 4.7's "literal" instruction-following requires an immediate shift from abstract trigger descriptions to explicit, case-driven triggers. This cycle focuses on restoring trigger parity and laying the foundation for Phase 5 orchestration via Session Rewind.

## Strategic Alignment
- **S1 (Automatic Agent/Skill Improvement)**: Resolving the Opus 4.7 regression is the top S1 priority. We must prove the pipeline can auto-adapt to a SOTA model's behavioral shift.
- **S2 (Multi-Agent Orchestration)**: The Session Rewind and OTEL Tracing specs drafted in the last cycle move to the "Implementation Design" phase.
- **S3 (Platform Generalization)**: A2A v1.2 (Agentic AI Foundation) is confirmed as the target identity layer for Gemini/Claude parity.

## Priority Topics (Action Required)

### P0 — Must Resolve (Operational)
- **Opus 4.7 Regression Recovery**:
    - **Task**: Run `autoresearch-optimizer` on `.claude/agents/meta-agent-factory.md` specifically targeting `claude-opus-4-7`.
    - **Goal**: Restore Bayesian posterior mean to ≥0.85 and `ci_lower` to ≥0.75.
    - **Prompt Hint**: "De-abstract the trigger verbs. Instead of 'Triggered when the user wants a skill,' use 'Invoked when the user says: I need a skill for X' or 'I want to build an expert for Y'."
- **Incremental Budget Gates**:
    - **Task**: Implement the "Watchdog Pulse" in `scripts/lib/cost_ceiling.sh`. 
    - **Action**: Alert and halt if a single task segment consumes >25% of the 30-day average session budget.

### P1 — Strategic Research & Implementation
- **Session Rewind Prototype**:
    - **Task**: Using the Agent SDK v0.1.64, prototype a 3-iteration "Rewind Test" for the optimizer.
    - **Metric**: Measure token savings vs. a non-rewind session of the same length.
- **Goal-Consistency Hook**:
    - **Task**: Design a reflection prompt for `post-tool-use.sh` that checks for "Goal Hijacking" in Track B (high-coupling) tasks.
    - **Constraint**: Must use a Haiku-class model for low latency/cost.

### P2 — Platform Generalization
- **A2A v1.2 Identity Bridge**:
    - **Task**: Draft a "Transpiler" spec that maps `SKILL.md` frontmatter to A2A Agent Card v1.2 metadata.
    - **Focus**: Portability between Claude Code and Gemini CLI.

## Research Quality Feedback
- **Assessment**: Excellent recovery from the blackout. The drafting of the Session Rewind and OTEL Tracing specs (2026-04-23) shows strong strategic foresight.
- **Feedback**: The Opus 4.7 regression analysis was sharp. Identifying the "literal style" as the root cause provides a clear path for the optimizer. 
- **Improvement**: Continue using `xhigh` effort selectively to manage quota while tackling these reasoning-heavy P0/P1 tasks.

## Team Recommendations
- **Researcher**: Prioritize the optimizer loop for the 4.7 regression. This is the blocker for the entire Phase 4 rollout.
- **Steward**: Monitor the `cost_ceiling.sh` alerts once the Budget Gates are implemented.
