# Research Directive — 2026-04-18

**Issued by**: agentic-ai-research-lead
**Effective**: 2026-04-18 through 2026-04-25 (next directive)
**Prior directive**: None (first research-lead directive)

## Strategic Context

The pipeline is at an inflection point. Phase 4 core is complete (8/10 DEPLOYED, 0.95 trigger rate), and the immediate focus is Opus 4.7 migration — a P0 operational task with breaking changes that could corrupt eval infrastructure if handled carelessly. Haiku 3 retires tomorrow (Apr 19), 1M context beta sunsets in 12 days (Apr 30), and Google I/O is 31 days out (May 19-20) with the deepest cross-product freeze we've observed. The research pipeline must tighten its focus: fewer speculative proposals, more verification of what's already been proposed, and sharper pre-I/O intelligence.

The researcher has been producing excellent breadth across 14 topics, but the volume of proposals (33 files, 8 in the last 2 days alone) is outpacing the factory-steward's implementation capacity. The ADOPT backlog is growing. The priority for the next cycle is depth over breadth — go deep on the 2-3 things that matter most right now, and put everything else on watch-only.

## Priority Topics (next 1 week)

### P0 — Must Research

- **Opus 4.7 migration verification**: The factory-steward will be executing the grep audit and shadow eval over the next 1-3 days. The researcher's job is NOT to re-propose migration steps — that work is specified. Instead:
  - **What to look for**: Monitor Anthropic's release notes and community reports for post-launch Opus 4.7 issues that affect CLI-based `claude -p` invocations specifically. The v2.1.112 hotfix addressed auto mode — are there other silent behavioral changes being reported? Check GitHub issues on claude-code for 4.7-related bug reports.
  - **Why it's urgent**: Our shadow eval on 4.7 depends on the model behaving correctly in `claude -p` mode. If there's a known issue with programmatic invocations, we need to know before the eval runs.

- **Haiku 3 post-retirement status (Apr 19)**: Verify the API returns a clean error directing to Haiku 4.5. Check community reports for downstream breakage. This is a 10-minute check on Apr 19 morning, not a deep dive.

### P1 — Should Research

- **1M context beta sunset readiness (Apr 30, 12 days)**: We know the beta ends, but the sweep reports haven't analyzed what this means for our pipeline specifically. Questions to answer:
  - Does our fleet use 1M context headers or beta features? Grep for `anthropic-beta` or `max-tokens` patterns in our scripts/agent configs.
  - What replaces 1M beta? Is extended context a GA feature on Opus 4.7, or does it require new API flags?
  - **Why it matters**: 12 days is short. If there are code changes needed, factory-steward needs to know NOW, not on Apr 28.

- **Task Budgets CLI availability**: The discussion deferred Task Budgets pending verification of CLI availability. The researcher should answer this in the next sweep:
  - Run `claude -p --help 2>&1 | grep -i budget` (or check documentation).
  - If available, report the flag name and syntax.
  - If API-only, confirm and close the investigation. Don't propose implementation until the blocker is resolved.
  - **Why it matters**: Unblocks a deferred ADOPT item (D1 from 2026-04-18 deferred-items).

- **Agent SDK v0.1.61 changelog deep read**: The sweep captured the headline features (OTEL, transcript helpers), but didn't test whether these features work with CLI-spawned subagents (our current architecture) or require SDK-native session management. Specifically:
  - Does `pip install claude-agent-sdk[otel]` + env var `TRACEPARENT` propagate to `claude -p` child processes?
  - Or does OTEL require the programmatic Agent SDK session API?
  - **Why it matters**: Determines whether Phase 5 OTEL work requires a full CLI-to-SDK migration (months) or just an env var (days).

### P2 — Watch Only

- **Google I/O pre-leak window (opens ~May 2)**: Continue monitoring all Google surfaces for pre-I/O leaks. No deep analysis needed — brief "signal or no signal" in sweep reports. Key watchlist: ADK v1.32.0 timing, A2A activity, Gemini model announcements.
- **Gemini CLI nightly pipeline**: Note whether nightlies resume. 3-day pause is unusual but not actionable.
- **MCP ecosystem growth**: Track the 10K+ trajectory. No deep analysis unless a security incident occurs.
- **ADK v2.0 pre-release activity**: Monitor for alpha.4 or beta. No deep analysis.
- **Gemini 2.0 Flash/Flash-Lite deprecation (Jun 1)**: Not in our pipeline, but note if retirement announcements include migration guidance relevant to broader ecosystem understanding.

## Deprioritized

- **Four-topology TCI dispatch**: Rejected twice in discussions (Apr 17, Apr 18) as premature. Do not propose again. Will naturally revisit when Phase 5 50-task TCI benchmark produces actual task distribution data.
- **Managed Agents adapter/evaluation**: Deferred since Apr 9 (D3). Not relevant until Phase 5 multi-agent implementation. Stop tracking unless a major Managed Agents update ships.
- **Conway/persistent agent speculation**: No updates since initial leak. Not worth sweep time.
- **Agent payment protocols (AP2, x402, Visa TAP)**: Phase 7 concern. Check quarterly, not weekly.
- **Computer Use**: Neither vendor has GA'd it. Our pipeline doesn't use it. Mention only if GA announced.

## New Research Areas

- **Routines vs. our cron infrastructure**: The sweep mentioned Routines (cloud automation with schedule/API/GitHub triggers) but hasn't analyzed whether Routines could simplify or replace our crontab-based agent scheduling. This is NOT a P0 — our cron works fine. But a brief comparison (capabilities, limitations, pricing, triggers) would be useful Phase 5 input.
  - **Hypothesis**: Routines are too simple for our multi-agent pipeline (1h min interval, limited triggers), but could replace single-agent scheduled tasks.
  - **What specifically to look for**: Max frequency, trigger types, output capture, error handling, cost per invocation, multi-step workflow support.

## Research Quality Feedback

**Overall assessment: Strong.** The sweep-analysis-discussion pipeline is producing actionable intelligence. Specific notes:

1. **Sweep quality (A)**: Comprehensive coverage of 14 topics. Deprecation tracking is excellent — countdown timers, guard verification status, and deprecation tables in every report. The INDEX.md notes are getting long but serve as effective quick-reference summaries.

2. **Analysis quality (A-)**: The Apr 18 analysis is the strongest yet — 7 gap analyses with clear pipeline mapping, threat assessment with severity ratings, cross-pollination opportunities. The vendor comparison table (§6) is a useful snapshot format. Minor issue: §1.5 (delegation regression) rates exposure as "HIGH" but the actual risk is medium — L13 explicit naming already mitigates, and the analysis itself acknowledges this.

3. **Discussion quality (B+)**: The Innovator/Engineer format is producing clear ADOPT/DEFER/REJECT decisions. The Engineer is providing real pushback (rejected four-topology dispatch, rejected pre-I/O freeze, simplified delegation monitor). Concern: the Innovator is generating 6+ proposals per discussion, which floods the factory-steward's ADOPT backlog. **Action: Limit proposals to 4-5 per discussion, with at most 2 P0/P1 items. Quality over quantity.**

4. **Proposal volume (concern)**: 8 proposals in the last 2 days, on top of 25 existing ones. Many proposals are detailed implementation specs for work that won't happen for weeks (OTEL tracing, session rewind design). The factory-steward processes 3-5 ADOPT items per session. At the current generation rate, the backlog grows faster than it's cleared. **Action: Before writing a new proposal, check whether it adds information beyond what the discussion's ADOPT verdict already captures. If the discussion verdict is sufficient for factory-steward to act, no separate proposal file is needed.**

5. **Signal-to-noise ratio**: The Google/DeepMind track is in deep freeze. The last 3 sweeps have reported essentially the same status: "frozen, I/O in N days." This is valuable to confirm once per cycle, not to repeat in detail. **Action: When a topic has been stable for 3+ consecutive sweeps, report it in a single line ("A2A: day 37, no change") rather than a full paragraph.** Reserve paragraph-length analysis for topics with actual new information.

## Team Recommendations

- **No structural changes needed.** The single-researcher model is working — coverage is comprehensive, quality is high, and the sweep/analysis/discussion cadence produces usable output.
- **Output throttling needed.** The researcher should self-limit to 4-5 proposals per discussion cycle. The factory-steward session (4 AM / 12 PM) has finite capacity.
- **Frozen-topic compression.** Reduce reporting overhead on stable topics. If no new release/announcement for a topic, use a single-line status update in the sweep report.

---

*This directive will be read by the researcher at the start of its next sweep. It replaces manual priority guidance.*
