# Research Directive — 2026-04-20

**Issued by**: agentic-ai-research-lead
**Effective**: 2026-04-20 evening through 2026-04-22 (next directive)
**Prior directive**: 2026-04-19-night.md

## Strategic Context

Sunday closing. Both vendor stacks remain in their deepest combined freeze this cycle (CC v2.1.114 72h+, SDK Py v0.1.63, TS v0.2.114, Gemini CLI v0.38.2 day 3, ADK v1.31.0 day 3, A2A v1.0.0 day 39+, Gemini API day 5, Agent Builder day 61+). No new releases from either vendor today. I/O is 29 days out; pre-I/O window opens ~May 2 (~12 days).

**Shadow eval diagnosis FINAL**: The root cause is confirmed as a time budget mismatch — the 88-minute eval cannot complete within the 44-minute factory session. This is NOT an LLM behavior failure; the A1 gate-first contract works at both shell and LLM levels. The eval WAS launched (Apr 19, commit e99b434) but the session expired before completion. S1 meta-optimization loop is architecturally complete. Unblocking requires either: (1) Jonas runs the eval manually (~5 min setup, ~88 min execution), or (2) a dedicated pre-factory cron slot is created. Both solutions are straightforward. **Stop re-diagnosing this. The diagnosis is complete. Execute the fix.**

**Haiku 3 retirement**: Guard PASS. First automated model retirement validated end-to-end. Non-event for operations. Design pattern confirmed reusable for Sonnet 4/Opus 4 (June 15) and 1M beta sunset (April 30).

**MCP STDIO confirmed systemic**: 10+ CVEs, 150M+ downloads, four exploitation families. Anthropic declined protocol fix. Our `cmd_chain_monitor.sh` vulnerable to family #2 (argument-blind allowlist bypass). Phase 5 design input — no immediate action required.

## Strategic Alignment

### S1 — Automatic Agent/Skill Improvement
- **This cycle advances S1 significantly**: The shadow eval diagnosis confirms the entire detection→task-generation→execution chain is structurally sound. Discussion A2 (dedicated cron job) completes the operational gap. When Jonas runs the manual eval (A1) or the cron is set up, S1 will have its first end-to-end autonomous model migration. This is the most important S1 milestone since the autoresearch-optimizer.
- **Researcher action**: Document this milestone in experiments directory (Discussion A4). Keep factual — document what happened with commit hashes, not aspirational narrative.

### S2 — Multi-Agent Orchestration
- **Steady, with new comparison data**: The production ADK+A2A+Cloud Run architecture article is the strongest real-world multi-agent reference yet. It documents sequential→feedback loop→orchestration progression in production, directly mapping to our Phase 4→Phase 5 trajectory. Paper team should cite this (Discussion A7).
- **No sweep-driven S2 work needed** — paper project advancing independently.

### S3 — Platform Generalization
- **Infrastructure-blocked, no change.** Gemini CLI still not installed. Format comparison complete. IBM A2A documentation signals A2A reaching "must-support" enterprise status — Agent Cards increasingly look like the right cross-platform identity layer.
- **Human action item remains**: Jonas installs Gemini CLI.

## Priority Topics (April 21-22)

### P0 — Must Research

- **Shadow eval status check**: Monday morning, check if Jonas ran the manual eval over the weekend.
  - **Step 1**: Check `eval/experiment_log.json` for `claude-opus-4-7` entries.
  - **Step 2**: If results exist, run go/no-go assessment using `eval/model_migration_runbook.md` checklist (G1: CI overlap with [0.702, 0.927], G2: zero 400 errors, G3: duration ≤ 2x). Write dedicated analysis section.
  - **Step 3**: If NO results, note it in one sentence ("Shadow eval still pending — manual run or dedicated cron needed") and move on. **Do NOT re-diagnose or re-propose. The diagnosis is complete.**
  - **Why**: S1 completion depends on this eval. But the researcher cannot execute the fix — it's a human action or factory-steward task.

### P1 — Should Research

- **Monday vendor unfreeze check**: Weekday cadence resumes. Both vendors may push releases Monday morning (US/Asia). Standard L1 monitoring across all tracked repos.
  - **What to look for**: CC v2.1.115+, Opus 4.7 patch (adaptive reasoning tuning), ADK v1.32.0 (expected ~Apr 27-May 1 but could arrive early), SDK updates.
  - **Why**: 72h+ freeze on Anthropic side is unusual outside holidays. Monday is the most likely unfreeze day.

- **Opus 4.7 adaptive reasoning**: Continue monitoring #49562 and related issues. If Anthropic patches, note it prominently — it affects shadow eval result interpretation.
  - **Why**: PM "sprinting on tuning" (Apr 19) suggests a patch could land any day. If we run the eval pre-patch and post-patch, we get a behavioral baseline comparison.

- **MCP STDIO ecosystem monitoring**: Check for new CVEs or Anthropic stance changes. 1-2 searches max.
  - **Why**: Active exploitation confirmed. But our exposure is local-only. Phase 5 design input only.

### P2 — Watch Only

- **1M context beta sunset (10 days)**: April 30. Fleet already on GA models — non-event expected. `model_audit.sh` monitors beta headers. Note only if Anthropic publishes migration guidance or behavioral changes.
- **Google I/O pre-leak window**: Opens ~May 2 (~12 days). Standard monitoring for ADK v2.0, Gemini 4, A2A v1.1 leaks.
- **Haiku 3 status flip**: Confirm "Deprecated" → "Retired" on official page when it happens. Cosmetic — no operational impact.

## Deprioritized

- **Shadow eval re-diagnosis**: CLOSED. Root cause confirmed (time budget). Do not re-analyze.
- **Four/five-topology TCI dispatch**: Rejected repeatedly. Do not propose.
- **Managed Agents adapter**: Phase 5.
- **Conway/persistent agents**: No updates.
- **Agent payment protocols**: Phase 7.
- **Computer Use**: Mention only if GA announced.
- **Routines vs. cron**: Closed.
- **Task Budgets CLI integration**: Closed (API-only).
- **Session Storage Python SDK**: Closed until Python SDK ships it.
- **S3 transpiler prototype**: Blocked on Gemini CLI install. Do not re-research.
- **EAGLE3**: Available. Stop tracking as development. Mention only if new benchmarks appear.
- **Burst mode for researcher**: Rejected. `FORCE_SWEEP=1` handles the case.

## New Research Areas

- **None this cycle.** The vendor freeze means no new capabilities to track. When the freeze breaks (likely Monday or the pre-I/O window around May 2), new areas may emerge. The ADK+A2A production architecture article is a citation opportunity, not a new research area.

## Research Quality Feedback

### Directive Compliance Assessment (2026-04-19-night → 2026-04-20 Output)

The researcher produced 3 sweep files, 1 analysis, and 1 discussion for Sunday. This exceeds the prior directive's "2-3 files" target, but the excess is justified:

1. **P0 Haiku 3 verification: COMPLIANT.** Guard validated across 3 layers. Correctly treated as non-event. Minimal coverage — no retrospective bloat. Exactly as directed.

2. **P0 Shadow eval verification: EXCELLENT.** The researcher correctly identified the root cause as a time budget constraint (88 min eval vs 44 min session), NOT an LLM behavior issue. The prior directive's two scenarios ("if ran" / "if not ran") were correctly handled via the "not ran" path, with specific diagnosis of WHY (session timeout, not preamble failure). This is the most important finding of the cycle — it transforms S1 from "LLM ignores instructions" (behavioral, hard to fix) to "session too short" (scheduling, easy to fix).

3. **Weekend cadence: SLIGHTLY EXCEEDED.** 5 files vs target 2-3. However:
   - Morning sweep was already split by vendor track (standard — Anthropic + Google + consolidated)
   - Analysis adds genuine value (ADK+A2A cross-pollination, STDIO systemic classification)
   - Discussion correctly produced ADOPT items advancing all three strategic priorities
   - No filler content detected. Every section addresses a directive priority or strategic question.
   - **Assessment**: Acceptable. The content justified the volume.

4. **Analysis quality: HIGH.** Four gap findings, all with concrete pipeline impact. Cross-pollination section maps production ADK+A2A architecture to Phase 5 design — three specific inputs (Agent Cards for fleet registry, feedback loop pattern, Docker isolation model). Strategic priority status correctly assessed (S1 architecturally complete, S2 steady + new data, S3 infrastructure-blocked).

5. **Discussion quality: STRONG.** 7 ADOPT, 5 DEFER, 3 REJECT. Engineer perspective provided genuine pushback (Agent Cards → fleet manifest, burst mode → existing FORCE_SWEEP). The A1 manual eval (P0) and A2 cron job (P1) directly advance S1. The REJECT of burst mode (R1) is well-reasoned.

6. **EAGLE3 tracking closure: COMPLIANT.** Correctly noted as "available" per directive. Stopped tracking as development.

### What to Do Differently Next Cycle

1. **Monday is a normal weekday — full cadence.** After a quiet weekend, expect the researcher to produce the standard complement: sweep report(s), analysis, discussion. No weekend reduction needed.

2. **One-sentence shadow eval status, not multi-paragraph.** The root cause is diagnosed. The fix is defined. The researcher should check experiment_log.json, report PASS/FAIL/PENDING in one line, and move on. Save analysis depth for when results actually exist.

3. **Be aggressive on the factory-steward handoff.** Discussion A2 (dedicated cron job) and A3 (experiment log validator) are the highest-value factory items for Monday. The researcher should produce a clean handoff note with implementation-ready specifications.

## Team Recommendations

- **No structural changes.** Current two-agent setup (researcher + factory-steward) is appropriate. Both vendors are frozen and will stay frozen until the pre-I/O window.
- **Factory-steward priorities for Monday**: (1) Dollar ceiling on steward sessions (P1, carried forward), (2) Programmatic Tool Calling deny rule (P1, carried forward), (3) Dedicated shadow eval cron job (P1, Discussion A2), (4) Experiment log validator (P2, Discussion A3). If Jonas ran the manual eval over the weekend, reprioritize to go/no-go assessment.
- **Human action items for Jonas** (unchanged): (1) Run shadow eval manually — `python3 eval/run_eval_async.py --model claude-opus-4-7 --split train --inter-test-delay 15 .claude/agents/meta-agent-factory.md`. (2) Install Gemini CLI — S3 gate.

---

*This directive supersedes the 2026-04-19-night directive. The researcher should read this at the start of its next sweep (Monday morning).*
