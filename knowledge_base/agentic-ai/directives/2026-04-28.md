# Research Directive — 2026-04-28

**Issued by**: agentic-ai-research-lead
**Effective**: 2026-04-28 night cycle through 2026-04-29 afternoon (next directive)
**Prior directive**: 2026-04-24.md

## Strategic Context

Today produced **two architecturally significant findings** (subagent resumability primitive documented Apr 28, dispatch-primitive comparison verdict: transpiler feasible) and a cluster of operational hygiene items. The S3 day-7 work-around delivered as designed — three of four S3 sub-problems now have positive feasibility verdicts (tool portability via MCP solved, dispatch primitives transpiler-feasible, agent format partially portable per Apr 18). The fourth — orchestration-protocol comparison — is the natural next research target but is deliberately deferred (D3 in today's discussion).

The pipeline is in **decision-friction mode**: research output quality is high, but the factory has not committed since 2026-04-24 afternoon (4 days). The factory queue is at ~15 items, with P2 backlog aging since Apr 22, and today's discussion adds 7 new ADOPTs (5 of them sub-30-min ops items, 1 P1 design section, 1 P2 schema commit). If the next research cycle generates new P0 work, queue conflict materializes. This directive deliberately throttles new research output to let factory drain.

**Key context shifts since last directive:**
1. **Subagent resumability primitive documented Apr 28** — new vendor-shipped capability that inverts Phase 5 watchdog architecture from kill+retry to pause+inspect+resume. First long-running multi-agent pattern available without external state stores.
2. **Dispatch-primitive comparison verdicted: transpiler feasible.** S3 architectural risk materially reduced. Canonical skill schema queued (P2) as the reversible commit.
3. **CC v2.1.119/.120 + 8 regressions** documented. Upgrade target revised: **v2.1.119** preferred, **avoid v2.1.120** (silent release, broken auto-update, `--resume` TypeError crash).
4. **#49562 postmortem closes the root-cause loop.** Three engineering missteps named, all reverted by Apr 20 (v2.1.116). Opus 4.7 model exonerated. Issue #49562 itself OPEN day 12, zero staff response — P2 downgraded per directive's day-10 rule.
5. **Factory has not committed since Apr 24.** P2 backlog 6+ days old. The next factory session is the operational priority, not new research.

## Strategic Alignment

### S1 — Automatic Agent/Skill Improvement

**This cycle delivers two indirect S1 advances:**
1. `PostToolUse.duration_ms` (CC v2.1.119) unlocks per-tool latency as a future optimizer signal — pre-condition for joint trigger+latency optimization. Wiring deferred (D2) until CC upgrade.
2. #49562 postmortem clarifies shadow eval baseline: Apr 20 (v2.1.116) is the post-regression reference. Sonnet 4.6 → Opus 4.7 migration evaluation can proceed on uncontaminated data once Jonas upgrades.

**This cycle does NOT produce new S1 research output** — both advances are gated on the human CC upgrade. Status: incremental, no new strategic signals to research.

### S2 — Multi-Agent Orchestration

**This cycle delivers the most consequential S2 advance in this directive's history:**
- **Inspect-Resume pattern (Adopt #1, P1)** composes Anthropic's Apr 28 resumability primitive with our planned TCI watchdog into a **novel proposed pattern not present in either vendor's documentation**. Citable in S2 paper recovery-pattern section. This is paper-grade material.
- **Dispatch convergence on five semantic axes** (fresh-context isolation, recursion ban, prompt-as-only-channel, allowlist tools, description-driven discovery) is the strongest cross-vendor convergence pattern documented in agentic AI to date — also paper-grade.
- **S2 paper anchor count: 5 pending** (SPIFFE, Memory Profiles, dispatch convergence, Inspect-Resume composition, subagent resumability primitive). Trigger gate is factory queue < 10.

### S3 — Platform Generalization

**This cycle locks in the largest S3 architectural progress in project history:**
- **Dispatch verdict delivered** (Finding 6): partially portable, transpiler feasible with name-binding + feature-degradation rules. 5/12 axes fully portable.
- **Canonical schema queued (Adopt #3, P2)**: reversible JSON Schema commit; defines the contract future transpilers reference.
- **Three of four S3 sub-problems now have positive verdicts.** Orchestration protocol is the only architectural unknown remaining — explicitly deferred (D3) until factory P2 clearance.

S3 tool portability via MCP: one-sentence monitoring only. Both vendors have production MCP support.

## Priority Topics (April 29-30)

### P0 — Must Research

- **Pre-I/O signal window opens May 2-4 (T-4 to T-6 days from now). Researcher to begin signal-collection mode for I/O 2026 (May 19-20).**
  - **What specifically to look for**:
    1. New session-list updates on https://io.google/2026/program/ — particularly any sessions naming ADK, A2A, Mariner, Astra, Gemma, or Gemini Enterprise Agent Platform in the title (none have been named so far).
    2. Pre-I/O blog posts on blog.google or developers.googleblog.com mentioning agent platform or ADK v2.0 GA timing.
    3. Any "Android Show I/O Edition" (May 12 pre-event) preview content that hints at agent-platform updates.
    4. ADK v1.32.0 / v2.0.0b2 release in the Apr 27-May 1 window (today is day 2 of that window). If shipped, full release-note analysis required (similar to v2.0.0b1 graph orchestration deep dive).
    5. gemini-cli v0.40.0 stable release (preview.4 is day 3). If v0.40.0 stable ships, the rendered changelog should catch up — verify the four missing entries (v0.39.1 + v0.40 previews + v0.41 nightlies) become documented.
  - **Why**: I/O 2026 is the largest expected catalyst for our pipeline since Cloud Next. Pre-I/O signal accumulation is the foundation for I/O-day rapid response. Last cycle's directive correctly limited I/O to passive monitoring (T-25d); the window now actively opens.
  - **Output**: Standard sweep section on I/O signals. One paragraph if signals. One sentence if no signals.

- **CC v2.1.121 release watch (highest-leverage tactical item).** Cadence is ~daily during active development. v2.1.121 should land within 2-4 days of Apr 28 and is the natural fix point for the eight community-documented regressions on v2.1.119/.120.
  - **What specifically to look for**: (1) Release date and CHANGELOG entry. (2) Whether the eight gist-documented regressions (especially #53044/#53041 `--resume` TypeError, #53028 broken auto-update) are explicitly fixed. (3) Whether the three Apr 27 issues (#53972 OTel parent-span loss, #53973 model over-generates, #53976 modal freeze) have linked PRs. (4) Whether v2.1.120 ever gets retroactive release notes (currently day 4 silent).
  - **Why**: When v2.1.121 ships, the CC version advisory (operational hygiene Item 2) updates immediately. Jonas's upgrade target may shift from v2.1.119 → v2.1.121.
  - **Output**: One paragraph if v2.1.121 ships; one sentence ("CC v2.1.121: not yet shipped, day [N]") if not.

### P1 — Should Research

- **CC version advisory hygiene watch.** As CC versions evolve (v2.1.119 today → v2.1.121 expected → potentially v2.1.122+), the recommendation set in `eval/model_migration_runbook.md` (once factory adds it per ops hygiene Item 2) needs refresh. Researcher's daily action: add a one-line advisory update to the next sweep when a new CC version ships.
  - **What to look for**: Each new CC release: read the changelog, check community gists / Apr 27-style issue clusters, update the recommended/avoid sets.
  - **Why**: Our cron pipeline depends on CC stability. The v2.1.119/.120 episode (silent release + 8 regressions) demonstrates that release notes alone are insufficient — community-issue triage is now part of the researcher's standard workflow.
  - **Output**: One paragraph in the next sweep that touches CC versions.

- **#49562 status downgrade confirmation.** Today the issue moved to P2 watch-only per the day-10 rule. Confirm the discipline holds: one sentence per directive cycle, no investigative work.
  - **What to look for**: One `gh api repos/anthropics/claude-code/issues/49562 --jq '.state, .comments, .updated_at'` query per sweep. If the issue closes, surface that fact. If staff response appears, surface that. Otherwise: one sentence and move on.
  - **Why**: The root cause is closed by the postmortem. Tracking the issue itself is hygiene only.
  - **Output**: One sentence.

### P2 — Watch Only

- **Subagent resumability stability.** New primitive documented Apr 28. Watch for changes in the public docs at https://code.claude.com/docs/en/agent-sdk/subagents — specifically the `agentId` contract, `resume: sessionId` semantics, and 30-day cleanup default. If I/O changes any of these, our Phase 5.4 design (Inspect-Resume pattern) needs review.
  - One sentence unless the docs change.

- **A2A v1.x version-misreport propagation.** The TheNextWeb-origin "v1.2 + signed agent cards" misreport is now in three channels (TheNextWeb, PRNewswire, Stellagent). Track for further propagation, especially to research-grade venues (arxiv, Semantic Scholar, security advisories). If the misreport reaches a more authoritative venue, escalate with a paragraph correction note in `knowledge_base/agentic-ai/google-deepmind/a2a-protocol.md`. The factory will write the rebuttal section per ops hygiene Item 3.
  - One sentence unless escalation needed.

- **MCP**: Spec at 2025-11-25, 154 days steady. One sentence unless new CVEs or protocol changes.

- **Gemini CLI web changelog catch-up.** Web docs lag tags by 4 days. If v0.40.0 stable ships and the changelog catches up to all four missing entries (v0.39.1 + previews + nightlies), the operational note can close. One sentence.

- **#53972 OTel parent-span loss.** Conditional on future OTEL wiring; no factory queue churn. Tracking item only. One sentence.

- **#53973 "Claude 4.7 ignores project constraints."** Single-data-point complaint. Watch for additional reports — if a cluster forms (3+ similar issues), escalate to MODERATE; otherwise one sentence.

## Deprioritized

- **Orchestration-protocol comparison (A2A multi-hop vs SDK forked subagents).** **DEFERRED (D3)** until factory P2 backlog clearance. The methodology is set (today's dispatch comparison establishes the pattern); the work is well-bounded (~300-500 lines). Promote to P0 in the next-but-one directive (i.e., the directive after the factory's P2-clearance session). Do NOT generate this comparison in the next research cycle — it would directly conflict with the factory's clearance work.
  - **Re-check trigger**: Factory queue drops below 12 items.

- **Transpiler implementations (`transpile_to_claude.py`, `transpile_to_gemini.py`).** **DEFERRED (D1)** until Gemini CLI install completes AND the open question on `invoke_subagent` exposure is empirically validated. Do NOT scaffold transpiler code; the canonical schema (Adopt #3) is the reversible commit.
  - **Re-check trigger**: Gemini CLI v0.39.0+ stable installed.

- **`PostToolUse.duration_ms` optimizer wiring.** **DEFERRED (D2)** until CC v2.1.119+ installed. Document the design as `eval/optimizer-latency-roadmap.md` instead.
  - **Re-check trigger**: CC v2.1.119 (or v2.1.121+ if v2.1.120 is being avoided) installed.

- **Shadow eval re-run.** Blocked on CC upgrade. **One sentence per directive: "Shadow eval: blocked on CC upgrade. No change."** Do not escalate — instrumentation is complete (per-test logging, failure analysis template, re-run protocol).

- **Gemini CLI install for S3 implementation.** Blocked on human action. **One sentence per directive: "Gemini CLI install: blocked, day [N]+. Dispatch comparison delivered as work-around."**

- **1M context beta sunset (Apr 30, T-2d).** Confirmed non-issue (Opus 4.6, Sonnet 4.6 native 1M). **Do not mention.**

- **Computer Use, EAGLE3 / Gemma serving, Managed Agents adapter, Conway patterns, Agent payment protocols.** Maintain prior deprioritized status. One sentence each only if a GA / major announcement lands; otherwise omit.

- **Cloud Next follow-up.** Coverage closed Apr 24. Stop monitoring.

## New Research Areas

- **None this cycle.** The directive is deliberately throttling new research output to allow factory queue clearance. The next directive (post-factory-clearance) will likely promote the orchestration-protocol comparison to P0 as the next net-new research deliverable.

## Research Quality Feedback

### Directive Compliance Assessment (2026-04-24 → 2026-04-28 output)

**Overall: EXCELLENT.** The researcher executed all four prior-directive priorities with discipline.

1. **P0 dispatch-primitive comparison: DELIVERED.** `experiments/dispatch-primitive-comparison.md` exists with verdict "partially portable, transpiler feasible." Table format, parameter-by-parameter, ~300-500 lines as specified. This is the most architecturally consequential research deliverable produced in this directive's history. **Strong execution.**

2. **P1 I/O passive monitoring: COMPLIANT.** No new I/O signals reported correctly. The Apr 28 morning Google sweep noted the May 12 "Android Show I/O Edition" pre-event as a new datapoint but correctly framed it as one paragraph without escalation. **On-target.**

3. **P1 #49562 day-10 status: DOWNGRADE TRIGGERED.** Day 12 today, P2 downgrade applied per the directive's rule. Verbatim status only going forward. **On-target.**

4. **P2 watch-only items: COMPLIANT.** CC v2.1.119+, ADK v1.32.0/v2.0.0b2, Gemini CLI v0.40.0, MCP, S3 tool portability via MCP — all received one-sentence treatment in their respective sweeps. **On-target.**

5. **Deprioritized items: COMPLIANT.** Shadow eval, 1M context beta, Computer Use, S3 transpiler prototype, Cloud Next — all received the prescribed one-sentence-or-omit treatment. **On-target — discipline held.**

6. **Sweep length and signal-to-noise**: Anthropic morning ~140 lines, Google morning ~130 lines, afternoon ~165 lines (longer due to community regressions context) — all within the directive's "standard ~100-150 lines" range or justified deviation. Analysis ~480 lines justified by 9 findings + 2 architecturally significant items + multi-sweep consolidation. **Right-sized.**

7. **Discussion ADOPT discipline**: 7 ADOPTs, 0 net-new factory items (all refinements to existing artifacts or single-file additions). **Excellent discipline.** The Engineer's location-based push-back on the Innovator's standalone Inspect-Resume file (R1 reject) demonstrates real adversarial review, not rubber-stamping.

8. **Continuity Notes carry-forward**: Maintained across all three sweeps and the analysis. The misreport hygiene table is novel addition this cycle — captures the A2A "v1.2" misreport propagation cluster as a tracked item. **Strong directive responsiveness.**

### What to Do Differently Next Cycle

1. **Throttle new P0 research output.** The factory queue is at ~15 items + 7 new ADOPTs from today (3 P1, 4 P2-P3). The factory has not committed since Apr 24. Producing a new P0 deliverable in the next research cycle would create queue conflict that the discussion's D3 deferral was designed to prevent. **The next sweep should be a tight signal-collection pass (~80-120 lines), not a generation pass.**

2. **Pre-I/O signal collection becomes the dominant theme starting May 2.** Today is T-21 days. Pre-I/O window opens T-15 to T-17 (May 2-4). The next cycle is the last "calm" cycle before signal density rises. Use it for hygiene cleanup (CC version advisory updates, A2A misreport rebuttal note prep) rather than novel deliverables.

3. **Continue the consolidated human-action format.** Current format is correct: one-line CC upgrade target + one-line Gemini CLI install per directive. The Apr 24 directive established this format and today's sweeps held the line. Maintain.

4. **Misreport hygiene as a tracked discipline.** The "verify-at-tag-level" practice for A2A versions is now standard. Apply the same discipline preemptively to any future "v1.x" claim from a non-tag source for ADK, Gemini CLI, or CC. Specifically: if WebSearch surfaces a release-version claim, the first verification should always be `gh api .../tags`, not the WebSearch summary.

5. **Sweep length norm holds.** Standard ~100-150 lines unless a genuinely new signal emerges. If only stabilization signals exist, sweeps should shorten further (~80-100 lines). Today's afternoon sweep was 165 lines because of the community-regression context — that justification was correct, but it's not a new norm.

## Team Recommendations

### Human Action Items for Jonas (Consolidated — Format Unchanged from Apr 24)

1. **Upgrade CC to v2.1.119** (NOT v2.1.120 — silent release, eight community regressions, broken auto-update). This unblocks shadow eval AND `PostToolUse.duration_ms` optimizer wiring (D2). The advisory will live in `eval/model_migration_runbook.md` once factory processes ops hygiene Item 2.
2. **Install Gemini CLI v0.39.0+ stable** — gates S3 implementation. Day 7+ blocker. Day-7 work-around dispatch comparison delivered. Empirical transpiler validation (D1) and orchestration-protocol comparison (D3) both gate on this.
3. **Run paper pipeline** when factory queue drops below 10 — five anchors are now ready (SPIFFE, Memory Profiles, dispatch convergence, Inspect-Resume composition, subagent resumability primitive). Threshold check is informational; Jonas decides.

### Factory-Steward Priority Queue (~15 existing items + 7 new ADOPTs from today)

**The factory has not committed since 2026-04-24 afternoon (4 days). This is the operational priority for the next factory session.** Recommended sequencing (in order):

1. **Adopt #1 — Inspect-Resume Phase 5.4 section** (P1, ~30 min). Architecturally consequential; section within Phase 5 design index, not standalone file. Citable in S2 paper. **Highest-leverage single addition this week.**
2. **Adopt #2 — Task↔Agent dual-name validator pass** (P1, ~10 min). Real defect probability with low fix cost. Limit scope to `eval/run_eval_async.py`, `eval/run_eval.sh`, `scripts/lib/*.sh`, `.claude/hooks/*.sh`.
3. **ROADMAP status line update** (P1, per `roadmap-updates-2026-04-28.md` Change 1). Day counts shifted, CC version advisory shifted to v2.1.119, subagent resumability primitive added, dispatch verdict added.
4. **Adopt #4 — CC version advisory section in `eval/model_migration_runbook.md`** (P2, ~10 min). v2.1.119 recommended, v2.1.120 avoid. Dashboard reads via grep.
5. **Adopt #6 — I/O Sensitivity registry update** (P2, ~5 min). Two rows: subagent resumability + dispatch primitives. Belongs in existing Phase 5 design index P1 item #1.
6. **Adopt #5 — A2A misreport rebuttal note in KB** (P2, ~10 min). Three-paragraph Version Verification Notes section. May be done by researcher in next sweep instead.
7. **Adopt #3 — Canonical skill schema** (P2, ~1-2 hours). JSON Schema + round-trip test against 23 Claude agents. Most substantive code commit but reversible. Could slip to next session if budget tight.
8. **Adopt #7 — S2 paper anchor surfacing in dashboard** (P3, ~20 min). Lowest priority; informational only.
9. **Existing P2 backlog clearance** (~5 items, aging since Apr 22). Once today's adopts process, attack these.

**Total estimated effort**: ~3-4 hours for adopts #1-7 + existing P1/P2 backlog. Comfortably within 1-2 factory sessions.

**Critical sequencing constraint**: The orchestration-protocol comparison (D3) cannot be promoted to P0 in the next-but-one research-lead directive until the factory queue drops below 12. The factory's clearance pace directly gates the next research P0.

### No Structural Team Changes

The pipeline structure is performing well. Researcher executed Apr 24 directive with discipline (excellent compliance + delivered architecturally significant deliverable). Discussion produced 7 ADOPTs at 0 net-new factory queue items — the gold-standard ratio. The Engineer's location-based push-back on R1 demonstrates real adversarial review.

**One observation** (not a change recommendation): The factory's 4-day commit gap is the only operational concern. If the gap extends to 7+ days without explanation, escalate. Factory is the bottleneck phase right now, and the next-but-one research P0 (orchestration-protocol comparison) is gated on its throughput.

| Metric | Value | Assessment |
|--------|-------|------------|
| Directive compliance | 5/5 priorities executed; ≤1 net-new ADOPT met (0 net-new) | Excellent |
| P0 deliverable | dispatch-primitive comparison delivered with verdict | Strong |
| Sweep scope | 3 sweeps today (~140/130/165 lines) | Right-sized |
| Analysis depth | 9 findings, 4 CP, 6 threats, full continuity table, ~480 lines | Strong |
| Discussion quality | 7 ADOPTs, 0 net-new factory items, 1 explicit REJECT (R1) | Excellent (real adversarial review) |
| Strategic alignment | S1: incremental. S2: substantive (Inspect-Resume + 5 anchors). S3: largest progress in project history. | Most balanced advance to date |
| Factory commit gap | 4 days since Apr 24 afternoon (15 items + 7 new ADOPTs) | Operational concern — clearance is next directive's gate |
| Misreport hygiene | A2A "v1.2" misreport tracked across 3 channels, rebuttal queued | Discipline maintained |

---

*This directive supersedes 2026-04-24.md. Key changes: (1) **Pre-I/O signal collection promoted to P0** — window opens May 2-4, T-4 to T-6 days from now. (2) **CC v2.1.121 release watch promoted to P0** — natural fix point for eight v2.1.119/.120 regressions, 2-4 day expected ETA. (3) **Orchestration-protocol comparison explicitly deferred** (D3) — gated on factory P2 clearance per discussion engineering review. (4) **Throttled new research output** — next cycle is tight signal-collection pass (~80-120 lines), not generation. (5) **Human action target updated**: CC upgrade target shifts v2.1.118 → v2.1.119, with explicit advice to AVOID v2.1.120. (6) **#49562 P2 downgrade confirmed** — day 12, postmortem closes root cause. (7) **Factory commit gap (4 days) flagged** as operational concern — gates next research P0 promotion.*
