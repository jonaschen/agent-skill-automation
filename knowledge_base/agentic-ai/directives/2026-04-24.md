# Research Directive — 2026-04-24

**Issued by**: agentic-ai-research-lead
**Effective**: 2026-04-24 through 2026-04-25 (next directive)
**Prior directive**: 2026-04-23-night.md

## Strategic Context

Both vendor tracks have entered a stabilization period. Anthropic shipped nothing since the CC v2.1.118 + SDK triple release on Apr 23. Google's Cloud Next concluded Apr 24 with no new platform announcements beyond the Day 1 keynote. The industry exhales; we consolidate.

The pipeline is in the right posture: the researcher produced a correctly-sized normalization sweep (140 lines vs. 247 for Cloud Next catch-up), maintained strict ADOPT discipline (0 net-new queue items), and resolved the A2A version divergence tracking item. The factory has 15 active items but has not committed since Apr 23 — the P2 backlog (5 items, all Phase 5 design notes) is now 2+ days old and needs attention.

**Key context shifts since last directive:**
1. **Stabilization confirmed.** Both vendors are digesting their own announcements. Next signal window is pre-I/O (~May 2-15).
2. **A2A version divergence resolved.** Triple-verified at v1.0.0. TheNextWeb's "v1.2" was a media error. Phase 5 design can cite A2A with confidence.
3. **Gemini CLI v0.39.0 stable released.** The S3 blocker is now a human-install-only bottleneck. Unified `invoke_subagent` provides the comparison target. Per the day-7 work-around pivot, research should proceed with documentation-based comparison even without install.
4. **Human action debt accumulating.** CC upgrade (day 5+) and Gemini CLI install (day 7+) are both execution blockers, not research gaps. Everything on the research/engineering side is instrumented and ready.

## Strategic Alignment

### S1 — Automatic Agent/Skill Improvement

**This cycle advances S1 indirectly.** The MCP tool hooks design note (factory P3 item #14) is still queued. Shadow eval remains blocked on human CC upgrade — 8th directive cycle of this blocker. No new S1 signals from either vendor.

**Day-7 pivot for shadow eval**: Stop escalating the CC upgrade as P0 in every sweep. The researcher has done everything possible. Reduce to one-sentence status: "Shadow eval: blocked on CC upgrade. No change." The pipeline instrumentation (per-test logging, failure analysis template, re-run protocol) has been complete since Apr 21. When Jonas upgrades, the eval runs immediately — no research prep needed.

### S2 — Multi-Agent Orchestration

**This cycle advances S2 through three channels:**
1. **SPIFFE Agent Identity deep technical details** (Finding 6 from today's analysis) provide the first published production agent identity architecture. Directly citable in the S2 paper's governance section. The authorization-vs-authentication observation (Anthropic = what can you do; Google = who are you) is a novel finding for the paper.
2. **Unified sub-agent dispatch convergence** (CP1) — both vendors independently arrived at `dispatch(type, prompt) → result`. Citable as convergent evolution in orchestration design.
3. **Paper pipeline trigger**: Cloud Next data is the richest S2 paper input since the project started. The paper-synthesizer should run when factory queue drops below 10 items or by May 1, whichever comes first.

### S3 — Platform Generalization

**This cycle activates the day-7 work-around for S3.** Gemini CLI install has been blocked for 7+ days. Per A5 (adopted in today's discussion), the pivot strategy is: **instruct the researcher to produce a documentation-based dispatch-primitive comparison** (`Agent(subagent_type, prompt)` vs `invoke_subagent(agent, prompt)`) from release notes and source documentation alone, without requiring CLI install.

This is the narrowest possible S3 feasibility test (per A2 from today's discussion — sharpen format comparison to dispatch primitives). If dispatch semantics are portable, the rest of S3 becomes a transpiler problem. If not, S3 needs a different architecture.

S3 tool portability via MCP: one-sentence monitoring only. Both vendors have production MCP support. Problem solved.

## Priority Topics (April 25-26)

### P0 — Must Research

- **S3 dispatch-primitive comparison (documentation-based)**: Produce a side-by-side analysis of Claude Code's `Agent` tool dispatch vs Gemini CLI's `invoke_subagent`. Focus on: parameter semantics, return value contract, specialization mechanism, error handling model, context sharing behavior. Use release notes, source documentation, and published API references — do not require CLI install.
  - **What specifically to look for**: (1) Does `invoke_subagent` support the same `subagent_type` specialization pattern? (2) How does each handle context passing — full conversation vs. fresh context? (3) What's the sub-agent tool permission model in each? (4) Can the dispatch interface be abstracted into a vendor-neutral `dispatch(type, prompt, options) → result`?
  - **Why**: This is the day-7 pivot for S3 (work-around for Gemini CLI install blocker). The `invoke_subagent` unification in v0.39.0 provides the first clean comparison target. This comparison determines whether S3 format portability is architecturally feasible.
  - **Output**: New file `knowledge_base/agentic-ai/experiments/dispatch-primitive-comparison.md`. Table format: parameter-by-parameter comparison. Verdict: portable / partially portable / not portable.

### P1 — Should Research

- **I/O pre-preparation (passive monitoring)**: I/O is T-25 days (May 19-20). Continue tracking published session descriptions for ADK/A2A/Gemini 4 mentions. Cloud Next established the baseline; I/O will deepen it.
  - **What to look for**: Session description updates, speaker announcements linking to known contributors, any pre-I/O blog posts or API previews.
  - **Output**: One paragraph if new I/O signals. Otherwise one sentence: "I/O [N] days. No new session signals."

- **#49562 status (approaching P2 threshold)**: Day 8 today. If still silent at day 10 (Apr 28), downgrade to P2 Watch Only. SDK v0.1.65 `ThinkingConfig.display` is the relevant workaround.
  - **Output**: One sentence: "#49562: day [N], [status], [staff response if any]."

### P2 — Watch Only

- **CC v2.1.119+**: Monitor for follow-up release. One sentence.
- **ADK v1.32.0 / v2.0.0b2**: Expected ~Apr 27-May 1. One sentence.
- **Gemini CLI v0.40.0**: Preview active. One sentence.
- **MCP**: Steady. One sentence unless new CVEs or protocol changes.
- **S3 tool portability via MCP**: One sentence only: "MCP convergence: [status]."

## Deprioritized

- **Shadow eval re-run**: Human action (CC upgrade). One sentence only: "Shadow eval: blocked on CC upgrade. No change." Do not escalate further — everything is instrumented.
- **1M context beta sunset (Apr 30)**: Confirmed non-issue for our pipeline (Opus 4.6, Sonnet 4.6 have native 1M). Do not mention.
- **Computer Use**: Beta. Mention only if GA announced.
- **S3 transpiler prototype**: Blocked on Gemini CLI install. Do not mention. (The dispatch comparison above is the work-around.)
- **EAGLE3 / Gemma serving**: Stop tracking.
- **Managed Agents adapter / Conway patterns / Agent payment protocols**: Phase 5-7. Do not mention.
- **All items inherited from prior directive's deprioritized list**: Maintained.
- **Cloud Next follow-up**: Day 2-3 sessions revealed no new agent platform announcements beyond Day 1 keynote. Cloud Next coverage is complete. Stop monitoring Cloud Next specifically.

## New Research Areas

- None this cycle. The dispatch-primitive comparison (P0) is the only new work product, and it falls within the existing S3 research track.

## Research Quality Feedback

### Directive Compliance Assessment (2026-04-23 night → 2026-04-24 output)

**Overall: EXCELLENT. The researcher executed the normalization posture perfectly.**

1. **ADOPT discipline**: The directive asked for ≤1 net-new ADOPT. The researcher produced exactly 0 net-new factory queue items. All 6 discussion ADOPTs were template refinements or refinements to existing items. This is the ideal normalization output — useful work without queue pressure.

2. **Cloud Next follow-up (P0)**: Correctly identified that Cloud Next Day 2-3 produced no new agent platform announcements. The SPIFFE deep technical details from published docs were a legitimate P0 finding — they deepened existing Cloud Next coverage rather than discovering new events. The sweep correctly compressed the Anthropic track (quiet day, 3 findings) and expanded the Google track (3 substantive findings).

3. **A2A version divergence (P1)**: **Resolved.** Triple verification (GitHub tag, a2a-protocol.org spec, spec content) confirms v1.0.0. This was strong investigative work — the researcher didn't accept the TheNextWeb report at face value and cross-validated against authoritative sources. P1 tracking item closed.

4. **Sweep length**: 140 lines — correctly back to standard range after the 247-line Cloud Next catch-up. This shows the researcher adapts to signal intensity appropriately.

5. **Analysis quality**: The 4 cross-pollination opportunities (CP1-CP4) and 4 threat assessments are substantive and novel. CP1 (unified sub-agent dispatch convergence) and CP3 (security model divergence as authorization vs authentication) are particularly strong — both are observations not present in any prior analysis and are directly citable in the S2 paper.

6. **Continuity Notes**: The continuity table at the end of the analysis (6 rows tracking prior findings) was explicitly requested in the prior directive's quality feedback. Correctly implemented. Good directive responsiveness.

### What to Do Differently Next Cycle

1. **Produce the dispatch-primitive comparison (P0 above)**. This is the most important output for the next cycle. It should be thorough enough to determine S3 architectural feasibility. Table format with parameter-by-parameter comparison. ~300-500 lines is appropriate for this type of comparative analysis.

2. **Keep sweep length standard** (~100-150 lines) unless a genuinely new signal emerges (CC v2.1.119, ADK v1.32.0, I/O pre-announcement). The stabilization period should produce shorter sweeps, not longer ones.

3. **Stop repeating human action escalations verbatim**. The CC upgrade and Gemini CLI install have been escalated for 8+ directive cycles. The message has been delivered. Going forward: one sentence each in the sweep's action items section. Save the analysis space for the dispatch comparison.

4. **Factory queue awareness**: The P2 backlog (5 items, aging since Apr 22) needs the factory's attention. The researcher should avoid producing any new work that competes with factory P2 clearance. If the factory processes 3-4 items in the next session, the queue should drop to ~11-12 items — approaching the paper pipeline trigger threshold (< 10 items).

## Team Recommendations

### Human Action Items for Jonas (Consolidated — No Longer Repeated in Full)

1. **Upgrade CC to v2.1.118** — Blocks shadow eval. Everything is instrumented. When upgraded, eval runs immediately.
2. **Install Gemini CLI v0.39.0 stable** — Gates S3 implementation. Day-7 pivot: documentation-based comparison proceeding as work-around.
3. **Run paper pipeline** when factory queue < 10 items — Cloud Next data ready for S2 paper.

### Factory-Steward Priority Queue (~15 active items)

**No changes from prior assessment.** The factory should focus on P1 clearance (items #1-3) this session, then move to P2 backlog (items #4-8) next session.

Key note: the A2A version correction (C1 from discussion) — update any factory queue items or design notes referencing "A2A v1.2" to "A2A v1.0.0". This is a correction applied during normal factory work, not a queue item.

### No Structural Team Changes

The pipeline is performing well in normalization mode. The researcher's output quality is high. Discussion discipline is strong. The day-7 work-around pivot (A5 from today's discussion) is the only behavioral change, and it's already incorporated into this directive's P0 instruction.

| Metric | Value | Assessment |
|--------|-------|------------|
| Directive compliance | ≤1 net-new ADOPT met (0 net-new) | Excellent |
| Sweep scope | 7 findings, 140 lines | Right-sized for normalization |
| Analysis quality | 4 CP + 4 threats, CP1/CP3 novel | Strong |
| A2A resolution | P1 item closed via triple verification | Good investigative work |
| Discussion ADOPTs | 6 items, all refinements, 0 net-new queue | Disciplined |
| Factory queue | 15 items, P2 backlog aging (2+ days) | Monitor P2 clearance |
| Strategic alignment | S1: blocked. S2: paper data ready. S3: day-7 pivot activated | Balanced |

---

*This directive supersedes 2026-04-23-night.md. Key changes: (1) S3 dispatch-primitive comparison elevated to P0 — day-7 work-around pivot activated, (2) Shadow eval escalation reduced to one-sentence — everything instrumented, nothing more to do, (3) Cloud Next follow-up monitoring completed — stop tracking Cloud Next specifically, (4) Human action escalation format condensed — message delivered, stop repeating verbatim, (5) A2A version confirmed at v1.0.0 — P1 tracking item closed.*
