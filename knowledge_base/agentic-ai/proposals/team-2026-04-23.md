# Team Proposal — 2026-04-23

**Topic**: Team Composition & Workload Assessment
**Researcher**: agentic-ai-research-lead (Gemini)

## 1. Current Assessment
The two-agent research fleet (`researcher` and `research-lead`) has successfully stabilized after the "Observability Blackout." The current workload is heavy on **technical specification** (Session Rewind, OTEL, Budget Gates) and **regression fixing** (Opus 4.7). The researcher is effectively alternating between P0 operational recovery and P1 strategic design.

## 2. Proposals

### 2.1 "Shadow Performance" Sub-Agent (P1)
**Concept**: As new models (Opus 4.7, Gemini 3.1) release more frequently, we need a dedicated "Shadow Performance" sub-agent.
**Role**: This sub-agent would run in the background, executing a subset of the Phase 2/3 eval suite on *non-deployed* models and reporting regressions *before* we attempt to upgrade.
**Implementation**: Can be implemented as a specialized configuration of the `skill-quality-validator`.

### 2.2 Security Auditor (Phase 5 Prep) (P2)
**Concept**: With the emergence of "Goal Hijacking" as a top OWASP risk for multi-agent systems, we should prepare a "Security Auditor" role.
**Role**: Specifically tasked with reviewing "Track B" (high-coupling) task traces for intent drift or adversarial tool use.
**Timing**: Should be ready for Phase 5.5 (HITL/Reflections).

### 2.3 Workload Management
No additional human headcount or new agent identities are required this cycle. However, we recommend a **"Strategic Rest"** for the researcher once the Opus 4.7 regression is resolved. This involves a 24h period of "L1 Only" (news monitoring) to reset quota and prevent burn-out (token-based exhaustion).

## 3. Resource Requirements
- **Compute**: Requesting an additional **20% token quota allocation** for the next 48h to accommodate the parallel `autoresearch-optimizer` branches for the Opus 4.7 recovery.

## 4. Expected Outcome
Stability in the core `meta-agent-factory` on SOTA models and a verified implementation path for Phase 5's state management.
