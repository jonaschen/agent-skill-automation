# ROADMAP Update Recommendations — 2026-04-10

Generated by agentic-ai-researcher (Mode 2c: L4 Strategic Planning)
Based on: Deep Analysis 2026-04-10 + Discussion 2026-04-10

---

## PROPOSED CHANGES

### 1. Phase 4.4: Add fleet version check task (P1 — ADOPT from discussion A2)

**Current state**: No task for fleet version management.

**Proposed addition** to Phase 4.4 (Security hardening):
```
- [ ] **Fleet version check**: Create `scripts/lib/check_fleet_version.sh` + `scripts/lib/fleet_min_version.txt` (>=2.1.97). Source from all 6 daily scripts. Warn on mismatch, never block. Picks up MCP memory leak fix, 429 retry fix, and permission hardening — P1
```

**Justification**: Three operationally significant fixes in v2.1.97 (memory leak, retry exhaustion, permission bypass). Fleet has no version verification mechanism. Zero-cost defense-in-depth.

---

### 2. Phase 4.4: Add prototype collision audit task (P0 — ADOPT from discussion A1)

**Current state**: No audit for JS prototype collision vulnerability.

**Proposed addition** to Phase 4.4 (Security hardening):
```
- [ ] **Prototype collision audit**: Grep `.claude/` configs for JS prototype property names (toString, valueOf, hasOwnProperty, constructor, __proto__). Verify clean; escalate if any match found — P0
```

**Justification**: 30-second verification with zero downside. Closes audit gap from v2.1.97 changelog.

---

### 3. Phase 5.3.0: Expand A2A evaluation scope (P1 — ADOPT from discussion A3)

**Current text**: "Evaluate A2A v1.0.0 ... Critical test: can A2A message format express all 6 message types natively?"

**Proposed text**: "Evaluate A2A v1.1 **SDK integration patterns** for scrum-team-orchestrator transport. Critical test: what Python A2A SDK integration pattern gives us A2A-native transport for our 6 message types? Scope expanded from format-mapping to SDK integration architecture per April 10 analysis — A2A now production-proven across all three hyperscalers (150+ orgs)."

**Justification**: A2A crossed the irreversibility threshold. The question is no longer "should we use A2A?" but "how do we integrate the A2A SDK?" Evaluation scope should match this shift.

---

### 4. Phase 5.3: Add A2A-native design principle note (P2 — from discussion D1)

**Proposed addition** to Phase 5.3 section header or as a note:
```
**Design principle (2026-04-10)**: Implement the 6-message-type bus directly on the A2A Python SDK — not as a custom protocol with an A2A wrapper. A2A is now the de facto inter-agent standard (150+ orgs, triple hyperscaler integration). Our message types map to A2A Task metadata.
```

**Justification**: Records the design direction while deferring implementation to post-I/O. Zero cost now; prevents wrong-direction work later.

---

### 5. Phase 5.4: Add RSS memory monitoring requirement (P2 — from discussion D3)

**Proposed addition** to Phase 5.4 (watchdog-circuit-breaker):
```
- [ ] Monitor process RSS alongside token velocity — alert on sustained >200MB/hr growth. Thresholds TBD from real Phase 5 session profiles
```

**Justification**: MCP memory leak (50 MB/hr) exposed systemic risk for long-running agent sessions. Phase 5 multi-agent sessions will be longer. Memory should be a first-class watchdog signal.

---

### 6. Phase 6.4: Add EAGLE3 speculative decoding note (P2 — from analysis 1.3)

**Proposed addition** to Phase 6.4 (Model packaging pipeline):
```
- [ ] Optional: Evaluate EAGLE3 speculative decoding for Gemma 4 — 1.72x speedup with 277MB draft head. Note: Gemma 4 hybrid attention has known bugs (attention scaling, V=K handling, partial RoPE) that standard frameworks may not handle correctly — verify serving framework compatibility
```

**Justification**: Narrows Gemma 4's speed disadvantage from 5.5x to ~3.2x vs Qwen. Not blocking but provides a concrete optimization recipe for latency-sensitive edge deployments.

---

### 7. Risk table: Add A2A version migration risk

**Proposed new risk row**:
```
| A2A v1.0→v1.1 migration | 5 | Defer Phase 5 implementation to post-I/O; evaluate v1.1 SDK before committing to integration pattern | New — deferred pending I/O |
```

**Justification**: If we start building on A2A v1.0 SDK and v1.1 changes the API surface, we'd need to rework. The current deferral to post-I/O is the mitigation, but it should be tracked as a named risk.

---

### 8. Risk table: Add fleet version skew risk

**Proposed new risk row**:
```
| Fleet Claude Code version skew | 4 | Fleet version check script (>=minimum); warn on mismatch in all daily scripts | New — mitigation proposed (P1) |
```

**Justification**: 8 daily agent runs with no version verification. OS-level auto-updates or missed manual updates could silently regress security fixes or introduce breaking changes.

---

### 9. Phase 4 status line update

**Proposed status update** (ROADMAP line 4):
```
**Status as of 2026-04-10: Phase 4 in progress. Remaining: stress test execution, regression test, cost analysis, fleet version check, prototype collision audit. Fleet: monitoring effort levels (day 2 of 3, Apr 9-12). Phase 4 hard deadline: May 9, 2026 (29 days, before Google I/O).**
```

---

## NO-ACTION ITEMS (recorded for context)

- **Managed Agents branding rules** (P3): Partners may use "Claude Agent" but not "Claude Code" branding. Relevant for Phase 7 white-labeling. No ROADMAP change needed yet.
- **Enterprise stack pricing comparison** (P2): Waiting on Google Agent Engine pricing. Record for Phase 7 planning when both vendors have published.
- **Pre-I/O monitoring**: Already in place (task 4.4). No ROADMAP change. Factory-steward I/O freeze instruction proposed separately in skill-updates.
