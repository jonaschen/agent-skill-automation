# Agentic AI Sweep Report — 2026-04-18 (Evening, Anthropic Only)

## Executive Summary
- **Two new Claude Code releases**: v2.1.113 (native binary spawn, major security hardening, Opus 4.7 Bedrock fix) and v2.1.114 (agent teams crash fix). Published April 17.
- **Opus 4.7 post-launch bug landscape mapped**: Bedrock `thinking.type.enabled` error RESOLVED in v2.1.113. Token burn rate regression under investigation. Quality regression reports are low-quality but align with documented behavioral changes.
- **No `claude -p`-specific bugs found** — our pipeline's programmatic invocations appear safe on Opus 4.7 with v2.1.113+.
- **P1 directive items resolved**: Task Budgets NOT available on CLI (API-only, use `--max-budget-usd` for CLI cost control). OTEL tracing works with `claude -p` via env vars (no SDK migration needed — days not months to implement). 1M beta sunset audit confirmed clean (already completed morning sweep).
- **Haiku 3 retires TOMORROW** (April 19). All guards verified.

## Anthropic Updates

### Claude Code
- **v2.1.113** (April 17, 19:09 UTC): **Native binary spawn** (CLI spawns per-platform binary instead of bundled JS). **5 security fixes** (deniedDomains, macOS dangerous paths, exec-wrapper matching, find -exec, sandbox permission prompt). **Opus 4.7 Bedrock ARN fix** (`thinking.type.enabled` 400 error resolved). **Subagent 10-min timeout** (prevents hanging). **`/loop` Esc cancellation**. **15+ bug fixes** including MCP concurrent-call timeout, `CLAUDE_CODE_EXTRA_BODY` effort on non-effort models, long-context compaction on resumed sessions.
- **v2.1.114** (April 17, 23:26 UTC): Single fix — crash in permission dialog when agent teams teammate requests tool permission.
- **Source**: https://github.com/anthropics/claude-code/releases, https://code.claude.com/docs/en/changelog

### Agent SDK
- **No new releases** since v0.1.61 (April 16). **OTEL architecture clarified** (directive P1 resolved): telemetry is produced by the CLI, not the SDK. Configuration via env vars (`CLAUDE_CODE_ENABLE_TELEMETRY=1` + `CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1` + `OTEL_TRACES_EXPORTER=otlp`). **Works with `claude -p`** — parent env vars propagate to child CLI processes. No SDK migration needed for tracing. `TRACEPARENT` propagates to Bash tool subprocesses.
- **Source**: https://code.claude.com/docs/en/agent-sdk/observability

### Model Context Protocol
- MCP: no change since morning sweep. 10K+ servers. Triggers & Events WG active.

### Tool Use & Function Calling
- **Task Budgets CLI investigation** (directive P1 resolved): NOT available on Claude Code CLI. API-only feature (Messages API + `task-budgets-2026-03-13` beta header). CLI alternative: `--max-budget-usd` (dollar cap) + `--max-turns` (iteration cap). Investigation closed.
- **Source**: https://platform.claude.com/docs/en/build-with-claude/task-budgets

### Computer Use
- No change. Still research preview on API. No GA announced.

### Multi-Agent Patterns
- **v2.1.114**: Agent teams crash fix (teammate permission dialog). **Routines** capabilities detailed: min 1h interval, `claude/` branch prefix only, Pro 5/day, Max 15/day. **Five-pattern taxonomy** confirmed from 2026 Agentic Coding Trends Report (adds Message Bus + Shared State to the four documented patterns).

### Model Releases
- **Opus 4.7 day 3**: Post-launch bug landscape mapped. Bedrock `thinking.type.enabled` error RESOLVED (v2.1.113). Token burn rate reports (1% quota per prompt) under investigation — likely 1.35× tokenizer + default `high` effort. Quality regression reports (3 issues) are low-quality `needs-repro`. **No `claude -p`-specific bugs found.**
- **Haiku 3**: retires TOMORROW (April 19). `deprecated_models.json` guard verified. `claude-code-security-review` #69 documented silent failure from hardcoded Haiku model — pattern lesson recorded.
- **1M beta sunset** (April 30, 12 days): Audit confirmed CLEAN — fleet uses GA models with native 1M. No code changes needed.

## Implications for Our Pipeline

- **P0: Update to v2.1.113+** before next steward run — Opus 4.7 Bedrock fix, subagent timeout, security hardening. The native binary spawn is a significant architecture change; verify `claude -p` works correctly.
- **P0: Haiku 3 retirement tomorrow** — all guards verified, no action needed.
- **P1: OTEL tracing implementation unblocked** — add 6 env vars to `scripts/daily_*.sh` scripts to get distributed tracing. Requires deploying an OTEL collector (Jaeger/Grafana Tempo). Implementation: days, not months. Recommend as next factory-steward ADOPT item.
- **P1: Add `--max-budget-usd` to steward scripts** — Task Budgets are API-only, but `--max-budget-usd 10.00` on `claude -p` invocations would provide cost guard. Low-effort change.
- **P1: Monitor Opus 4.7 token burn rate** — if 1.35× tokenizer overhead is confirmed, steward session costs increase ~35%. Baseline with shadow eval before full migration.
- **P2: Agent teams crash fix** (v2.1.114) — relevant for future Phase 5 agent teams topology.
- **P2: Routines vs cron confirmed** — Routines cannot replace our pipeline (1h min interval, `claude/` branches only, no perf logging). May work for simple single-agent tasks.

## Directive Item Resolution

| Directive Item | Priority | Status | Finding |
|---------------|----------|--------|---------|
| Opus 4.7 `claude -p` bug check | P0 | ✅ Resolved | No `claude -p`-specific bugs. Bedrock ARN fix in v2.1.113. |
| Haiku 3 post-retirement check | P0 | ⏳ Tomorrow | Guards verified. Check API error response on Apr 19 morning. |
| 1M context beta sunset readiness | P1 | ✅ Resolved | Fleet clean. No beta headers. No code changes needed. |
| Task Budgets CLI availability | P1 | ✅ Resolved | NOT available on CLI. Use `--max-budget-usd` instead. |
| Agent SDK OTEL CLI compatibility | P1 | ✅ Resolved | Works via env vars. No SDK migration needed. Days to implement. |

## Actions Taken
- Updated all 7 Anthropic KB files with 2026-04-18 evening findings
- New entries: claude-code.md (v2.1.113 + v2.1.114), model-releases.md (Opus 4.7 bug landscape, Haiku 3 pre-retirement), tool-use.md (Task Budgets CLI investigation closed), agent-sdk.md (OTEL CLI architecture clarified), multi-agent-patterns.md (Routines details, 5-pattern taxonomy), model-context-protocol.md (no change), computer-use.md (no change)
- No new `deprecated_models.json` entries needed

## Next Sweep Focus
- **Haiku 3 retirement verification** (April 19 morning) — confirm API returns clean error pointing to Haiku 4.5
- **v2.1.113 native binary stability** — verify `claude -p` works correctly with new binary spawn architecture
- **Opus 4.7 token burn rate investigation** — track GitHub issues #49356, #49541, #49562 for Anthropic response
- **Google I/O pre-leak window** (opens ~May 2, 14 days) — begin monitoring
