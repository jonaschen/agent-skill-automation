# Steward Review — 2026-04-16

> Reviewer: project-reviewer agent | Run time: 7:00 AM Asia/Taipei
> Fleet version: 2.1.104 (minimum 2.1.101) — OK for all agents that ran

---

## Summary

| Steward | Verdict | Commits | Key Finding |
|---------|---------|---------|-------------|
| android-sw-steward | on-track | 2 | Phase 6 defined and 6.1 complete (L3-qualcomm-kernel-expert); routing 105/105 (100%); test count 117→124 |
| arm-mrs-steward | needs-correction | 3 | 488→508 eval tests; EX-3 formalized; P0 arm-pmu.md blocked day 9+; auto-commit captured `note.txt` junk artifact |
| bsp-knowledge-steward | on-track | 2 | 300→340 eval cases; graph 1109→1167 nodes; `kuzu_db` binary committed to git (should be gitignored) |
| ltc-steward | blocked | 0 | No run on 2026-04-16; rate limit hit in 13s on 2026-04-15; 0 meaningful work since 2026-04-13; API key blocker open |

---

## Android-SW Steward

### Verdict: on-track

### What Was Done
- **Phase 6 defined**: 5 deliverables (6.1–6.5) added to ROADMAP — OEM L3 skill buildout and maintenance mode. Directly addresses the P1 from the 2026-04-13 review ("define Phase 6").
- **Phase 6.1 complete**: `L3-qualcomm-kernel-expert` skill deployed:
  - `skills/L3-qualcomm-kernel-expert/SKILL.md` — full L3 domain skill with SoC codename/GKI branch table, ADSP/PIL lifecycle, cam_smmu IOMMU patterns, KMI compliance, 8 forbidden actions, parent-skill handoff rules
  - `skills/L3-qualcomm-kernel-expert/scripts/check_qcom_kmi_symbols.sh` — KMI audit script (audits `.ko` against `abi_gki_aarch64.xml`, exits non-zero on non-allowlisted symbols)
  - `skills/L3-qualcomm-kernel-expert/references/qualcomm_kernel_architecture.md` — SoC codenames, module org, PIL crash recovery, ION→DMA-BUF migration, 16KB impact
- **HS-042** (hindsight note): Documents `vendor/qcom/opensource/` routing gap (kernel modules misrouted to `hal-vendor-interface-expert`). TC-101–TC-105 added to routing suite.
- **Research**: GKI 6.12.69 (`2026-03_r10`) confirmed current; CVE-2026-21385 (Qualcomm Display, actively exploited) documented as HS-043. 16KB deadline awareness maintained.
- Tests: routing 105/105 (100%), dirty_pages 0 errors (15 skills), skill_lint 0 errors.

### Correctness Assessment
- Routing accuracy maintained at 100% (105/105) after adding 5 new L3 test cases (TC-101–TC-105).
- Test count grew from 117 to 124 (+7) — healthy, no regressions.
- SKILL.md structure review: frontmatter includes `layer: L3`, `path_scope`, `version`, `parent_skill` fields; path tables are accurate; trigger conditions are path-based and concrete; 8 forbidden actions present (meets ≥5 minimum); parent-skill escalation rules well-defined.
- `check_qcom_kmi_symbols.sh` uses `abi_gki_aarch64.xml` directly — correct approach for KMI audit.
- HS-043 (CVE-2026-21385 active exploit) correctly flagged as requiring vendor kernel cherry-pick (GKI upgrade alone insufficient).
- Duration: 922s for 9 files across 2 commits — efficient.

### Alignment Assessment
- Addresses the open P1 from the 2026-04-13 review: Phase 6 is now fully defined.
- Phase 6.1 is the first concrete deliverable on the Phase 6 gate criterion (≥2 L3 OEM skills needed before phase closes).
- ROADMAP updated to v1.5 with gate criteria clearly stated — steward is self-directing appropriately.
- `dirty_pages.json` updated to cover 15 skills (14 L2 + 1 new L3) — scope-awareness maintained.

### Progress Assessment
- **Outstanding**: The steward received no Phase 6 definition and created one autonomously in a single session, then immediately delivered 6.1. This demonstrates high-quality autonomous initiative.
- Phase 6 gate requires ≥2 L3 skills; 1/2 delivered. Next target: L3-mediatek-kernel-expert (Phase 6.2).
- Research is active and proportionate — CVE tracking, GKI versioning, ASB awareness all maintained.

### Skill Quality
- `skills/L3-qualcomm-kernel-expert/SKILL.md` was created this session.
- **Note**: Android-SW skills are AOSP domain skills (path-indexed, used by the AOSP-focused agent internally). They are NOT Claude Code trigger-based skills and the pipeline `skill-quality-validator` is not applicable — it measures LLM trigger routing accuracy, which is not the quality dimension for AOSP domain skills.
- The repo's own quality gates apply: `skill_lint.py` 0 errors, `validate_dirty_pages.py` 0 errors, routing 105/105 — all pass.

### Risks Identified
- **Low risk — AOSP A16 source drop**: Phase 6.3 is gated on the Q2 2026 AOSP source drop. Steward is correctly monitoring via research. No action needed until the drop.
- **L3-aware router (6.4) pending**: TC-101–TC-105 tests document the L3 routing gap but the live router isn't updated yet. Next sessions should address `test_router.py` modifications for L3 path dispatch.

### Recommendations for Next Session
1. Begin Phase 6.2 — `L3-mediatek-kernel-expert`. Follow the same structure as 6.1 (SKILL.md + audit script + architecture reference). Target: MediaTek `vendor/mediatek/kernel_modules/` path scope, TINYSYS/SSPM firmware patterns, MT6XXX defconfig conventions.
2. Begin Phase 6.4 — update `tests/routing_accuracy/test_router.py` live router to dispatch `vendor/<oem>/` paths to L3 skills before L2 fallback.
3. Continue monitoring GKI 6.12 for the 16KB Play Store deadline (May 1/31 2026) — if OEM prebuilt audit is needed, capture it in a hindsight note.

---

## ARM MRS Steward

### Verdict: needs-correction

### What Was Done
- **EX-2d / EX-3 formalization**: EX-3 milestone formally added to ROADMAP with four sub-targets: EX-3a MRS refresh (newer build), EX-3b Armv9.7-A tracking, EX-3c GICv5 prep, EX-3d PMU monitoring.
- **Eval expansion**: +20 eval tests (488→508): 5 arm-reg, 4 arm-search, 3 arm-search_t32, 3 arm-gic_search, 3 arm-coresight_search, 2 arm-pmu.
- **Research**: Confirmed September 2025 MRS build (`AARCHMRS_OPENSOURCE_A_profile_FAT-2025-09_ASL0.tar.gz`) — newer than current Build 445 (March 2025). 4 additional Armv9.6-A FEAT_* identifiers found (FEAT_BTIE, FEAT_CMPBR, FEAT_F16MM, FEAT_MTETC). GICv5 Linux kernel driver v7 patches from July 2025 confirmed, not yet stable.
- **Auto-commit recovery**: Session left `note.txt` and updated `.claude/steering-notes.md` uncommitted — the auto-recovery script committed both.

### Correctness Assessment
- 508 eval tests all pass (100%) — consistent quality.
- `tools/eval_skill.py` expanded (+110 lines) for new test coverage — appropriate.
- MRS source files (Features.json, Instructions.json, Registers.json) not modified — hard constraint respected.
- **`note.txt` artifact**: The auto-commit captured a file containing a raw `claude --resume` command (`claude --resume 67b85524-175a-4632-9261-694c99144e11`). This is a debug/session management artifact and should not be in the repo. It is noise but not harmful.
- `.claude/steering-notes.md` write succeeded this session (was blocked for 9+ days in prior sessions). The steering notes file now exists and contains the P0 arm-pmu.md context — this is positive.
- Duration: 1271s for 4 files — proportionate to eval expansion work.

### Alignment Assessment
- Eval growth (318→423→453→461→488→508) is well within EX-2/EX-3 scope.
- EX-3 formalization is aligned with what the reviewer recommended.
- **P0 arm-pmu.md remains unresolved (day 9+)**. This has been flagged in every review since Apr 5. The new steering notes file correctly documents the issue, but the steward cannot self-resolve it.
- New MRS build (Sep 2025) identified — EX-3a-1 is actionable but requires human file download.

### Progress Assessment
- Eval suite growth: +20 tests in one session while maintaining 100% pass rate — good.
- EX-3 proposal now formally in ROADMAP: concrete and well-scoped.
- However, core P0 blocker (arm-pmu.md) persists. The steward is making productive progress around the blocker but the blocker itself is consuming cognitive resources across sessions.

### Skill Quality
- No skill.md files modified this session. No validator invocation needed.

### Risks Identified
- **[ESCALATE] P0 arm-pmu.md blocker (day 9+)**: Steward cannot write to `.claude/skills/arm-pmu.md`. The issue is now in the steering notes with clear evidence. Human must act. See escalation section.
- **`note.txt` junk file committed**: Contains a raw `claude --resume` command. Should be deleted and added to `.gitignore`. Minor but clutters the repo.
- **New MRS build blocked**: Sep 2025 build requires human download. EX-3a-1 cannot proceed without it.

### Recommendations for Next Session
1. Skip arm-pmu.md retry — human action is required. Focus on EX-3b (Armv9.7-A tracking) instead.
2. Add 4 new FEAT_* identifiers (FEAT_BTIE, FEAT_CMPBR, FEAT_F16MM, FEAT_MTETC) to the tracking document alongside the 18 previously documented.
3. Continue eval expansion toward the 520+ milestone — test coverage for the new FEAT_* extensions would be valuable.
4. Delete `note.txt` in the next session commit; add `note.txt` to `.gitignore` if this pattern recurs.

### Steering Notes (included here — cannot write to external `.claude/` repos from sandbox)

**2026-04-16 — Project Reviewer Feedback**

**Verdict**: needs-correction (P0 blocker persists; all other work excellent)

**Issues:**
1. P0 arm-pmu.md blocked day 9+ — human must grant write permission or create the file.
2. `note.txt` committed (contains raw `claude --resume` command) — repo junk.
3. Sep 2025 MRS build confirmed but requires human download (EX-3a-1).

**Required Actions (next session):**
1. Do not retry arm-pmu.md — document in session summary that human action is required.
2. Delete `note.txt`; add it to `.gitignore`.
3. Proceed with EX-3b (Armv9.7-A) and additional FEAT_* documentation.

---

## BSP Knowledge Steward

### Verdict: on-track

### What Was Done
- **Eval expansion 300→340**: 40 new eval cases (case_301–340) covering GIC NMI/GICv4.2 vNMI (10 cases), Panthor DRM/Mali CSF (10 cases), MIPI CSI-2 v4.0/v4.2 (10 cases), cross-domain Blackboard scenarios (10 cases).
- **Safety gates**: 82/82 safety gate tests + 422 schema validation tests pass, 0 failures.
- **Knowledge graph expansion**: 2 new seed scripts committed in the research auto-commit: `arm-coresight-full.py` (393 lines) and `linux-lpddr5x-ufs4.py` (364 lines). Graph grew from 1109 to 1167 nodes (+58).
- **ROADMAP updated**: Reflects 340 eval cases and 1109 nodes (pre-research measurement; perf JSON shows 1167 post-research).
- **`.claude/steering-notes.md` created**: Successfully written for the first time (previously blocked). Contains the P1 orphan fix and P2 items from the 2026-04-08 steering note.
- **Auto-commit**: Research session left 5 files uncommitted — auto-recovery committed them.

### Correctness Assessment
- All 82 safety gates and 422 schema validation tests pass — no regressions.
- Eval case coverage well-chosen: GIC NMI directly extends prior GIC NMI work; Panthor DRM extends the Panthor seed script added 2026-04-12; MIPI CSI-2 extends the MIPI CSI-2 v4 seed script; cross-domain Blackboard cases test the ITS mentor architecture.
- `gen_eval_cases_301_340.py` (987 lines) is a proper generator script — eval cases are not hand-crafted noise.
- **Node count discrepancy**: Log and ROADMAP report 1109 nodes; perf JSON reports 1167. The research phase added 2 new seed scripts post-ROADMAP update, creating the discrepancy. This is legitimate (ROADMAP was updated before research completed), but next session should update ROADMAP to reflect 1167.
- **`knowledge-graph/kuzu_db` binary committed**: The Kuzu database file (16KB binary) was committed to git in the research auto-commit. Database binaries should be in `.gitignore`. This is a process issue — Kuzu generates this file at runtime and it should not be tracked.

### Alignment Assessment
- Phase 4 work (eval expansion, graph maintenance) directly on ROADMAP.
- Target of 340+ cases (per 2026-04-13 recommendation) exactly met.
- New domains (GIC NMI, Panthor DRM, MIPI CSI-2) match the seed scripts added in recent sessions — the eval cases are properly grounding new knowledge.
- Phase 4 gate: eval cases target is ≥500; currently at 340. Still ~4 more sessions at current pace.

### Progress Assessment
- Eval: 200→250→260→290→300→340 — consistent acceleration, pace increasing from ~30 to ~40/session.
- Graph: 501→1076→1109→1167 — well past the Phase 4 ≥1000 target.
- Cross-domain Blackboard eval cases (case_331–340) are particularly valuable — they test the three-layer mentor architecture, not just domain knowledge.

### Skill Quality
- No skill.md files modified this session. No validator invocation needed.

### Risks Identified
- **`kuzu_db` binary in git**: `knowledge-graph/kuzu_db` (16KB binary) committed. Git is not designed for database binaries; this will grow on each commit and cause diff noise. **Next session must add `knowledge-graph/kuzu_db` to `.gitignore` and remove it from tracking.**
- **Node count in ROADMAP stale**: ROADMAP shows 1109 nodes but actual count is 1167 post-research. Minor, but should be updated.
- **Phase 2/3 formal exit criteria pending**: Both phases show ~90% — exit criteria require human review. This is not a steward issue but a dependency on Jonas's time.

### Recommendations for Next Session
1. **Priority**: Add `knowledge-graph/kuzu_db` to `.gitignore` and run `git rm --cached knowledge-graph/kuzu_db`. This prevents the database binary from accumulating in git history.
2. Update ROADMAP node count from 1109 to 1167.
3. Continue eval expansion — target 380+ (another 40 cases). Focus on domains with weaker coverage: LPDDR5X/UFS4 (new seed script), CoreSight (existing but complex), and additional Blackboard multi-domain scenarios.
4. Begin preparing Phase 2/3 exit criteria summary document for human review — this is the bottleneck for formal phase gate closure.

---

## LTC Steward

### Verdict: blocked

### What Was Done (2026-04-16)
- **No log file**: `logs/ltc-2026-04-16.log` does not exist. The steward did not run on 2026-04-16.

### Recent History (context for verdict)
- **2026-04-15 (1pm session)**: Hit API rate limit in 13s. Duration: 13s, 0 commits, 0 files changed. Compliance: 0 violations.
- **2026-04-13 (last meaningful work)**: 2 commits — FastAPI lifespan migration, LINE dedup fix; `CORS middleware` for Vercel Web Client; Test 1 (full escalation pipeline) PASS confirmed. Tests stable at 6 (unit test count from perf JSON).
- **Blocking issue documented in `reports/blocked.md`**: `ANTHROPIC_API_KEY` in `.env` is invalid (401 error). Phase 2 L2 quality eval cannot proceed without a working API key.

### Correctness Assessment
- No new code to assess.
- The last committed work (2026-04-13) was correct — lifespan migration, CORS, dedup fix all appropriate production hardening.
- Test 1 PASS (full escalation pipeline verified) is a meaningful integration test result.
- SaMD compliance check: 0 violations reported across all recent sessions.

### Alignment Assessment
- The steward is blocked on two compounding issues: (1) API rate limits hitting the 5x-daily schedule, (2) `ANTHROPIC_API_KEY` invalid for L2 quality eval.
- Phase 8 (Digital Surrogate PoC) and Phase 2 L2 eval are the active work fronts per ROADMAP — both are stalled.
- Last 3 days: 0 meaningful commits. This is a stall pattern.

### Progress Assessment
- Phase 2: ~95% — L2 quality eval blocked by API key (401).
- Phase 7/8: Sprint 4.5 complete; Sprint 5 (nighttime companion trial) not yet started.
- The 5x-daily schedule is creating rate limit pressure without proportionate output — the 1pm Apr 15 session consumed a quota slot for 13 seconds of work.

### SaMD Compliance Check
- `compliance_violations: 0` across all recent sessions.
- Japan data firewall intact (no Japan references in user-facing output — last confirmed 2026-04-13).
- Agent 3 (`system_prompt_template.xml`) core rules not modified.
- `blacklist_scanner.py` ran clean in last full session.

### Skill Quality
- No skill.md files modified. No validator invocation needed.

### Risks Identified
- **[ESCALATE] API key invalid (401)**: `ANTHROPIC_API_KEY` in `.env` is invalid. Phase 2 L2 eval cannot proceed. This has been documented in `reports/blocked.md`. **Human must update the API key.** See escalation section.
- **[ESCALATE] No run on 2026-04-16**: The steward did not fire at any of its scheduled times today. The 2026-04-15 rate limit may have caused a cascade. **Human should verify the cron schedule and check whether rate limits reset.**
- **Schedule efficiency**: The 5x-daily schedule is burning rate limit quota on 13-second sessions. Consider reducing to 2-3x daily until the API key blocker is resolved.

### Recommendations for Next Session
1. **Prerequisite**: Human must update `ANTHROPIC_API_KEY` in `.env` before the steward can make progress on Phase 2 L2 eval.
2. Once unblocked: Run Phase 2 L2 quality evaluation (the last ~5% for Phase 2 completion).
3. Begin Sprint 5 planning (nighttime companion companion — the Digital Surrogate nighttime PoC).
4. Verify LINE webhook `webhookEventId` dedup and `X-Line-Signature` verification — open action item from 2026-04-12 research.

### Steering Notes (included here — cannot write to external `.claude/` repos from sandbox)

**2026-04-16 — Project Reviewer Feedback**

**Verdict**: blocked

**Issues:**
1. No run on 2026-04-16. Rate limit hit in 13s on 2026-04-15. Zero meaningful output for 3 days.
2. `ANTHROPIC_API_KEY` in `.env` invalid (401) — Phase 2 L2 eval blocked. See `reports/blocked.md`.

**Required Actions (next session — after human resolves API key):**
1. Run Phase 2 L2 quality evaluation immediately. This is the only remaining item for Phase 2 completion (~95% → 100%).
2. Begin Sprint 5 planning and first deliverable (Digital Surrogate nighttime system prompt).
3. Verify LINE webhook `webhookEventId` dedup — open for 4 days.

**Context:** Phase 2 completion is gating Phase 3 start. The API key blocker and rate limit pattern are preventing forward progress.

---

## Cross-Project Observations

1. **Steering notes write permissions now working**: Both ARM MRS (`.claude/steering-notes.md`) and BSP Knowledge (`.claude/steering-notes.md`) successfully wrote steering notes this session — previously blocked for multiple days. Android-SW already had no steering notes (none needed since all work was well-directed). This is a positive system health signal.

2. **ARM MRS new MRS build (Sep 2025) ↔ BSP Knowledge graph**: The Sep 2025 MRS build contains Armv9.6-A features including FEAT_BTIE, FEAT_CMPBR, FEAT_F16MM, FEAT_MTETC. BSP Knowledge's GIC and CoreSight graph nodes could benefit from updated register data once the ARM MRS steward processes the new build. When EX-3a-1 (MRS refresh) completes, notify BSP Knowledge steward to consider a graph update for GIC/CoreSight data.

3. **LTC stall pattern vs. 3-day gap**: The 2026-04-13 review noted missing runs on 2026-04-13 (Monday). Today (2026-04-16, Thursday) we have missing LTC run + rate limit issues. Two "no-run" events in the same week for the same agent suggests either schedule reliability issues or cascading rate limit problems from the 5x-daily frequency.

4. **`kuzu_db` binary in BSP git**: This is the same class of issue as ARM MRS's `note.txt` — auto-commit recovery capturing junk files. The auto-commit mechanism is working but lacks filtering for binary/temp files. A `.gitignore`-aware pre-commit check in the auto-recovery script would prevent both types of artifact commits.

5. **Android-SW Phase 6.1 routing gap (HS-042)**: The `vendor/qcom/opensource/` kernel module routing gap (kernel modules misrouted to `hal-vendor-interface-expert`) documents a pattern that may exist for MediaTek too. When `L3-mediatek-kernel-expert` is built, the steward should check for analogous `vendor/mediatek/kernel_modules/` routing gaps and add corresponding hindsight notes + test cases.

---

## Priority Actions

| Priority | Steward | Action | Rationale |
|----------|---------|--------|-----------|
| P0 | ltc | Human must update `ANTHROPIC_API_KEY` in `.env` | Phase 2 L2 eval blocked (401). 3 days stalled. |
| P0 | ltc | Human must check LTC cron schedule — no run today | No log file for 2026-04-16; rate limit cascade likely |
| P0 | arm-mrs | Human must create `.claude/skills/arm-pmu.md` or grant write permission | Day 9+ blocker; steward cannot self-resolve |
| P1 | arm-mrs | Human must download Sep 2025 MRS build (`AARCHMRS_OPENSOURCE_A_profile_FAT-2025-09_ASL0.tar.gz`) | Newer than current Build 445; needed for EX-3a-1 |
| P1 | bsp-knowledge | Add `knowledge-graph/kuzu_db` to `.gitignore` and `git rm --cached` | Binary database committed to git; will grow |
| P1 | arm-mrs | Delete `note.txt`; add to `.gitignore` | Junk artifact committed via auto-recovery |
| P2 | android-sw | Build Phase 6.2 — `L3-mediatek-kernel-expert` | Second L3 skill needed to meet Phase 6 gate |
| P2 | bsp-knowledge | Update ROADMAP node count 1109 → 1167 | Stale metric from pre-research measurement |
| P2 | ltc | Review 5x-daily LTC schedule — reduce to 2-3x until blocker resolved | Rate limits burning quota on 13-second sessions |

---

## Escalations

**[ESCALATION] ltc-steward: API key invalid + no run today (day 3 stall)**
Details: `ANTHROPIC_API_KEY` in `/home/jonas/gemini-home/long-term-care-expert/.env` returns 401. Documented in `reports/blocked.md` since 2026-04-13. Apr 15 session hit rate limit in 13s (0 commits). Apr 16 — no log file at all. Phase 2 L2 eval (the last 5% of Phase 2) and Sprint 5 both stalled.
Recommended human action: (1) Update `ANTHROPIC_API_KEY` in `.env` with a valid key. (2) Check `crontab -l` to confirm LTC cron is healthy. (3) Consider reducing LTC cron frequency from 5x to 2x daily until the key blocker is resolved to stop burning rate limit quota.

**[ESCALATION] arm-mrs-steward: arm-pmu.md blocked day 9+**
Details: Steward cannot write to `.claude/skills/arm-pmu.md` due to Claude Code sandbox permissions. Documented with exact error message in `.claude/steering-notes.md`. The arm-pmu skill would expose 5,953 PMU events across 38 CPU profiles — currently inaccessible to the query tool.
Recommended human action: Either (a) manually create `.claude/skills/arm-pmu.md` using the patch in `/tmp/arm-pmu-md-patch.txt` (if it still exists), or (b) run a Claude Code session in the ARM MRS repo with `.claude/skills/` write permission granted, or (c) move the skill file to a non-`.claude/` path as a workaround.
