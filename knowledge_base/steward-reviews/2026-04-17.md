# Steward Review — 2026-04-17

> Reviewer: project-reviewer agent | Run time: 7:00 AM Asia/Taipei
> Fleet version: 2.1.104 (minimum 2.1.101) — OK for all agents that ran

---

## Summary

| Steward | Verdict | Commits | Key Finding |
|---------|---------|---------|-------------|
| android-sw-steward | on-track | 2 | Phase 6.2 complete (L3-mediatek-kernel-expert); routing 110/110 (100%); tests 124→132; Phase 6 gate satisfied |
| arm-mrs-steward | on-track | 2 | EX-3b advanced — 18/22 v9.6-A FEAT_* verified in Build 445; 508→527 eval tests; P0 arm-pmu.md still human-gated (day 10+) |
| bsp-knowledge-steward | needs-correction | 2 | Eval 340→400 (+60, excellent); but graph_nodes regressed 1167→937 in perf JSON — measurement anomaly or rebuild regression; 2nd consecutive auto-commit recovery |
| ltc-steward | on-track | 2 | Recovered from blocked status; session-end 17-test suite, 148/148 passing; webhookEventId dedup verified; compliance 0 violations |

---

## Android-SW Steward

### Verdict: on-track

### What Was Done
- **Phase 6.2 complete**: `L3-mediatek-kernel-expert` skill deployed:
  - `skills/L3-mediatek-kernel-expert/SKILL.md` (320 lines) — full L3 domain skill with MediaTek `vendor/mediatek/kernel_modules/` path scope, CONNSYS/WMT combo-chip, SCP/SSPM firmware, TINYSYS, EMI MPU, MT6XXX defconfig conventions, parent-skill (kernel-gki-expert) handoff rules
  - `scripts/check_mtk_kmi_symbols.sh` (246 lines) — KMI audit for MediaTek out-of-tree modules
  - `references/mediatek_kernel_architecture.md` (374 lines) — architecture reference
- **Phase 6 gate satisfied**: ≥2 L3 OEM skills deployed (Qualcomm + MediaTek). This was the P2 recommendation from the 2026-04-16 review.
- **HS-044** (hindsight note): Documents MediaTek kernel module routing gap — analogous to HS-042 for Qualcomm (exactly as the cross-project observation in the previous review predicted would be needed).
- **Routing tests**: 110/110 (100%) — 5 new L3 routing test cases added.
- **Research**: CVE-2026-0049 (critical, `frameworks/base/` zero-interaction DoS) and CVE-2025-48651 (high, StrongBox SE firmware). Both documented as HS-045. GKI 6.12.69 stable, no KMI changes.
- **Validators**: `skill_lint.py` 15 skills 0 errors; `validate_dirty_pages.py` 16 skills 0 errors.

### Correctness Assessment
- Routing accuracy maintained at 100% (110/110) after adding 5 new L3 test cases — no regressions.
- Test count grew 124→132 (+8) — healthy expansion.
- SKILL.md structure: frontmatter includes `layer: L3`, `path_scope` with 4 MediaTek path prefixes, `parent_skill: kernel-gki-expert`, `android_version_tested: Android 16 (GKI 6.12)`. Path tables are detailed and concrete. Trigger conditions are path-specific. Inherited-paths table documents L2→L3 override relationships clearly.
- `check_mtk_kmi_symbols.sh` mirrors the Qualcomm equivalent — consistent approach across OEM L3 skills.
- HS-045 correctly categorizes CVEs as security patches (not architectural shifts) and does not mark skills dirty — appropriate judgment.
- Duration: 913s for 9 files across 2 commits — efficient.

### Alignment Assessment
- Phase 6.2 directly on ROADMAP. Phase 6 gate (≥2 L3 OEM skills) now satisfied.
- Steward followed the 2026-04-16 review recommendations precisely: built Phase 6.2 next, checked for MediaTek routing gaps (HS-044), and continued research monitoring.
- ROADMAP updated to reflect Phase 6 status (6.1, 6.2 done; 6.3–6.5 planned).

### Progress Assessment
- **Excellent**: Two consecutive sessions delivering one complete L3 skill each (6.1 on 04-16, 6.2 on 04-17). The steward is operating at peak autonomous productivity.
- Phase 6 gate now satisfied. Remaining 6.3–6.5 are: A16 source delta (gated on Q2 drop), router L3 dispatch update, and maintenance mode definition.
- Research is active and proportionate — CVE tracking maintains AOSP security awareness.

### Skill Quality
- `skills/L3-mediatek-kernel-expert/SKILL.md` created this session.
- As noted in the 2026-04-16 review: Android-SW skills are AOSP domain skills (path-indexed, used internally by the AOSP-focused agent). The pipeline `skill-quality-validator` measures Claude Code trigger routing accuracy, which is not applicable to AOSP domain skills. The repo's own quality gates apply: `skill_lint.py` 0 errors, `validate_dirty_pages.py` 0 errors, routing 110/110 — all pass.

### Risks Identified
- **Low risk — Phase 6.3 blocked on A16 source drop**: Q2 2026 timing. Steward monitoring correctly via research sessions.
- **Phase 6.4 (L3-aware router dispatch) still pending**: This was flagged in the 2026-04-16 review. The routing test cases (TC-101–110) exist but live router dispatch logic for `vendor/<oem>/` → L3 may not yet be wired. Should be addressed now that Phase 6 gate is met.

### Recommendations for Next Session
1. Begin Phase 6.4 — update `tests/routing_accuracy/test_router.py` live router to dispatch `vendor/<oem>/` paths to L3 skills before L2 fallback. The test cases (TC-101–110) are already in place.
2. Phase 6.5 — define maintenance mode criteria (trigger conditions for skill updates, monitoring schedule, version bump rules).
3. Continue AOSP research. If the April ASB patch for CVE-2026-0049 lands, evaluate whether it requires skill content updates.

---

## ARM MRS Steward

### Verdict: on-track

### What Was Done
- **EX-3b advanced**: Verified 18 of 22 publicly-documented v9.6-A `FEAT_*` identifiers are already present in Build 445 — correcting the prior assessment that had this as "4 additional." `research/feat-tracking.md` now records the accurate picture.
- **Eval expansion**: 508→527 tests (+19), including presence tests (with `min_version`) and absence hallucination guards for unpresent FEAT_* identifiers. 527/527 pass (100%).
- **P1 items addressed from 2026-04-16 review**:
  - `note.txt` deleted ✅
  - `.gitignore` updated ✅
  - EX-3b advanced with real data ✅
- **P0 arm-pmu.md**: Correctly skipped per reviewer guidance. Day 10+ blocker unchanged — documented in session summary.
- **Research**: No new MRS build beyond Build 445. Armv9.7-A authoritative feature list confirmed (3 mandatory: FEAT_EAESR, FEAT_FDIT, FEAT_PAuth_EnhCtl — all absent from Build 445, hallucination guards in place). ARM C1 Lumex family announced (Armv9.3-A + native SME2) — added to EX-3d watch list. PMU upstream verified (56 files, SHA-256 match for a725/x925).
- Committed: 2 commits, 6 files changed.

### Correctness Assessment
- 527 eval tests all pass (100%) — no regressions.
- MRS source files (Features.json, Instructions.json, Registers.json) not modified — hard constraint respected.
- Key correction made: prior sessions reported "4 additional" v9.6-A features, but actual verification shows 18/22 already present. This is an important data integrity improvement.
- `note.txt` deleted and `.gitignore` updated — both cleanup items from previous review completed.
- Duration: 967s for 6 files — proportionate to research + eval work.

### Alignment Assessment
- EX-3b (Armv9.7-A tracking) and EX-3d (PMU monitoring) both on ROADMAP.
- Steward followed all reviewer recommendations: skipped arm-pmu.md, focused on EX-3b, continued eval expansion beyond 520.
- New ROADMAP tasks added (3d-2b, 3d-2c) — appropriate scoping for future work.

### Progress Assessment
- Eval suite: 318→423→453→461→488→508→527 — consistent growth.
- Data quality: the v9.6-A FEAT_* correction is the most valuable finding — it corrects a systemic overcount that would have compounded across eval tests and research documents.
- ARM C1 Lumex family tracking is proactive — these SoCs ship Q1-Q2 2026 and will need PMU data when available.
- **Upgrade from needs-correction to on-track**: All P1/P2 items from previous review addressed. The only remaining issue (P0 arm-pmu.md) is correctly acknowledged as human-gated.

### Skill Quality
- No skill.md files modified this session. No validator invocation needed.

### Risks Identified
- **P0 arm-pmu.md still blocked (day 10+)**: Human must act. Unchanged from previous reviews.
- **EX-3a-1 MRS refresh still blocked**: Sep 2025 build requires human download. Unchanged.
- **ARM C1 Lumex PMU data**: No upstream JSON yet. The steward correctly added this to the watch list but cannot resolve it until ARM publishes the data.

### Recommendations for Next Session
1. Continue EX-3b — add eval tests for the 4 v9.6-A features NOT in Build 445 (FEAT_BTIE, FEAT_CMPBR, FEAT_F16MM, FEAT_MTETC) if absence guards are not already comprehensive.
2. Check EX-3d-2b and 2c new tasks — verify they have clear exit criteria in ROADMAP.
3. Continue eval expansion toward 540+. Focus on cross-skill integration tests (e.g., instruction + register lookup chains).
4. P0 arm-pmu.md: do not retry — human action required.

---

## BSP Knowledge Steward

### Verdict: needs-correction

### What Was Done
- **Eval expansion 340→400**: 60 new eval cases (case_341–400) in two batches:
  - Phase work: 40 cases (341–380) covering LPDDR5X/UFS4, CoreSight ETF/ETR/STM/CTI, Blackboard cross-domain, edge/negative tests.
  - Research auto-commit: 20 more cases (381–400).
- **P1 kuzu_db gitignore**: `knowledge-graph/kuzu_db` added to `.gitignore` and deleted from tracking ✅. Exactly as recommended in 2026-04-16 review.
- **P2 ROADMAP node count updated**: Now shows 1167 (up from 1109) ✅.
- **Research**: 3 new knowledge graph seed scripts: `jedec-lpddr6.py` (294 lines), `jedec-ufs4-1.py` (258 lines), `linux-6-18-lts-updates.py` (215 lines). `build_base_graph.py` modified (+3 lines to integrate new scripts).
- **Safety gates**: 82/82 passing. MCP integration tests: 180 passing. Schema validators: 380 passing.
- Committed: 2 commits, 68 files changed.

### Correctness Assessment
- **[ISSUE] Graph node count regression**: The phase work session reported `graph_nodes: 1167` (verified via detailed post-build query breakdown: Component=727, PowerDomain=33, ClockSource=52, Register=49, Interrupt=86, FailureMode=220; 1119 relationships). However, the session footer and performance JSON both report `graph_nodes: 937`. This is a **230-node drop** from the same day's earlier measurement. Possible causes:
  1. The research session modified `build_base_graph.py` (+3 lines) and the new seed scripts may have replaced or conflicted with existing graph construction, resulting in fewer nodes after rebuild.
  2. The perf JSON may measure only the base graph (before custom/extended scripts run), while the phase work report measures the full graph.
  3. A bug in one of the 3 new seed scripts could be causing node creation failures.
  - **Previous day**: perf JSON showed `graph_nodes: 1167` → today: `937`. This is a net regression.
- **Auto-commit recovery (2nd consecutive day)**: Research session again left uncommitted changes that required auto-recovery. The auto-commit captured 24 files (20 eval cases + 3 seed scripts + 1 modified script). This is a recurring pattern — the research phase consistently fails to commit its own work.
- Eval cases themselves appear well-structured (proper generator scripts, not hand-crafted).
- Safety gates and schema validators all passing — no test regressions detected.

### Alignment Assessment
- Phase 4 work (eval expansion, graph maintenance) directly on ROADMAP.
- Eval cases 340→400 addresses the P2 recommendation from 2026-04-16 review (target 380+).
- New JEDEC and Linux 6.18 LTS seed scripts extend the knowledge graph into memory/storage subsystem — relevant to BSP engineers.
- Phase 4 eval target is ≥500; currently at 400. About 2-3 more sessions at current pace.

### Progress Assessment
- Eval: 200→250→260→290→300→340→400 — accelerating pace (60 cases in one session is a new high).
- Graph: 501→1076→1109→1167→**937?** — the regression needs investigation before declaring progress.
- Both P1 and P2 items from the previous review addressed — responsive to feedback.

### Skill Quality
- No skill.md files modified this session. No validator invocation needed.

### Risks Identified
- **P0 — Graph node count regression (1167→937)**: The perf JSON shows a 230-node drop. This needs immediate investigation: did `build_base_graph.py` modification cause a subset of seed scripts to be skipped? Is this a measurement artifact? The steward must verify the actual node count in the next session by running the post-build query and comparing against the 1167 breakdown.
- **Recurring auto-commit pattern**: Research phase consistently leaves uncommitted work. This is the 2nd day in a row. The auto-recovery script catches it, but uncommitted work could be lost if recovery fails. The steward should commit explicitly at the end of the research phase.
- **Phase 2/3 formal exit criteria still pending**: Human review needed. Unchanged.

### Recommendations for Next Session
1. **P0**: Investigate graph node count. Run `python3 scripts/build_base_graph.py` and then query node counts per label (Component, PowerDomain, ClockSource, Register, Interrupt, FailureMode). Compare against the 2026-04-17 phase work breakdown (1167 nodes). If the 3 new seed scripts (jedec-lpddr6.py, jedec-ufs4-1.py, linux-6-18-lts-updates.py) cause a regression, fix or revert.
2. **P1**: Commit explicitly at the end of the research phase — do not rely on auto-recovery. Add a final `git add -A && git commit` before the session ends.
3. Continue eval expansion toward 440+ (another 40 cases). The Phase 4 target of ≥500 is within 3 sessions at current pace.
4. Update perf JSON measurement to use the same post-build query method as the phase work report (ensuring consistency).

### Steering Notes (included here — cannot write to external `.claude/` repos from sandbox)

**2026-04-17 — Project Reviewer Feedback**

**Verdict**: needs-correction

**Issues:**
1. **Graph node count regression**: Phase work reports 1167 nodes (verified via post-build query), but session footer and perf JSON report 937. This is a 230-node drop. The 3 new seed scripts added during research and the `build_base_graph.py` modification (+3 lines) are the likely cause. Must be investigated immediately.
2. **Recurring auto-commit**: Research phase left uncommitted work for the 2nd consecutive day. 24 files captured by auto-recovery.

**Required Actions (next session):**
1. Re-run `python3 scripts/build_base_graph.py` and verify node count matches 1167 (or higher with the 3 new seed scripts). If regression confirmed, debug the new scripts.
2. Explicitly commit all work before the research session ends — do not rely on auto-recovery.
3. Ensure the perf JSON `graph_nodes` field uses the same measurement method as the phase work post-build query.

**Context:** Graph node count is a key Phase 4 metric (target ≥1000). A drop from 1167 to 937 would put the project below the target. This must be a measurement issue or a build regression — either way it needs resolution.

---

## LTC Steward

### Verdict: on-track

### What Was Done (2026-04-16, 1pm session — reviewed today as LTC's first 04-17 session hasn't run yet)
- **Session-end test suite**: `tests/unit/test_session_end.py` (17 tests) validates `/api/sayo/session-end` end-to-end. Covers: happy path, JSON separator parsing, markdown fence stripping, input validation (4 cases), GCS upload gating, `SAYO_MEMORY_MAX_SESSIONS=5` truncation, `care_reports/` blob path routing.
- **`webhookEventId` dedup verified**: 8/8 tests pass. Integration in `line_webhook.py:913-917` confirmed correct. Closes the 4-day-open reviewer action item from 2026-04-12.
- **`boto3` import bug documented**: Default `USE_BEDROCK=true` triggers `import boto3` which isn't in requirements. Root cause documented — fix is human-actionable (set `USE_BEDROCK=false` in Cloud Run env).
- **Research**: Taiwan LTC 3.0 launched January 2026 (aligned with Hana's mission). Gemini Live API: `gemini-2.0-flash-live-001` shuts down June 1, 2026 — removal/update needed. `gemini-3.1-flash-live-preview` should be verified before Sprint 5.
- **Total unit tests**: 148/148 passing, 0 regressions.
- Committed: 2 commits (7ce5e79 + d564777), 5 files changed.
- **Additional human commits after steward session**: Phase 9 (daily proactive push for 花菜) — 3 commits by Jonas at 19:55, 20:08 (04-16) and 06:43 (04-17). These are human-driven work, not steward output.

### Correctness Assessment
- 148/148 unit tests passing — no regressions.
- `test_session_end.py` uses `patch.dict(sys.modules)` to mock `google.cloud.storage` — appropriate pattern for testing without GCS credentials.
- `webhookEventId` dedup verification closes a concrete reviewer action item — evidence-based closure.
- `boto3` import bug correctly identified as a configuration issue (env var), not a code bug.
- Duration: 879s for 5 files — efficient and productive.

### Alignment Assessment
- **Recovery from blocked status**: The previous two reviews flagged LTC as blocked (API key invalid, rate limit issues, no runs). The 04-16 1pm session shows meaningful output — the blocker has been resolved.
- Sprint 4 (session memory) work is on ROADMAP Phase 8.
- `webhookEventId` dedup was an open reviewer action item — now closed.
- Research (Taiwan LTC 3.0, Gemini Live API) is aligned with maintaining up-to-date knowledge.

### Progress Assessment
- **Significant recovery**: From 3 days of zero output (04-13 through 04-15) to a productive session with 17 new tests and a verified integration fix.
- Phase 8 Sprint 4 advancing — session-end endpoint now has comprehensive test coverage.
- Phase 2 L2 eval remains the outstanding item for Phase 2 completion (~95% → 100%).
- **Upgrade from blocked to on-track**: Meaningful work completed, tests passing, compliance clean.

### SaMD Compliance Check
- `compliance_violations: 0` in perf JSON.
- Research compliance scan: 25 `skills/*.md` files scanned, 0 real violations. 23 false positives correctly identified (terms like `diagnosis` appear only in negative clauses "NEVER say...").
- Japan data firewall intact — no Japan references in user-facing output.
- Agent 3 (`system_prompt_template.xml`) core rules not modified.
- `blacklist_scanner.py` ran clean.

### Skill Quality
- No skill.md files modified this session. No validator invocation needed.

### Risks Identified
- **Gemini Live API model deprecation**: `gemini-2.0-flash-live-001` in `live_api_diag.py` shuts down **June 1, 2026** — 45 days away. Must be removed/updated before then. Flagged in research but no code change made yet.
- **`boto3` import bug**: `USE_BEDROCK=true` default causes import error. Human must set `USE_BEDROCK=false` in Cloud Run env or add `boto3` to requirements.txt.
- **Phase 2 L2 eval still pending**: Requires valid `ANTHROPIC_API_KEY`. Unclear if the key was refreshed or if the steward worked around it.
- **test_count discrepancy**: Perf JSON shows `test_count_before: 6, test_count_after: 6` despite 148 unit tests passing per the log. The perf JSON test_count metric may be measuring something different (e.g., integration tests or a specific test suite count). Should be investigated for measurement accuracy.

### Recommendations for Next Session
1. **P0**: Remove or update `gemini-2.0-flash-live-001` reference in `live_api_diag.py` — 45-day countdown to shutdown.
2. **P1**: Attempt Phase 2 L2 quality evaluation if `ANTHROPIC_API_KEY` is now valid. This is the last ~5% for Phase 2 completion.
3. **P1**: Verify `gemini-3.1-flash-live-preview` model availability — needed before Sprint 5 caregiver trial.
4. Coordinate with human on Cloud Run env var changes: `USE_BEDROCK=false` for session-end production enablement.
5. Investigate perf JSON `test_count` metric (shows 6 vs 148 actual) — ensure measurement reflects true test coverage.

---

## Cross-Project Observations

1. **Android-SW HS-044 predicted by cross-project observation**: The 2026-04-16 review noted that the Qualcomm routing gap (HS-042) likely had a MediaTek analogue. Today's session confirmed this — HS-044 documents the `vendor/mediatek/kernel_modules/` routing gap. This validates the cross-project observation methodology.

2. **Auto-commit recovery pattern persists in BSP Knowledge**: Two consecutive days of research sessions leaving uncommitted work. ARM MRS had this issue on 04-16 but resolved it today (clean commits, no auto-recovery needed). BSP Knowledge should follow ARM MRS's improvement pattern — commit explicitly before session end.

3. **ARM MRS data accuracy improvement → potential BSP benefit**: The v9.6-A FEAT_* correction (18/22 already present, not "4 additional") demonstrates the value of primary source verification. BSP Knowledge's ARM-related graph nodes (GIC, CoreSight) should be cross-checked against the corrected feature availability data when the graph node regression is resolved.

4. **LTC recovery from blocked**: The 3-day stall (04-13 through 04-15) ended with a productive 04-16 session. The concurrent human Phase 9 work (daily proactive push) suggests Jonas is actively engaged with the LTC project — both steward and human are contributing, which is the intended model.

5. **Eval expansion acceleration across projects**: Android-SW (+8 tests), ARM MRS (+19 tests), BSP Knowledge (+60 cases) — all three active stewards expanded their test/eval suites this session. This is a healthy fleet-wide quality improvement signal.

---

## Priority Actions

| Priority | Steward | Action | Rationale |
|----------|---------|--------|-----------|
| P0 | bsp-knowledge | Investigate graph node count regression (1167→937) | Key Phase 4 metric may have regressed below ≥1000 target |
| P0 | arm-mrs | Human must create `.claude/skills/arm-pmu.md` or grant write permission | Day 10+ blocker; steward cannot self-resolve |
| P0 | ltc | Remove/update `gemini-2.0-flash-live-001` before June 1 shutdown | 45-day countdown; breaks `live_api_diag.py` |
| P1 | bsp-knowledge | Commit explicitly at end of research phase | 2nd consecutive auto-recovery; uncommitted work risk |
| P1 | ltc | Human must set `USE_BEDROCK=false` in Cloud Run env | Unblocks session-end production enablement |
| P1 | arm-mrs | Human must download Sep 2025 MRS build | Needed for EX-3a-1 MRS refresh |
| P2 | android-sw | Begin Phase 6.4 — L3-aware router dispatch | Routing test cases exist; need live dispatch wiring |
| P2 | ltc | Run Phase 2 L2 quality eval (if API key now valid) | Last ~5% for Phase 2 completion |
| P2 | bsp-knowledge | Continue eval expansion toward 440+ | Phase 4 target ≥500; currently at 400 |

---

## Escalations

**[ESCALATION] arm-mrs-steward: arm-pmu.md blocked day 10+**
Details: Steward cannot write to `.claude/skills/arm-pmu.md` due to Claude Code sandbox permissions. Documented in `.claude/steering-notes.md` with exact error. The arm-pmu skill would expose 5,953 PMU events across 38 CPU profiles — currently inaccessible to the query tool. This has been escalated in every review since Apr 5.
Recommended human action: Either (a) manually create `.claude/skills/arm-pmu.md` using the steward's generated content, or (b) run a Claude Code session in the ARM MRS repo with `.claude/skills/` write permission granted, or (c) move the skill file to a non-`.claude/` path.

None of the other stewards meet escalation criteria today. LTC recovered from its blocked state. BSP Knowledge's graph node regression is flagged as P0 for the steward to address but does not rise to human escalation (steward can self-investigate and fix).
