# Steward Review -- 2026-04-09

## Summary

| Steward | Verdict | Commits | Key Finding |
|---------|---------|---------|-------------|
| android-sw-steward | on-track | 2 | Phase 5 milestones defined, live routing benchmark at 100% accuracy, 3 A16 hindsight notes (AVF/pKVM, SELinux/KeyMint, connectivity) |
| arm-mrs-steward | needs-correction | 1 | CoreSight expanded 54→69 registers, EX-2 milestone defined with numeric targets, 423 eval tests; arm-pmu.md P0 still blocked (day 4) |
| bsp-knowledge-steward | on-track | 2 | Orphan remediation complete (209 Components + 49 FailureModes + 43 others linked, 321 new relationships); graph 946 nodes; 232 eval cases |

---

## Android-SW Steward

### Verdict: on-track

### What Was Done
- **Phase 5 milestones defined** (5.1–5.5) in ROADMAP.md v1.3:
  - 5.1: A16 validation pass (not started)
  - 5.2: GBL bootloader skill refresh (not started)
  - 5.3: 16KB page size deep-dive (not started)
  - 5.4: Live routing benchmark — **100/100 test cases (100% accuracy)** ✅
  - 5.5: Documentation refresh — README updated Alpha→Beta v0.5 ✅
- **Live router implementation**: Replaced stub mode in `test_router.py` with real `route_task()` using path_scope parsing + keyword matching + primary topic detection (409+ lines modified)
- **Research session -- 3 new A16 hindsight notes**:
  - HS-037: AVF/pKVM LL-NDK, early boot VMs, FF-A, Ferrochrome, device assignment
  - HS-038: SELinux IOCTL hardening macro, KeyMint 4.0 moduleHash
  - HS-039: Unified ranging (UWB/BT/WiFi), AIS GATT, IMS API expansion
- **4 skills flagged dirty** (virtualization, security, connectivity, trusted firmware)

### Correctness Assessment
- All 3 validators pass: routing tests 100/100, dirty_pages 0 errors, skill_lint 0 errors
- Test count stable at 117 (appropriate — router test is separate from routing cases)
- Phase 5 milestones are well-structured with clear gate criteria and an M6 milestone added to the summary table
- Research hindsight notes are properly sourced from AOSP documentation
- AOSP publication cadence change (Q2/Q4 starting 2026) is a valuable forward-planning insight

### Alignment Assessment
- **Directly follows yesterday's #1 recommendation** ("Define Phase 5 milestones in ROADMAP.md") — excellent steering responsiveness
- Phase 5.4 (live routing benchmark) delivered same session as milestone definition — impressive velocity
- Research continues to build A16 forward intelligence, expanding the hindsight note library (now HS-037 through HS-039)
- Dirty page flagging on 4 skills sets up future A16 validation pass (Phase 5.1)

### Progress Assessment
- 780 insertions across 8 files — substantial, well-organized
- Duration: 1,103s (18.4 min) — efficient for scope delivered
- Phase 5 is 2/5 deliverables complete on its first day — strong start
- Routing benchmark at 100% exceeds the 95% target — over-delivered

### Skill Quality
- No skill files modified this session (content updates are in hindsight notes and dirty_pages.json)

### Risks Identified
- **Low risk**: 4 skills flagged dirty but not yet updated. This is expected — Phase 5.1 (A16 validation pass) will address them systematically.
- **Observation**: The 100% routing accuracy may indicate the test cases are too easy or the router is over-fitted to the existing 100 cases. Consider adding adversarial/ambiguous cases to stress-test the router.

### Recommendations for Next Session
1. Begin Phase 5.1 (A16 validation pass) or 5.3 (16KB page size guide) — both have clear deliverables and high value
2. Consider adding 10-20 adversarial routing cases to validate the 100% benchmark isn't an artifact of test-set alignment

---

## ARM MRS Steward

### Verdict: needs-correction

### What Was Done
- **CoreSight expansion**: 54→69 registers across 8 components (was 6)
  - CSTF (CoreSight Trace Funnel): new component — 8 registers (FUNNEL_CTRL, PRICTL, integration test, claim tags)
  - CSRT (CoreSight Trace Replicator): new component — 7 registers (IDFILTER0/1/2, integration test, claim tags)
  - Fixed parameterized register resolution bug (exact match now prioritized over suffix-stripping)
- **17 new eval tests** (406→423, 100% pass)
- **EX-2 milestone defined** in ROADMAP.md with numeric targets:
  - CoreSight: 70+ registers / 8+ components (currently 69/8 — nearly met)
  - PMU: 40+ CPU profiles (currently 36)
  - GIC: 50+ registers (currently 41)
  - Eval: 450+ tests (currently 423)
  - Future Milestones table added (GICv5, SMMU, CI/CD, MRS Refresh)
- **Steering notes file created** (`.claude/steering-notes.md`) — documented P0 blocking status with exact error messages and prepared content
- **Research**: Confirmed no new MRS build (v9Ap7+), no new PMU CPU profiles, GICv5 announced but no data yet

### Correctness Assessment
- 423/423 eval tests pass — no regressions
- CoreSight data follows established JSON schema (994 lines added)
- Source MRS files NOT modified — compliant
- `python3` used throughout — compliant
- Bug fix for parameterized register resolution is a real correctness improvement (exact match priority)
- Steering notes are well-structured and clearly document the permission blocking issue

### Alignment Assessment
- **P1 resolved**: Post-H8 milestones defined (EX-2) — directly follows yesterday's recommendation
- **P0 NOT resolved but properly documented**: arm-pmu.md update attempted 3 ways (Edit, Write, Bash) — all blocked by runtime permissions. The steward prepared the full content and documented the exact error. This is now a **human-action blocker**, not a steward compliance issue.
- EX-2 targets are reasonable and grounded in current progress trajectory
- Research correctly concluded "no changes needed" — appropriate restraint

### Progress Assessment
- 1,235 insertions across 7 files — substantial, focused data expansion
- Eval suite growth: 352 → 387 → 406 → 423 over 4 days (71 new tests in 4 days)
- CoreSight EX-2a is 69/70 registers, 8/8 components — nearly complete
- Duration: 1,150s (19.2 min) — efficient
- EX-2 provides formal tracking for work that was previously informal — good governance improvement

### Skill Quality
- No skill files modified — which remains the problem, but is now confirmed as a **permission issue, not a steward compliance issue**

### Risks Identified
- **P0 (day 4): arm-pmu.md blocked by runtime permissions** — the steward has now documented 3 distinct failed approaches with exact error messages. This is a genuine permission configuration issue requiring human intervention.
- **EX-2a nearly complete**: CoreSight at 69/70 registers means the target will be met next session, which may leave the steward without a clear CoreSight expansion goal. ETM and CTI expansion tasks are listed.

### Steering Notes (included in review)

**2026-04-09 -- Project Reviewer Feedback**

**Verdict**: needs-correction (downgraded from escalation — P0 is confirmed permission-blocked, not steward non-compliance)

**Issues**
1. **P0 (day 4)**: `.claude/skills/arm-pmu.md` still stale. Steward properly documented 3 failed write attempts in `.claude/steering-notes.md`. This is now solely a human-action item — the steward has done everything within its power.

**Required Actions (next session)**
1. Continue EX-2 data expansion (GIC registers are the lowest coverage area: 41/50 target)
2. If human has resolved the arm-pmu.md permission issue, update the file with the prepared 36-CPU content
3. Add eval tests for the new CSTF and CSRT components (mentioned as TODO in EX-2d)

**Context**: The P0 blocking status is now well-documented with exact error messages. The steward demonstrated good faith by attempting all available write methods and documenting the failures. Future reviews should not penalize the steward for this item unless human action has unblocked it and the steward still fails to act.

---

## BSP Knowledge Steward

### Verdict: on-track

### What Was Done
- **Orphan remediation** (`fix-orphan-connectivity.py`, 624 lines):
  - 209 orphan Components remediated
  - 49 disconnected FailureModes linked
  - 8 unlinked PowerDomains connected
  - 35 unlinked ClockSources connected
  - 321 new relationships added (513→834 total)
  - Registered in `build_base_graph.py` as final step
  - Validated: 0 orphan Components, 0 disconnected FailureModes
  - Script is idempotent (safe to re-run)
- **Research -- 2 new seed scripts, graph to 946 nodes**:
  - `linux-drm-panic-gpu-recovery.py` (282 lines) — DRM panic handler, GPU recovery patterns
  - `mipi-evs-thermal-netlink.py` (216 lines) — MIPI EVS, thermal netlink subsystem
- **7 new eval cases** (226–232, total 232)
- ROADMAP.md updated: M4.4 now includes orphan remediation as complete deliverable

### Correctness Assessment
- Orphan fix script is properly sourced (ARM TRMs, Linux kernel docs, AMBA specs) — not fabricated relationships
- Script is idempotent — good safety design
- Graph grew from 908 to 946 nodes (+38) while simultaneously fixing connectivity
- 7 new eval cases is steady growth (225→232)
- **Note**: Perf JSON now shows `graph_nodes: 946` — **metric is fixed!** (was 0 for 3+ days)
- Eval count growth: 225→232 (perf JSON shows before/after correctly)

### Alignment Assessment
- **Directly addresses yesterday's P1 recommendation** ("Address orphan graph nodes") — excellent steering responsiveness
- Orphan count reduced from 209+ to 0 — complete remediation, not partial
- Graph expansion continues toward 1000-node target (946/1000 = 94.6%)
- M4.4 updated in ROADMAP to reflect orphan remediation as complete
- New seed scripts (DRM panic, MIPI EVS/thermal) are relevant to BSP engineering

### Progress Assessment
- 1,207 insertions across 13 files — productive session
- Duration: 4,801s (80 min) — longest session of the three, but delivered substantial remediation + expansion
- Graph connectivity dramatically improved: 321 new relationships is a 62.6% increase in graph edges
- 946/1000 nodes — within striking distance of the Phase 4 target
- This session addressed the most critical outstanding risk (orphan nodes) while still expanding the graph

### Skill Quality
- No skill files modified this session

### Risks Identified
- **P2 (day 5): CLAUDE.md/ROADMAP.md phase inconsistency** — ROADMAP header still lists Phase 2 as "~90%" while Phase 4 is well underway with multiple deliverables complete. Not critical but misleading for readers.
- **P2 (day 5): 205+ skipped tests** — still undocumented/untriaged. Each session adds new eval cases without addressing the skipped test question.
- **Observation**: Duration (80 min) is 4x longer than the other two stewards. The graph_nodes metric fix suggests the build_base_graph.py rebuild may be time-consuming. This should be monitored.
- **M4.1 remaining items**: Graph versioning and knowledge gap detector still needed before M4.1 is complete.

### Recommendations for Next Session
1. Push toward 1000 nodes — only 54 more needed. This is a psychologically meaningful milestone and within reach in 1-2 sessions.
2. Complete M4.1 remaining items: graph versioning (date/hash tagging per ingest) and knowledge gap detector
3. Update CLAUDE.md to reflect Phase 4 progress (remove "Phase 2 is the active work front")
4. Investigate and document the 205+ skipped tests — are they intentional, obsolete, or broken?

---

## Cross-Project Observations

- **All three stewards responded to yesterday's steering recommendations**: Android-SW defined Phase 5, ARM MRS defined EX-2 milestones and documented the P0 blocker, BSP Knowledge fixed orphan nodes. This is the strongest collective steering response observed to date.
- **ARM architecture findings propagating across repos**: ARM MRS steward's research on v9.7-A FEAT_GCIE (GICv5) and FEAT_LSCP/TLBID/MPAMv2 matches BSP Knowledge's graph nodes on ARMv9.7 extensions. Android-SW's HS-037 (FF-A standardized TrustZone communication) directly relates to ARM MRS's GIC architecture work. The three repos are converging on a consistent understanding of ARM's 2025-2026 architecture evolution.
- **Orphan remediation pattern is reusable**: BSP Knowledge's `fix-orphan-connectivity.py` approach (idempotent relationship-adding script registered as a build step) could serve as a template for the ARM MRS steward if similar connectivity issues arise in its CoreSight/GIC/PMU data.
- **Duration variance**: Android-SW (18 min), ARM MRS (19 min), BSP Knowledge (80 min). The BSP steward's longer duration correlates with the graph rebuild being part of its workflow. This is acceptable given the productivity but worth monitoring if it increases further.
- **Recurring pattern resolving**: The "data expansion outpaces quality fixes" pattern noted for 4 days is now improving — BSP orphan nodes fixed to 0, ARM MRS has formal targets. Only the arm-pmu.md skill file remains as an outstanding gap, and it's a permission issue.

## Priority Actions

| Priority | Steward | Action | Rationale |
|----------|---------|--------|-----------|
| P0 | arm-mrs | **Human action required**: Grant write permission for `.claude/skills/arm-pmu.md` or update manually | Day 4; steward has exhausted all available approaches; 36 CPUs in data, ~8 in skill |
| P1 | bsp-knowledge | Complete M4.1 remaining: graph versioning + knowledge gap detector | Core Phase 4 deliverable incomplete |
| P1 | bsp-knowledge | Push graph to 1000 nodes (54 remaining) | Phase 4 quantitative target at 94.6% |
| P2 | bsp-knowledge | Update CLAUDE.md to reflect Phase 4 status | Day 5 carry-forward; misleading to readers |
| P2 | bsp-knowledge | Document/triage 205+ skipped tests | Day 5 carry-forward |
| P2 | android-sw | Consider adding adversarial routing cases to stress-test 100% benchmark | Quality hardening for Phase 5 |

## Escalations

**[ESCALATE] arm-mrs-steward: arm-pmu.md skill file — HUMAN ACTION REQUIRED (Day 4)**

The steward has now documented three distinct failed write approaches (Edit, Write, Bash) with exact error messages in `.claude/steering-notes.md`. The content for the update (36-CPU grouped listing by family) is prepared but cannot be written. This is definitively a runtime permission issue, not steward non-compliance.

**Recommended human action**: Either:
(a) Grant the steward write access to `.claude/skills/arm-pmu.md` in the next session (check permission config)
(b) Manually update the file using the steward's prepared content
(c) Delegate to factory-steward with explicit write authorization

The escalation from yesterday stands but is now **reclassified as a permission/configuration issue** rather than a steward behavior issue.

---

**Previous escalation RESOLVED: bsp-knowledge-steward orphan nodes**

The P1 escalation from yesterday (158+ orphan Components, 35+ disconnected FailureModes) has been **fully resolved**. The steward remediated 209 orphan Components, 49 FailureModes, 8 PowerDomains, and 35 ClockSources with 321 new relationships. Validated: 0 orphans remaining. The fix is idempotent and registered in the build pipeline.
